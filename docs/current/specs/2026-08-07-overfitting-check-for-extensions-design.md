# ガイドライン拡張の過剰適合点検の設計（ADR-0079）

- **Date**: 2026-08-07
- **対象**: ガイドライン拡張（原則・CLAUDE.md・スキルの追加/改定）の全経路に、過剰適合点検と点検結果の記録義務を導入する
- **関連 ADR**: ADR-0079（本設計の決定）。是正パターンの参照元: ADR-0032（観測可能な発動条件）/ ADR-0073（適用例への降格・根拠と世代・退役経路）/ ADR-0040（CLAUDE.md 事前判定）
- **契機**: 2026-08-05 の Issue-0033/0034 スキル化サイクルで、単一プロジェクト（LoopForAlpha）出所の教訓が汎用スキルの拘束的規範として spec 承認まで進んだ（検出はユーザー目視に依存。worklog `MakeAiInstructions-2026-08-05-07`）
- **スコープ外**: 退役候補の機械検出（Issue-0048 の領分）。`worklog-extract` のスコープ確定手順の変更（出所点検は `worklog-skillify` 側で行う）

## 設計の骨子

本ガイドラインは、開発対象とするシステムの種類・性質を限定せず、利用する AI モデルも限定しない。この前提を守るため、規範・手順・観点を追加または強化する拡張のすべてに「過剰適合点検」を課し、点検結果の記録を義務化する（ADR-0079）。

- ルールの正本は CONTRIBUTING.md の横断節に置き、各シナリオのチェックリストとスキル（`extend-guidelines` / `worklog-skillify`）は正本を参照する
- 点検は設計承認前（spec を作る場合は spec 確定前）に実施し、結果を定型ブロックで成果物に残す。点検ブロックの無い拡張 spec は確定させない
- 過剰適合の芽が見つかった場合は、除去ではなく是正パターン（降格・ゲート・退役経路）で汎用性を回復する。特定状況の知見はゲート付きで残す価値がある

## 変更 1: CONTRIBUTING.md に横断節「全シナリオ共通: 過剰適合の点検」を新設

配置: 「設計思想」節の直後、「シナリオ: 原則を追加・変更するとき」の前。挿入する節の全文:

```markdown
## 全シナリオ共通: 過剰適合の点検

### 背景

本ガイドラインは、開発対象とするシステムの種類・性質を限定せず、利用する AI モデルも限定しないことを前提に設計されている。特定のシステム種別に過剰適合した手順・ルールは他種のシステム開発で非効率・不合理になり、特定の AI モデルに過剰適合した手順・ルールは将来のモデルの性能発揮を阻害する（ADR-0079）。

### 適用対象

規範・手順・観点を追加または強化する拡張のすべて（原則の追加・変更、CLAUDE.md の規範追加、スキルの新規作成・改定）。誤記修正・表現の圧縮・参照の張り替えなど、規範の内容を変えない変更は対象外。

### 点検の観点

設計承認前（spec を作る場合は spec 確定前）に、以下の 3 観点を点検する:

1. **出所の偏り**: 根拠となった観測・課題・worklog エントリの出所プロジェクト数とモデル世代を実際に数える。単一プロジェクト・単一モデル世代の教訓を、無条件の拘束的規範へ昇格させていないか
2. **システム種別依存性**: 特定のシステム種類・技術スタック・テスト文化・開発体制を暗黙の前提にしていないか。前提が崩れるプロジェクトでもその規範は合理的か
3. **AI モデル/ツール依存性**: 特定モデル・ツールの挙動・制約（既知バグ、コンテキスト制約、ツール仕様）を暗黙の前提にしていないか。より高性能な将来モデルの性能発揮を制限する方向に働かないか

### 是正パターン

過剰適合の芽が見つかった場合、記述の除去ではなく以下の型で汎用性を回復する（特定状況の知見はゲート付きで残す価値がある）:

- **適用例への降格**: 拘束的規範ではなく「発火条件に紐づく適用例」として記載し、出所（プロジェクト・根拠 id）を明記する（ADR-0073 の型）
- **観測可能な発動条件でゲート**: 前提が成立する場合に限り適用されるよう、エージェントが観測できる発動条件を付ける。モデル依存の規範は事象確認済みモデルを列挙し、未確認モデルには適用しない（ADR-0032 の型。実例: CLAUDE.md の構造化質問ツール規範）
- **根拠と世代＋退役経路**: 根拠エントリ id と観測されたモデル世代を添え、前提が消えたときに退役させる経路を定義する（ADR-0073 の型）

### 記録義務

点検結果を次の定型ブロックで成果物に残す。spec を作る拡張では spec に、spec を作らない小規模拡張では当該 ADR の Context または Consequences に記載する。**点検ブロックの無い拡張 spec は確定させない。**

    ### 過剰適合点検（ADR-0079）

    | 観点 | 判定 | 根拠 |
    |------|------|------|
    | 出所の偏り | 問題なし / 要是正→<適用した是正パターン> | <出所プロジェクト数・件数・モデル世代の実数> |
    | システム種別依存性 | 同上 | <前提の有無と内容> |
    | AIモデル/ツール依存性 | 同上 | <前提の有無と内容> |
```

## 変更 2: 各シナリオのチェックリストへ参照行を追加（7 箇所）

追加する行（7 箇所とも同一）:

> - 過剰適合の点検を実施し、点検ブロックを記録したか（「全シナリオ共通: 過剰適合の点検」/ ADR-0079）

挿入先:

1. 「シナリオ: 原則を追加・変更するとき」のチェックリスト末尾
2. 「シナリオ: CLAUDE.md を更新するとき」の注意事項末尾（このシナリオにはチェックリストが無いため。文体のみ注意事項に合わせ「過剰適合の点検を実施し、点検ブロックを記録すること（「全シナリオ共通: 過剰適合の点検」/ ADR-0079）」とする。参照の書式は他 6 箇所と同一）
3. 「シナリオ: Skillを新規作成するとき」のチェックリスト末尾
4. 「シナリオ: ワークフロー起点スキル（start-work）を変更するとき」のチェックリスト末尾
5. 「シナリオ: 機能ブロック駆動の設計スキル（feature-block-design）を変更するとき」のチェックリスト末尾
6. 「シナリオ: 振り返りスキル（retrospective）を変更するとき」のチェックリスト末尾
7. 「シナリオ: 振り返りで抽出された課題に対策するとき」のチェックリスト末尾

「シナリオ: ADRを記録するとき」「シナリオ: 未決事項・課題を記録するとき」「シナリオ: CLAUDE.md を棚卸しするとき」は追加しない（記録・整理行為であり、規範を追加する拡張ではない。棚卸しは規範を減らす側のフロー）。

## 変更 3: `skills/extend-guidelines/SKILL.md` へ点検工程を追加

手順「4. brainstorming を開始する」の「brainstorming では以下を念頭に置く」リストへ追加:

> - 設計承認の前に、CONTRIBUTING.md「全シナリオ共通: 過剰適合の点検」を実施し、点検結果の定型ブロックを spec（spec を作らない場合は ADR）へ記録する（ADR-0079）

## 変更 4: `skills/worklog-skillify/SKILL.md` へ出所点検を追加

「手順」の 2 と 3 の間に新ステップを挿入し、以降を renumber する（3→4、4→5、5→6）:

> 3. **汎用パスの出所点検**（汎用スコープのみ）: 根拠エントリの出所プロジェクト数・モデル世代を数える。単一プロジェクトまたは単一モデル世代に偏る場合、CONTRIBUTING.md「全シナリオ共通: 過剰適合の点検」の是正パターン（適用例への降格・発動条件ゲート・退役経路。スコープ降格の提案を含む）の適用をユーザーへ提示する（ADR-0079）

あわせて「関連 ADR」節へ `ADR-0079（汎用パスの出所点検）` を 1 行追加する。

## 過剰適合点検（ADR-0079）

本拡張自身への点検適用（ドッグフーディング）:

| 観点 | 判定 | 根拠 |
|------|------|------|
| 出所の偏り | 問題なし | 契機事例は 1 件（本 repo 2026-08-05、worklog `MakeAiInstructions-2026-08-05-07`）だが、導入する規範は特定事例の症状値を含まない工程規範（数値ゲートなし）であり、根拠は本ガイドラインの設計前提（システム種別・モデル非限定）そのもの |
| システム種別依存性 | 問題なし | 点検観点・是正パターン・記録義務はいずれも開発対象システムの種類に言及せず、拡張という本 repo 固有の行為のみを対象とする |
| AIモデル/ツール依存性 | 問題なし | 点検・記録の工程規範でありモデルの能力を制限する方向の制約ではない。将来モデルでも記録義務は検証可能性の担保として機能する |

## 変更対象外

- `CLAUDE.md` / `docs/overview/principles.md` / template 対象ファイル → 変更なし。`scripts/sync-template.ps1` の実行は不要（CONTRIBUTING.md・skills/ は template 対象外）
- `worklog-extract` → 変更なし（スコープ確定は従来どおり。出所点検は skillify 側の委譲直前ゲートとして実施）
- README のスキル一覧 → 変更なし（新規スキルの追加は無い）

## 検証

1. CONTRIBUTING.md に節「全シナリオ共通: 過剰適合の点検」が存在すること: `Select-String -Path CONTRIBUTING.md -Pattern '全シナリオ共通: 過剰適合の点検'` が 1 件以上返る
2. ADR-0079 への参照件数: `Select-String -Path CONTRIBUTING.md -Pattern 'ADR-0079'` が 8 件以上（横断節内 + チェックリスト 7 行。節内の複数出現があるためこれを下限とする）
3. スキル 2 件への配線: `Select-String -Path skills/extend-guidelines/SKILL.md,skills/worklog-skillify/SKILL.md -Pattern 'ADR-0079'` が各ファイル 1 件以上
4. `worklog-skillify` の手順番号が 1〜6 の連番であること（renumber 漏れの目視確認）
5. プラグイン更新後のスキル本文反映: ユーザーに `/plugin marketplace update ai-driven-dev-principles` を依頼し、改定スキルを起動して返る本文と repo 実ファイルを突合する（Issue-0044 の運用知見）

## サイクル完了時の後処理

- ADR-0079 を Accepted へ昇格（実装完了・検証後。ADR-0019）
- 対応する open issue は無し（本サイクルはユーザー起点。close 対象なし）
