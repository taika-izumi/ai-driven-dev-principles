# ADR-0143: Claude標準サブエージェントの保護は書き込み系ツールの除去で成立させ権限モードに依存しない

- **Status**: Accepted
- **Date**: 2026-09-08

## Context

Issue-0136では検査委譲の共通保護条件（ADR-0141）を定め、Claude Code側はCLIを別プロセスとして起動し固定検査MCPだけを公開する構成で実証した（ADR-0142）。標準のAgent/Task機構から起動する子AIについては、ツール制限・権限継承・実行を伴う検査・再委譲が未確認のまま残っていた。

2026-09-08、Claude Code 2.1.263・Windows 11で、親に `Bash, Edit, Read, Write` を持たせた状態から標準のAgent/Taskで子を起動する実測を行った（実行記録: `docs/records/experiments/2026-09-08-claude-native-subagent.json`）。子の利用可能ツールを許可リストで絞った構成では、Bash・PowerShell・Write・Editがいずれも `No such tool available` で不発だった。一方、親を `acceptEdits` にして子に `Write` を渡した構成では、子が保護対象の代用品を実際に上書きし、作業ディレクトリ外への書き込みだけが拒否された。固定検査1操作だけを渡した構成では、正常1成功・変異1失敗・復元1成功と保護代用品4件の拒否を確認した。

ユーザーは実測結果の提示に対し、未確認事項は次セッションへ引き継ぎ、確定した範囲の反映を今行う案を選択した（2026-09-08、「1で」）。

同日さらに、エージェント定義ファイルのfrontmatterによる制限も実測した。拒否リスト（`disallowedTools`）でシェル・ファイル編集・ファイル作成・再委譲を外した子は、5ツールすべてを実際に呼び出して失敗した。接続の限定公開（`mcpServers`）は、`--strict-mcp-config` を併用すると子の起動自体が拒否されたが、併用しない場合は子だけが接続を受け取り、親のツール一覧には接続が現れなかった。

親の権限チェックを無効にした起動（`bypassPermissions`）でも実測した。ツールを `Read` だけにした子は書き込み系ツールを一切持たず、保護代用品は無変更だった。一方、`Write` を渡した子は保護代用品を上書きでき、`acceptEdits` では承認経路が無く拒否された作業ディレクトリ外への新規作成も成功した。

## Considered Alternatives

- **権限モードで子を制御する**: 追加設定なしで扱えるが、実測では `acceptEdits` の親から起動した子が作業ディレクトリ内の保護対象を上書きできた。パス単位の保護にならず、既存の共通条件（全操作経路の制限）を満たさない。
- **子の利用可能ツールから書き込み可能な内蔵ツールを外す**: 実測で書き込み・編集・シェルのすべてが不発になり、親が同じツールを保持していても子へ渡らなかった。設定は起動時のエージェント定義または起動引数で行うため、既存機能の範囲で足りる。
- **実測を記録するだけで規範へ反映しない**: 変更量は最小だが、次に標準サブエージェントへ検査を委譲するたび同じ確認を繰り返し、確認済みの構成と未確認の構成を取り違える経路が残る。
- **標準サブエージェントを実行を伴う検査の対象外と決める**: 判断は単純になるが、固定した1操作だけを渡す構成が実際に成立したため、成立する経路を捨てることになる。

## Decision

Claude Code標準のAgent/Task機構で実行を伴う検査を委譲するとき、保護の成立条件は**子の利用可能ツールから書き込み可能な内蔵ツール（シェル・ファイル編集・ファイル作成）を外すこと**とする。権限モードの指定はこの条件の代替にしない。

実行が必要な検査は、あらかじめ固定した1操作だけを子へ渡す構成で行う。渡し方は2通りを確認した。セッション側へ公開した接続を子のツール許可リストで1操作に限定する方法と、エージェント定義ファイルのfrontmatterで子だけに接続を公開する方法である。後者は親のツール一覧に接続が現れないため公開範囲が狭いが、`--strict-mcp-config` を併用すると定義ファイル側の接続が無効になり子が起動しない。実行そのものは既存Codexのサンドボックス内で行った（ADR-0142の構成を子へ付け替えた形であり、CodexをClaude Code利用者の必須依存にする決定ではない）。

再委譲は既定で可能なため、不要な場合は子の許可リストからAgent（Task）を外すか、定義ファイルの拒否リストへ入れる。どちらでも子は再委譲できず、孫まで起動した場合も孫の許可リストによる制限は保たれた。

確認済み範囲はClaude Code 2.1.263・Windows 11・`--print` 実行に限る。対話セッション（autoモード）での挙動、別OS・別版は未確認とし、既存の限定構成の成功をこれらへ読み替えない。

## Consequences

標準サブエージェントへ検査を委譲する経路が、確認済みの構成として使えるようになる。委譲元は起動前にツール許可リストを確認する作業が増えるが、確認は開始イベントの実ツール一覧で足りる。

権限モードでは保護できないことを明記するため、`acceptEdits` や自動承認の運用で子へ書き込み系ツールを渡している場合は、保護対象を作業ディレクトリの外へ置くか、ツールを外す必要がある。

エージェント定義ファイルは起動時に読み込まれるため、実行中のセッションで定義を足しても使えない。委譲元が新しい制限構成を使うには新しいセッションを起動する必要がある。

未確認範囲を明示したため、対話セッションでの実挙動と、別OS・別版の確認はIssue-0136の追加検証として次サイクルへ残る。

接続の限定公開と `--strict-mcp-config` は併用できないため、委譲元はどちらか一方を選ぶ。定義ファイルで子だけに公開する場合、セッション側に設定済みの他の接続は読み込まれるため、不要な接続は起動引数の拒否リストでツールごと外す（実測では設定済みの3接続のツールを親子とも一覧から外せた）。

親の権限チェックを無効にした起動では、作業ディレクトリの境界も保護にならない。保護対象を作業ディレクトリの外へ置く回避策はこの起動では成立せず、子から書き込み可能なツールを外す方法だけが残る。

- 改訂記録（追加実測: 定義ファイルのfrontmatterによる制限の確認、および親の権限チェックを無効にした起動の確認。2 回）: Context・Decision の確認済み範囲と実行構成、Consequences、過剰適合点検の出所欄・2026-09-08。Status は Accepted のまま維持
- **部分修正（ADR-0144）**: Decision が保護の成立条件として挙げた「書き込み可能な内蔵ツール（シェル・ファイル編集・ファイル作成）」という列挙は不完全であり、NotebookEdit のような列挙外の書き込み可能な内蔵ツールが残る。対話セッション（autoモード）で拒否リスト方式の子に残った NotebookEdit が保護対象の代用品を承認要求なしで上書きした実測により、書き込み系ツールの外し方は許可リスト方式に限ることとした（`skills/subagent-dispatch/references/inspection-isolation.md` の対応表にあった「外し方は許可リストと拒否リストのどちらでもよい」は成立しない）。本 ADR の骨格（書き込み系ツールの除去で保護を成立させ、権限モードを保護の代わりにしない）は維持され、Status も Accepted のまま維持する

## 過剰適合点検（ADR-0079）

| 観点 | 判定 | 根拠 |
|------|------|------|
| 出所の偏り | 問題なし | 実測は本リポジトリ1プロジェクト・2026-09-08の12 run（run-a1/a2/b2/c1/c2/c3/d/e1/e1b/e2/f1/f2。設定不正で起動しなかったbと、起動を拒否されたe1は経路の不成立として別に数える）。モデル世代はclaude-haiku-4-5-20251001（親子とも）、確定前レビューはclaude-sonnet-5。単一プロジェクト・単一世代の観測だが、規範化するのは当該ツール（Claude Code）の実行経路に限定した対応確認であり、ツールを限定しない一般規範へは昇格させていない |
| システム種別依存性 | 問題なし | 開発対象システムの種類・技術スタック・テスト文化に依存しない（対象はAIエージェントの起動構成）。引き写し箇所: `skills/subagent-dispatch/references/inspection-isolation.md` 手順2「全操作経路を制限する」を引き継ぐ（同手順はADR-0141の決定「書き込み可能な操作がすべて保護された経路だけを通ることを確認する」を規範本文へ実装したもの）。ツール別の確認というADR-0141のゲート（ツール・版・OSの明示）もそのまま保持している |
| AIモデル/ツール依存性 | 問題なし | 本決定はClaude Codeというツールの挙動に依存する内容だが、適用範囲を「Claude Code 2.1.263・Windows 11・print実行」に明示的にゲートしており、他ツール・他版・他OSへ無条件に適用しない。引き写し箇所: `references/inspection-isolation.md`「ツール別に確認する条件」の既存ゲート（版・OS・確認済み範囲と限界の併記）を引き継ぐ。将来モデルの性能発揮を制限する方向の要求は含まない（要求は起動時のツール構成であり、モデルの判断を狭めない） |
| 規範の廃止条件 | 定義済み | Claude Codeが子エージェントへのパス単位の書き込み制限（作業ディレクトリより細かい保護経路）を提供したことを実機ヘルプ・公式文書・実測で確認できた時点で、本決定の「権限モードを代替にしない」条項を見直し候補とする。逆に、ツール許可リストで書き込み系ツールを外しても子が書き込めた実測が1件でも出た場合は、本決定の成立条件そのものを再検討する |
