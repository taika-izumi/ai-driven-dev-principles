# Docker SandboxesのAIなし試験の実施記録

## 2026-09-09: ログインと初回作成

ユーザーが`0136-note-sbx-smoke-proposal.md`に対応する「ログインと、この範囲の試験を進める（推奨）」へ「1で」と回答した。Dockerログインを開始し、ユーザーがブラウザーで認証。CLIはログイン成功・終了0を返した。OpenAI認証やモデル起動は行っていない。

ログイン後の`sbx ls --json`ではsandboxes=[]。同名VMがないことを確認し、承認済みの名前・CPU2・メモリ2GiB・共有スキルなし・workspaceなし・当該VMdeny規則・固定shell digestでcreateを実行した。

結果: create終了1、`global network policy has not been initialized`。CLIは`sbx policy init <allow-all|balanced|deny-all>`を案内。作成後の一覧もsandboxes=[]だった。入力搬入、exec、回収、停止試験は未着手。VM未作成のためstopは実行していない。

証拠: 当該worktreeの`.tmp/sbx-smoke-20260909-01/before-vms.json`、`create.stdout.txt`、`create.stderr.txt`、`started-at.txt`。before-vms.jsonはdaemon起動の診断文を含むCLI出力原文であり、JSON単独として扱わない。合成原本のtracked.txtは準備時のSHA256と一致した。

## 追加で必要と判明した初期設定

`sbx policy init --help`で以下を確認した。

- ローカル初回起動前に全体ネットワークポリシーの初期化が必須。
- 初期設定は全ローカルsandboxesに適用され、当該VMだけの既定ではない。
- allow-allは全外向き通信を許可、balancedはAIサービス・パッケージレジストリ等を許可、deny-allは既定で拒否。
- 後から個別・kitの規則が重なる。deny-allの選択だけで将来のすべてのVM・kitの実効拒否を検証済みにはしない。
- `--sandbox`によるinit対象指定はクラウド専用。ローカル試験VMだけに初期化を限定できるとは扱わない。

初期化は試験の承認範囲に明示した「当該VMだけの規則」より広いため、ここで停止した。これはCLIの前提不足による停止であり、自動承認審査からの拒否ではない。未知フラグを削って再試行したり、通信許可を広げたりしていない。

## 次に提示する操作

`sbx policy init deny-all`でDocker Sandboxesの初期ネットワーク方針を設定し、その実体を`sbx policy ls --json`で確認する案。今回だけでなく、今後このPCで作るローカルsandboxesにも関わる。通常のDocker Desktop/LoopForAlphaのネットワーク設定を変更する操作ではない。

既定拒否を推奨する理由は、この試験にVMの外向き通信が不要なため。以後モデルやパッケージ取得先が必要になったら、当該用途の許可規則を別に確認する負担がある。設定は後で変更できるが、全ポリシーのresetや削除は今回行わない。

承認後は既存VMを再照会し、同名が無い場合に限り同じ固定イメージ・保護条件で作成を再試行する。その後は承認済み試験範囲に従う。

## 2026-09-09: deny-all承認後の再試行

ユーザーが「deny-allで初期化し、試験を再開する（推奨）」へ「1で」と回答。`sbx policy init deny-all`は終了0。`policy ls --json`でglobalのnetwork deny、resources=["**"]、activeを確認した（ADR-0155）。前後のVM一覧は空。

同じ固定argvでcreateを再試行。CLIはworkspaceなし、CPU2、memory=2g、指定digestの設定を解決したが、PREPARE IMAGEで終了1・500 Internal Server Errorとなった。出力は`.tmp/sbx-smoke-20260909-01/create-retry2.stdout.txt`と`create-retry2.stderr.txt`。不完全な試験を成功扱いせず、搬入以後は実施していない。

### 切り分け

`C:/Users/d12an/AppData/Local/DockerSandboxes/sandboxes/state/sandboxd/daemon.log`の19:41:39の該当リクエストには、内部原因として次が記録されていた。

`Cannot connect to the Docker daemon at unix://C:\Users\d12an\AppData\Local\DockerSandboxes\sandboxes\state\sandboxd\docker.sock`

起動ログでは19:37:21に同じsocketでserving、backend startedを出しているが、直後の内部照会でも接続不能となっている。Docker Hubからの通信拒否と認定する根拠はなく、deny-allを緩和していない。通常のDocker Desktop/LoopForAlphaのデーモンとは別のsbx内部経路。

- `sbx daemon status`: running。
- `sbx diagnose --json`: pass=12、warn=0、fail=0。仮想化・認証・ディスク等の基本診断は成功。ただし上記の実イメージ処理は失敗。`--upload`は使っていない。ローカル結果は`diagnose.json`。
- .NETのUnixDomainSocketEndPointでログのsocketへ接続し、GET /_pingによる読み取り確認を試みた。スラッシュ表記・Windowsネイティブ表記の両方が「無効な引数」で接続前に失敗。後者の結果は`socket-probe.json`。これだけで製品内部と同じ原因とは確定しない。
- `sbx ls --json`: sandboxes=[]。最終出力は`after-vms.json`。

[Docker sbx-releases Issue #157](https://github.com/docker/sbx-releases/issues/157)に、Windows版0.30.0で同種の内部socket接続失敗の利用者報告がある。一次の再現報告として参照するが、提供元による今回0.42.1の原因確定・修正保証ではない。掲載された状態フォルダの退避やDocker Desktop再起動は、同報告でも本症状を解消したとはされていないため、そのまま実行していない。

### 現在の状態と再開条件

Docker認証とsbx全体deny-allは設定済み。sbx 0.42.1は導入済み。試験VMは0台で、モデル・exec・搬入・回収は未実施。取得イメージの完成も確認できていない。既存Docker・LoopForAlpha・他worktree・原本を変更していない。

試験はsbx内部バックエンド不成立でblocked。独自のsocket修正、保護緩和、全体reset、状態フォルダの削除、未検証の版への入替えは行わない。対応する修正・有効な回避策が確認された場合に、影響範囲を確認して同じ試験を再開する。別基盤へ移行する場合は、比較資料の条件に戻り、クラウド利用や構成変更を個別判断する。

## Docker Desktopとの関係の追加確認

ユーザーから、LoopForAlphaでDocker Desktopを導入・利用済みだが今は使っていないとの説明を受け、読み取り確認した。Docker CLIの選択contextはdesktop-linuxで、接続先は`npipe:////./pipe/dockerDesktopLinuxEngine`。DOCKER_HOST・DOCKER_CONTEXT・DOCKER_CONFIG・DOCKER_TLS_VERIFYのプロセス環境変数は未設定。

当該contextへのversion照会は名前付きパイプ不存在で失敗。関連プロセス列挙ではsbxのみ確認し、Docker Desktopの関連プロセスは確認できなかった。一方、sbx daemon statusはrunning。これはLoopForAlphaのDocker Desktopが今回のsbx内部socketと同じ接続先ではないことを示す。既存Docker Desktopの設定・コンテナは変更せず、起動・再起動も行っていない。

[Dockerの導入要件](https://docs.docker.com/ai/sandboxes/install/)では現行sbxはDocker Desktop/Engineなしで利用可能と説明されている。Desktop停止だけで今回のsbx内部接続障害を説明できない、というのが現時点の評価。環境全体の間接的な影響まで否定したものではない。次セッションでDesktopの再設定・再インストールを原因未確認のまま行わない。

## セッション中断

ユーザーがコンテキスト増大を理由に新セッションへの切替を希望したため、この状態で中断する。承認済みの導入・ログイン・deny-all・固定試験は撤回されていない。新しいモデル起動・別基盤利用・保護緩和等へ承認を広げない。最新のsbxプロセス・認証・VM状態は再開時に読み取り確認する。
