# モデル裁量比較の事前確認・起動操作

- 状態: 2026-09-10、ユーザーが準備段階であることを確認したうえで「であれば1で」と起動を承認。事前確認の実施終了・移行条件未達。[結果](2026-09-10-model-discretion-preflight-results.md)を参照。本比較は対象に含めない。
- 目的: 非モデル確認では分からない、実モデル・親子の指示継承・内蔵編集の保護・停止・利用量回収を確認する。
- 正本: `D:/Dev/002_AiDev/WorkflowTrials/model-discretion-20260910/control/launch-packet/launch.json`。実行ファイル・引数配列・入力文・出力先・各条件の全保護パスを保存済み。

## 対象と順序

| 条件 | モデル・推論設定 | 試験用フォルダ名 |
|---|---|---|
| Codex・モデル裁量 | gpt-6-astra / medium | preflight-codex-discretion |
| Codex・既存手順 | gpt-6-astra / medium | preflight-codex-guideline |
| Claude・モデル裁量 | claude-opus-5[1m] / high | preflight-claude-discretion-minimal |
| Claude・既存手順 | claude-opus-5[1m] / high | preflight-claude-guideline-minimal |

フォルダはすべて上記試験ルートの直下。順番に実行し、各親は同じモデル・推論設定の公式の子を1体だけ使う。利用候補と制限はモデル内の2条件で揃える。

動作確認は、公開の目印ファイルの読み取り、親子による許可内ファイルの作成、専用の保護代用品への1回だけの書き込み試行、親子の指示・モデル名・ID・結果の回収。代用品以外の許可外ファイルへの変更試行や、製品コードの改修はしない。依頼文は4件の `*.prompt.txt` に保存した。

正常終了の回収後、外側からの停止が未確認の経路は同じ引数と `*.stop-prompt.txt` で確認する（最大4条件）。親子の準備完了ファイルと、子の60秒待機を観測したら、管理側が保持する今回のProcessハンドルからKill(true)し、捕捉済み子孫の終了も確認する。無関係なプロセス名で一括停止しない。共有デーモン側へ残った実行があるなど停止を証明できなければ、後続は起動しない。

## 実行経路と影響範囲

- Codex: `C:/Users/d12an/AppData/Local/Programs/OpenAI/Codex/bin/codex.exe exec --json ...`。名前付き保護プロファイル、approval_policy=never。モデルとの通信は通常ユーザー側のCLI、生成コマンドのネットワークは無効。
- Claude: 同じCodexの `sandbox --include-managed-config ...` 内で `C:/Users/d12an/.local/bin/claude.exe --print --output-format stream-json --verbose --forward-subagent-text ...`。permission-mode=dontAskと明示したツール許可を使い、OS側で書き込み範囲を制限する。Claude本体のモデル接続に必要なネットワークを有効にするため、この外側プロセスにはドメイン限定の通信保護を実証していない。Web/MCPの追加接続・外部公開を依頼しない。
- 作成側が書き込むのは当該試験用フォルダと公式サンドボックスが許す一時領域。管理側はcontrol/launch-packetへイベント、stderr、判定と時刻を保存する。CLI自身によるセッション等の通常記録はあり得る。通常設定・認証情報のコピーや変更は行わない。
- 元リポジトリ、採点原本、相手条件の成果物は保護対象。Python、検査依存、読み込むプラグインは読み取り専用として指定する。手動のACL変更はしないが、公式WindowsサンドボックスによるACL適用は行われる。過去の拒否継承が残った事実は準備記録に残しており、通常環境への影響が皆無とは主張しない。
- 通常の追加メモリと不要なプラグイン・MCPを除外する設定を使用する。Claudeの祖先フォルダに.claudeがないことと、課金先を変える8環境変数が当該通常ユーザーの実行環境にないことを確認した。起動時にも認証方式が変わっていれば止める。

## 確認済みと未確認

- 確認済み: 固定入力・固定検査の識別力、認証状態、Codexの条件別入力表示、固定プラグインの読み取りと内容一致、4条件の固定コマンドの書き込み境界、同4条件での既存テスト23件成功。
- 未確認: 要求どおりの実モデル、親子への指示継承、モデルの内蔵編集経路、実モデルの親子停止、親子の利用量集約、Claudeの保護下のモデル接続。自己申告だけでpassにせず、公式イベントと外側からの実体確認で判定する。子の利用量を親合計へ二重加算しない。取得不能はnullと理由を残す。
- JSONの出力先、終了コード、親子対応とモデル名を回収し、失敗・不明があれば本比較を開始しない。全項目がpassになっても、この承認範囲だけでは比較本体を起動しない。

## 費用・利用者の操作

事前確認の時間見込みは各モデル5〜15分程度（未実測の概算）。利用トークンと契約枠の消費量は事前不明で、結果から記録する。追加購入・API課金経路・利用枠リセットは使わない。ADR-0169の必要性と進捗に従い、新たな固定時間枠は設定しない。比較本体の各30分・計240分は未使用のまま。

利用者に求める操作は起動の採否だけ。ログインのやり直しや設定変更は予定しない。起動承認と実行環境の権限が揃わなければ実行しない。作成ファイルは戻せるが、消費した利用枠やモデルへ送信済みの入力は取り消せない。送信するのは本事前確認の入力、許可された固定資材・手順、実行結果であり、実会話ログ・製品データ・認証情報は含めない。


実施時の補正: Claudeは製品コードを含めない上表の最小入力を使用。子はpreflight-childの起動定義でOpus 5/highを指定し、CLAUDE_CODE_TMPDIRを当該作業領域内へ設定した。Codexの共通補助スキル指定はADR-0172へ反映。acceptEditsへの変更案はcontrol/launch-packet/launch-edit-permission-candidate.jsonに保存しただけで未適用・未承認。
