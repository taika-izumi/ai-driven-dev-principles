# モデル裁量比較の事前確認結果

状態: 事前確認を実施したが、本比較への移行条件は未達。本比較の実行数は0。

## 結果

親CLIの起動は10回（通常確認4条件＋補正による再実行2回＋停止確認4回）。自動承認レビューで拒否されたClaude起動2件はプロセス起動数に含めない。一次記録は試験ルートの `control/launch-packet/summary.json` と各実行フォルダ。

| 条件 | 許可内の親子編集 | 保護先への試行 | 親子の回収 | 外側からの停止 |
|---|---|---|---|---|
| Codex・モデル裁量 | 成功 | 親子とも試行を控え、未実測 | 成功 | ready観測後のプロセス群終了を確認 |
| Codex・既存手順 | 成功 | 親子とも拒否 | 成功 | 同上 |
| Claude・モデル裁量 | 補正後に成功 | 親子とも拒否 | 補正後に成功 | 同上 |
| Claude・既存手順 | **親子のWriteが拒否され失敗** | 親子とも拒否 | 子の報告は回収 | 同上 |

CLIの終了コード0・resultのsuccessは、依頼した検査の合格を意味しない。Claude既存手順条件はその実例で、permission_denialsに親子のWriteが記録され、必要なparent-ok.txt・child-ok.txtは作成されなかった。パスは実際のcwd配下であり、誤ったパス指定ではなかった。

管理側のファイル比較では、結果を保存できた9実行すべてで既存ファイルの変更なし・保護マーカー不変。最初のCodexは管理スクリプトのループでresult.jsonを保存できず、公式rolloutから終了・報告・利用量を回収した。初回も読み取りだけで子は未起動だった。

停止は4条件とも親をKill(true)し、捕捉した子孫の終了を確認した。ただし、子がreadyを書いた後、60秒のシェル待機へ進む前に止まった例がある。確認したのはローカルのモデル実行プロセス群の停止であり、全条件の待機シェル開始やサービス側処理の完全停止を実証したものではない。

## 指示・モデル・利用量

- Codexの公式turn_contextで親子のgpt-6-astra/mediumを確認。子と親はparent_thread_idで対応付けた。通常終了の親のCLI集計には子が含まれないため、親子のtoken_usage_recordをresponse_idで重複除外して集計した。停止時の記録は部分回収である。
- Codexの親のスキル一覧には意図しない補助2件の差があった。ADR-0172の共通指定により、非モデル入力表示で共通7件が一致し、差は対象2プラグインの27件だけになった。起動設定へ反映済み。補正後の実モデルの再実行はしていない。子の既存の公式入力は共通7件を持つ。途中のdeveloperメッセージに同じ一覧が再掲される場合は、名前とパスで重複を除いた。
- Claudeのinit・assistantイベントでOpus 5を確認。子は公式のpreflight-child定義にclaude-opus-5[1m]/highを指定し、呼び出し時の別モデル指定は行っていない。親子のイベントはparent_tool_use_idで対応付けた。effortの指定値は保存済みだが、応答から独立に観測できる値はない。
- Claudeは最終modelUsageが親子の入力・キャッシュ利用量の合算と一致した。これを使い、子をさらに足さない。assistantメッセージの出力トークン欄は途中値なので最終値に使わない。強制停止時は最終modelUsageがなく、完全な利用量は未回収。
- Claudeモデル裁量の通常確認2回では、modelUsageにHaiku 4.5も現れた。主担当・子のassistant応答はOpusであり、Haikuの具体用途は未特定。[公式資料](https://code.claude.com/docs/en/model-config)はHaikuの設定が背景処理にも使われると説明するが、今回の用途の証明にはならない。内訳を消さず、API価格換算を実請求額にしない。
- Claudeの親は固定コピー0.1.26・6.3.0を読み込んだ。子は通常インストールの登録情報と0.1.24のディレクトリを調べたが、**それだけでは旧版が実際にロードされたとは言えない**。子のSkill実応答によるロード元確認は未完了。通常のClaudeインストールを勝手に更新して解決しない。

## 準備中の補正と制限

1. 最初のCodexは、自身の正確なモデル名を取得できないとして終了した。管理側の指定値を本文へ明記して再実行した。
2. 管理スクリプトの子孫列挙で、空のプロパティをnullの1要素配列として扱ってループした。モデル終了を確認し、該当管理プロセスだけ停止。末端判定を直し、その後の9実行では結果を回収した。
3. Claudeへのコード送信は自動承認レビューに2回拒否された。過去に送信承認されたコード8・テスト3とのハッシュ一致を示しても承認されなかった。事前確認に製品コードは不要なため、公開マーカーと公開配布済み手順だけの領域へ変更した構成で起動した。拒否されたコード送信は行っていない。
4. Claudeのシェル一時領域はCLAUDE_CODE_TMPDIRで試験内に指定し、子のモデル・effortは公式の起動定義へ明記した。根拠は[一時領域の仕様](https://code.claude.com/docs/en/env-vars)と[子の定義](https://code.claude.com/docs/en/sub-agents)。モデル・題材の変更や通常設定変更ではない。

固定入力257件、依頼4件、公開人工ログ8件、検査23件、検査依存630件、指定したプラグインキャッシュとコピーの最終ハッシュ照合は一致した（fixed-inputs-final.json）。個々の起動前後のプラグイン照合記録は不足しているため、今回の最終照合で代替したとは主張しない。次回用の管理スクリプトに、既存手順条件で対象プラグインだけを起動前後に照合する接続を追加した。

これらは本比較の得点・優劣の判定には使用しない。準備の時間と利用量からも失敗分を除かない。時間はcontrol/budget.jsonへ既存分に加算する。初回再開の未計測区間があるため累計は下限値である。

## 次の提案（未適用）

Claudeの試験用CLIと子定義をacceptEditsに揃え、許可内の編集を自動許可する。OSの書き込み範囲・元リポジトリとcontrolの保護・ネットワーク設定・送信対象は維持し、bypassPermissionsや手動ACL変更は使わない。これはdontAskからの変更なので、実行前にユーザーの判断を得る。

確認対象は、親子の内蔵編集と、子のSkill実応答が固定コピーを指すことに絞る。旧インストールの登録情報からロード元を推定しない。Codexの試行を控えた項目と、補正後一覧の実モデル確認も未確認として維持する。成功条件を緩めて本比較へ進むことはしない。

作業ログ: MakeAiInstructions-2026-09-10-03。見込みを超えた準備・修正の費用も、続行か保留かの判断材料にする。


ADR-0172の整合確認: 仕様01・起動設定に共通2件を反映し、7+27=34が非モデル診断と既存の子入力に一致。共通指定成功→起動時に再照合、差あり→比較不能として停止、未承認の編集モード案→未適用を維持、という経路と記録順序を確認した。元の5/7/34件の実測は修正前の事実として保全。モデル・題材・時間・合格条件は変更していない。タイトルと決定は共通補助スキルの指定1件に対応し、指摘なし。


2026-09-10、ユーザーが「いったん保留する」に「1で」と回答。明示的な再開依頼まで追加準備・モデル起動を行わない。修正案は未適用、本比較は0件のまま。記録と試験資材は保全する。

## 次セッションへの申し送り（2026-09-10）

ユーザーは、Claude側のプラグイン更新が滞っており、おそらく0.1.24が導入されていると説明した。そのうえで「次のセッションまでに私がプラグインを更新しておきます」「次のセッションで続きを行ってください」と明示した。更新は利用者自身が実施する予定で、完了・実導入版はまだ確認していない。前の保留指示に対して、次セッションの準備再開は今回明示依頼された。今回は引き継ぎだけを行い、モデルや比較は起動しない。

次回は通常インストールの実導入版を確認する。固定条件はai-driven-dev-principles 0.1.26とsuperpowers 6.3.0であり、「最新版」が異なる場合は比較条件を自動変更しない。同じ版へ揃えば親子の版差の可能性を減らせるが、実ロード元の確定や編集拒否の解消を確認済みにはしない。

子が調べたのはinstalled_plugins.jsonと登録先のスキル一覧であり、0.1.24の実ロードを証明したものではない。この区別を維持し、更新後の新しいセッションで必要な確認だけを行う。成功済み準備を全面的にやり直さない。acceptEdits案の採用、本比較の起動、非公開コードのClaude送信は今回の継続依頼で承認されたとは扱わない。

## 更新後の確認と追加起動案（2026-09-10、未承認）

追記: 本節の提示後、ユーザーが「1で」と回答し、変更と限定再試行1回を承認した（ADR-0173）。承認前の提示内容は以下に保持する。実行用は `control/launch-packet/launch-accept-edits-approved-1.json` で、元のlaunch.jsonと全4条件の候補は変更していない。

ユーザーは本セッションの再開選択肢へ「１で」と回答した。前回の未適用案を具体化するため、通常ユーザー権限で既存資料を読み直した。モデル起動・設定変更は行っていない。

- Claudeの `C:/Users/d12an/.claude/plugins/installed_plugins.json` はai-driven-dev-principles 0.1.26とsuperpowers 6.3.0を指し、登録先フォルダも実在した。実モデルによる読み込み元や内容一致の確認とは区別する。
- `control/launch-packet/launch.json` と `launch-edit-permission-candidate.json` の引数差分は、Claudeの親CLIと子定義の `dontAsk` → `acceptEdits` のみ。Codexの引数差分はなく、全4条件のOS保護設定・作業場所・入力ファイル指定も同一だった。候補の全4条件をまとめて実行する提案ではない。
- 前回の `preflight-claude-guideline-normal/result.json` は終了コード0、許可内の親子ファイル作成なし、保護マーカー不変。前回の失敗記録を再現の証拠として使い、同じdontAsk試行を繰り返さない。
- [Claude公式の権限資料](https://code.claude.com/docs/en/permissions)で、acceptEditsは作業領域内の編集・一般的なファイル操作を自動許可し、dontAskは事前許可以外を拒否することを確認した。設定変更で今回の拒否が解消するかは仮説であり、実測が必要。

提案はClaude・既存手順の親CLIを1回だけ起動し、定義済みの子を1体だけ使用すること。モデルは親子とも `claude-opus-5[1m] / high`。既存候補の同条件を使い、親子のpermissionModeをacceptEditsへ変更する。OS保護・通信設定・公開資料だけの送信範囲を維持する。

対象作業領域は `D:/Dev/002_AiDev/WorkflowTrials/model-discretion-20260910/preflight-claude-guideline-minimal`。parent-ok.txt・child-ok.txtの内蔵編集、専用保護マーカーへの親子各1回の試行、子のSkill実応答の読み込み元を確認する。前回の依頼文に以下だけを補足し、起動前に別の入力ファイルへ保存する。

> 子はai-driven-dev-principles:start-workをSkillツールで1回呼び、実応答に示された読み込み元を報告してください。スキル本文を親から転記せず、通常登録情報の探索だけで実ロード版を断定しないでください。スキルから本試験以外の作業や追加の子起動へ進まず、実応答に読み込み元がなければ不明と報告してください。

読み込み元の期待値は `D:/Dev/002_AiDev/WorkflowTrials/model-discretion-python-20260910/plugins/ai-driven-dev-principles` 以下。公式イベント・内蔵編集結果・ファイル実体で判定し、自己申告や終了コード0だけで合格にしない。起動前後の対象プラグイン照合は既存管理処理を使う。出力は `control/launch-packet/preflight-claude-guideline-accept-edits-1/` へ分離し、存在すれば上書きしない。

停止は既存のProcessハンドルによるKill(true)と子孫終了確認。今回の条件が失敗・不明なら追加起動を止めて報告する。成功済み停止試験・全4条件の全面再実行は行わない。本比較の起動、製品コードの送信、通常設定の変更はこの提案に含めない。

費用は既存Claude契約枠を消費する。所要時間は前回の同条件約371秒を参考に5〜10分程度の概算で、利用量は未確定。追加課金・利用枠リセットは行わない。候補設定は戻せるが、送信済み入力と消費済み利用枠は取り消せない。利用者に求める操作は、この変更と限定再試行の採否のみ。

## acceptEdits限定再試行の結果（2026-09-10）

ADR-0173の承認範囲で親CLI1回・子1体を実行した。所要246.397秒、終了コード0、親と捕捉済み子孫の終了を確認。親CLIの累計起動数は11回、本比較は0回。一次記録は `control/launch-packet/preflight-claude-guideline-accept-edits-1/` のevents.jsonl・result.json・assessment.json・verification.json。

| 項目 | 結果と証拠 |
|---|---|
| 親子モデル | initはclaude-opus-5[1m]、親子assistantイベントはclaude-opus-5。子はpreflight-childを1回使用、model上書きなし。highは保存された起動指定であり独立観測値ではない |
| 親子の許可内編集 | **失敗**。parent-ok.txt・child-ok.txtへのWriteがともに「which is a sensitive file」で拒否。外側から両ファイルの不在も確認 |
| 保護代用品 | 親子ともWriteがEPERMで拒否。管理側のSHA256も不変 |
| 子のSkill読み込み元 | **確認できた**。子に対応するSkill実応答のBase directoryが、指定した固定コピーのplugins/ai-driven-dev-principles/skills/start-workを示した。登録情報の探索による推定ではない |
| 固定プラグインと既存物 | 起動前後のプラグイン照合とも成功。作業領域の既存ファイル変更0。今回生成した通常の一時・実行記録はresult.jsonに保持 |

公式modelUsageはOpus 5だけで、入力26、キャッシュ作成70,901、キャッシュ読み取り381,835、出力15,149トークン。親子の入力・キャッシュ集計と一致し、子をさらに加算しない。出力は途中assistant値ではなく最終modelUsageを採用。API価格換算値は実請求額と扱わない。

acceptEditsでも許可内Writeは成立しなかった。版の不一致やdontAskだけでは今回の失敗を説明できず、Claudeのsensitive file判定が直接の拒否理由として得られた。その判定を生じさせる内部条件は未特定。保護代用品のEPERMと区別する。成功条件を緩めず、承認どおり追加起動を停止した。通常設定・保護範囲の変更やシェルによる代替編集は行っていない。次の候補はモデルを起動しない範囲で判定条件を調べること、または比較準備の保留であり、いずれも今回は実行していない。

ADR-0173の整合確認: 仕様00〜03・実行計画・承認提示と実パケットを照合。仕様02へ限定変更を反映し、1親・1子、固定モデルと版、比較8実行・各30分・計240分の未使用条件は一致。成功なら結果保存後に次の起動判断、失敗・不明なら保存して追加停止、という経路は承認・仕様・管理処理で一致。承認取得→限定パケット保存→起動前照合→1回起動→終了・実体・利用量回収→記録の順序を確認した。ContextのdontAsk失敗は変更案の動機であり成功の証明ではなく、結果の失敗とも矛盾しない。保留案の費用評価と限定案の事実確認という評価も対応する。タイトルと決定は編集モードの限定採用という1件に対応し、指摘なし。

## sensitive file判定の非モデル調査（2026-09-10）

ユーザーの「OKです。進めましょう。」を受け、モデル起動なしの原因調査を実施した。Claude・モデル裁量の成功記録と、既存手順の失敗記録・起動設定・実体を比較した。

| 調査 | 確認できたこと・限界 |
|---|---|
| 成功例のWrite | モデル裁量条件も同名parent-ok.txt・child-ok.txtへ内蔵Writeを使っていた。ファイル名・拡張子だけでは条件差を説明できない |
| 失敗条件の設定 | 親と子はacceptEdits、Writeを許可。試験用settingsの追加内容はメモリ無効化とCLAUDE.md除外。試験フォルダに.claude/settings.jsonは存在しない |
| 同じOS保護での読み取り | launch.jsonの各Claude条件から外側sandbox引数だけを再利用し、Claude本体の代わりに固定PowerShellを実行。両条件でcwdは指定先、CLAUDE_CONFIG_DIRは未設定。作業フォルダ・公開マーカーにリンク属性なし。.git・.claude・.claude/skillsは不存在。読み取り結果だけを新しいWrite成功の証拠にはしない |
| 公式の保護対象 | [権限モード資料](https://code.claude.com/docs/en/permission-modes#protected-paths)では保護対象への編集はacceptEditsでも確認が必要で、allowルールもこの判定を上書きしない。公開一覧には今回のtxtファイルや試験フォルダ名は含まれない。CLAUDE.mdが隣にあるだけで全ファイルを保護するという説明も確認できなかった |
| 同版配布物 | [公式npmメタデータ](https://registry.npmjs.org/@anthropic-ai%2fclaude-code/2.1.267)と配布アーカイブを確認。版2.1.267、7ファイル、SHA1は11710a497103b7cd03f8035c587875bd91b7925dで一致。内容は起動ラッパー・インストーラー・型定義・資料・実行ファイルで、権限判定ソースは含まれない。インストールスクリプトは未実行 |
| 導入済み実行ファイル | C:/Users/d12an/.local/bin/claude.exe内に同じsensitive fileエラー文字列が存在。周辺は文字列領域で、判定関数の制御フローは復元できず、原因の確定材料にはしない |

原因は未特定。観測済みの直接理由はClaudeのsensitive file判定であり、プラグインの有効化・指示読み込み・その他の内部状態のどれが条件差を生むかは切り分けていない。公開一覧にないことを誤判定の確証にせず、単純なallow追加で解消するとも主張しない。通常設定・OS保護・モデル・版は変更していない。公式GitHubの類似報告も検索したが、別のパス・版の報告を今回の原因の証拠には使っていない。

調査中の失敗: 最初の固定PowerShellプローブはConstrainedLanguageのメソッド呼び出し制限で失敗し、標準コマンドだけへ修正して両条件の読み取りが成功した。実行ファイル全体への正規表現検索は時間を要したため当該シェルだけを中断し、解析成功と数えない。モデル起動数は11回、本比較0回のまま。調査用の公式配布アーカイブは `.tmp/claude-sensitive-static/claude-code-2.1.267.tgz` に保存。

次に原因を確定するには、Claude自身の判定理由を含む診断ログ、または提供元による同版の判定条件確認が必要。追加モデル試行を行う場合も、単純なacceptEditsの繰り返しではなく、判定を区別できる診断項目と送信・起動範囲を具体化してから個別に判断する。診断起動・提供元への送信は未実施・未承認。

## 同じ作業場所でのプラグイン有無診断案（2026-09-10、起動未承認）

ユーザーが成功例を対照にした検証準備の続行を依頼したため、既存3試行を照合した。dontAskでもプラグインなしは成功、ありは失敗、acceptEditsへの変更後もありは失敗。ただし作業フォルダ・指示なども違う。次の案はその違いを減らすための権限診断であり、本比較の実行条件を置換しない。

起動候補は `.tmp/model-discretion-execution/plugin-diagnostic/launch-candidate.json`、共通の依頼本文は同ディレクトリのprompt.txt。候補には承認フラグfalseで親CLI2回だけを保存した。同じモデルOpus 5/high、acceptEdits、同じcwd・設定・OS保護・Read/Writeだけのツール・同じ入力を使い、プラグインなし→ありの順で実施する。比較差は2本のplugin-dir指定と診断ログ保存先だけであることを機械照合済み。Skill・子・シェルは診断担当へ公開しない。

作業場所は既存の `preflight-claude-guideline-minimal`。管理側で同所にpermission-diagnostic.txtを新規作成し、内容をPUBLIC_PERMISSION_DIAGNOSTIC_20260910（改行なし）にする。既存なら停止し上書きしない。各回はRead1回、同じ内容のWrite1回だけを指示する。両回の開始時に同じ実体・ハッシュであることを管理側で確認し、初回が内容を変えた場合は復元して続行せず停止する。対象は既存ファイルへの同内容書き戻しなので、前回の新規作成拒否の再現そのものではない。

詳細ログは同所のruntime-temp/diagnostic-plugin-off.debug.txtとdiagnostic-plugin-on.debug.txt。ローカルに保全し、ログを外部へ送信しない。CLIの実ヘルプで--tools・--plugin-dir・--debug-fileの存在を確認した。モデル送信は試験の公開指示・公開固定手順・無害な文字列と操作結果に限定し、既存のコード・採点データへの拒否を維持する。通常設定変更・別モデル・追加購入・利用枠リセットは含めない。

実行は既存run-model-preflight.ps1のPacketPathとRunSuffixに候補を渡し、UseExactPromptを指定する。これは保存済み共通依頼をそのまま渡すためで、親子確認用の依頼を混入させない。起動前に承認済みパケットへ保存してから実行する。記録はcontrol/launch-packet/diagnostic-plugin-off-normal/とdiagnostic-plugin-on-normal/へ新規保存し、既存なら停止。既存管理処理の起動前後の固定プラグイン照合・既存物と保護マーカーの比較・Processハンドルによる停止を使う。

結果はWrite実応答、対象ハッシュ、詳細ログ、initのモデル・ツール・プラグイン数0/2で確認する。両方で2操作が実行された場合にのみ結果を比較する。なし成功・あり失敗ならプラグイン読み込みに関連する条件へ絞れるが、スキル本文や内部機構までは断定しない。両方失敗なら有効化が必要条件ではない。両方成功ならこの限定診断で拒否を再現できず、新規作成・Skill呼び出し・ツール構成などとの差が残る。なし失敗・あり成功も観測のまま報告する。

編集拒否自体は診断結果として2回目へ進める。保護対象や既存内容の変更、モデル不一致、指定外の操作、Read/Write未実行、起動失敗なら後続を止める。2回終了後の追加起動は行わない。子なしの概算2〜5分程度、契約枠の消費は事前不明で、公式利用量を回収する。利用者に求める操作は限定2回の起動採否のみ。送信済み入力と消費済み利用枠は取り消せない。診断ファイル・設定・ログは保存し、自動削除しない。

2026-09-10、ユーザーが「一旦ここで区切りたいです。」と指示したため中断。診断2回の採否は未回答で、起動・対象ファイル作成は未実施。候補・依頼文・管理スクリプトは上記.tmp配下へ保存済み。再開依頼後に起動の採否から続ける。
