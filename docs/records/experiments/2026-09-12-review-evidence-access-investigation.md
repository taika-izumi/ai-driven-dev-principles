# Issue-0144: 根拠を自力で探す最小経路の調査

2026-09-12。前回の続きとして読み取り・検索経路を調べるというユーザー指示に基づく。構成の採用、モデル試験、規範改定は未実施。調査対象HEADは `803e5d3834cce6539c9f682d7decead78551c1a8`。開始時の追跡済み変更は0件。

## 確認結果

- ローカルの `C:/Users/d12an/.local/bin/claude.exe --version` は2.1.268。`--help`で `--tools`、`--restricted`、`--safe-mode`、`--strict-mcp-config`、`--no-session-persistence` を確認した。ヘルプの確認であり、モデル起動時の実効ツール一覧は未取得。
- `--tools` は公開する内蔵ツールを指定する。`--allowedTools`による事前許可だけで、他のツールが存在しなくなるとは扱わない。Read・Glob・Grepの3操作は、文書の取得・ファイル名検索・内容検索に対応する。
- `--restricted` はファイル操作を作業ディレクトリと追加指定先へ制限すると説明するが、単独では編集ツールの全排除を意味しない。`--safe-mode` はカスタマイズを無効化するが、内蔵ツールの制限とは別。実際の組合せによる成立は未検証。
- 既存実測は2.1.263の子にReadだけを公開した条件。書き込み系ツールの拒否と読み取りは確認済みだが、Glob/Grepによる探索は未検証。9月9日の振り返りにも、検索不能のため主担当がパスを全列挙したとある。
- 現在の `skills/pre-finalization-review/SKILL.md` は `references/examples-and-evidence.md` を案内し、同ファイルの見出しにはADR-0117がある。`docs/records/decisions/README.md`にも0117へのリンクがある。主担当のrg検索で到達経路の実在を確認した。独立担当が発見した証拠ではない。

## 候補の比較（未採用）

| 候補 | 読める根拠 | 準備と限界 |
|---|---|---|
| ClaudeのRead・Glob・Grepだけを公開 | 指定したファイル範囲を担当が必要に応じて検索・取得 | 既存CLIで指定でき、追加サービス不要。Git履歴を直接検索できない。対象外の参照や実行時保存は別途確認が必要 |
| 同じ3操作で固定した文書コピーを読む | コピーに含めた現在版・未コミット差分・文書化した履歴 | 対象版と送信可能範囲を固定しやすい。コピー漏れと鮮度確認の負担がある。コピー自体は書き込み防止機構ではない |
| 要求されたパス・版の原文を機械的に返す限定経路 | 許可範囲のファイルと履歴 | 主担当の要約を介さず返せるが、パス・版の検査と接続の保守が増える。既存接続は固定Pester検査用で、履歴取得用にそのまま使えない |

最小候補は3つのファイル操作である。ただし「現行文書の独立探索」と「任意のGit履歴への独立アクセス」は同じ達成範囲ではない。履歴が必要なレビューを前者だけで完了扱いにできない。履歴全文を常に事前出力する案も、資料量と準備費用を増やす。

## 実証へ進む際に具体化すること

初期入力に探索対象の根拠名や答えを含めず、目的・対象版・レビュー対象・入口だけを渡す。入力から外した根拠を検索で発見し、その内容から対象の不整合を指摘できたかを、ツール履歴と引用で判定する。本記録やIssue-0144自体を試験入力に含めるとADR-0117という答えを漏らすため、探索能力の証拠を弱める。

読めたこと、書き込み操作が利用不能だったこと、対象外を読めなかったことは別々に確認する。許可された読解の成功を伴わない一律失敗は合格にしない。読み取り範囲内のリンクの解決先、無視ファイル・隠しファイルの探索、出力省略も確認対象となる。

モデル・実行回数・送信可能なファイル集合・実行時の保存先と通常設定への影響を明示してから試験する。公開済みの追跡ファイルと未追跡の会話・試験証跡を混同しない。今回の調査ではモデル起動や追加送信を行っていない。

Issue-0133には全文読解による既存判断への係留の事例がある。探索可能性を増やしても、その判断の妥当性を問えるとは限らない。Issue-0143では、現在防ぎたい欠陥から問いを組み立て、ADRを変更不能な正解として渡さないことも検討対象になる。

## 根拠と確度

- ローカルCLIの版・ヘルプ（今回の実測）。実モデルの操作成功を保証しない。
- [Claude公式CLI資料](https://code.claude.com/docs/en/cli-reference)、[公式ツール資料](https://code.claude.com/docs/en/tools-reference)（2026-09-12閲覧）。提供元による仕様説明。Read/Glob/Grepの役割とツール制限を確認し、実測とは区別した。
- `skills/subagent-dispatch/references/inspection-isolation.md`、`docs/records/retrospectives/system/2026-09-09-claude-native-subagent-interactive.md`（過去版・限定構成の記録）。
- Issue-0144、Issue-0143、ADR-0187、`docs/records/reviews/2026-09-11-premise-intent-audit.md`、Issue-0133本体（課題の背景と既存知見）。

結論: 最小候補の指定方法と既存文書への導線は確認できた。独立担当による検索・発見・指摘、現行版の書き込み拒否、履歴アクセスは未実証であり、Issue-0144は未解決。
