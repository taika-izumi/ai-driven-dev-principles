# Issue-0144: 読み取り・検索環境の試験準備と起動前停止

2026-09-12。ユーザーは、能力比較ではなく環境案の成立を確かめる試験であることを確認し、「OK。進めましょう」と指示した。直前に提示した条件はClaude Opus 5・mediumの新規1実行、最大10分、275ファイルの複製へのRead・Glob・Grep、取得資料のClaude送信と通常の認証・実行時保存である。

## 準備と検証

- 対象版: `48c9a1c0a2c2dcb3913c2dbada4743080970b30e`。GitHubへ反映済みという過去記録に基づく。「公開版」という従来の表現は一般公開リポジトリであることの確認ではない。今回、リポジトリの公開範囲は確認していない。
- 対象: `skills`、`docs/overview`、`docs/current`、`docs/records/decisions`の275ファイル、2,211,892バイト。Git blobから複製し、全件のSHA256一致を読み直して確認。リンク・特殊なファイルモードは不許可とした。元の未コミット・未追跡ファイル、会話ログ、他プロジェクトは複製していない。
- 実体: `.tmp/issue-0144-evidence-access/`。`prepare.py`は複製・依頼文生成、`run.py`は起動・イベント記録・停止・前後ハッシュ比較。試験用資材で、配布対象ではない。
- `snapshot/`に資料と書き込み試験用`probe.txt`を配置。外側の`outside-probe.txt`は範囲外の読み取り拒否を調べる無害な代用品。`manifest.json`に元版・パス・サイズ・ハッシュを保存した。
- 起動案: safe-mode、restricted、strict-mcp-config、Read/Glob/GrepだけのtoolsとallowedTools、dontAsk、permission-prompts none、フック無効、no-session-persistence。通常認証を使うため通常領域の実行時保存が皆無とはしない。内蔵操作の制限とCLI内部保存は別である。
- 開始イベントに3ツール以外やMCP接続が現れたら停止。制限を解除する再試行は禁止。10分でこの実行プロセスを停止する。副エージェント・任意コード実行ツールは公開しない。
- 依頼文は`review-criteria.md`を検査対象とし、根拠名・正解を教えず入口から探索させる。対象ファイルは全文検査するが、根拠候補275ファイルの全件読破は課していない。資料が不足すれば未確認として報告させる。
- Writeを呼べない場合と、実際に呼んで拒否された場合は区別する。担当が「変更しなかった」だけでは拒否実証としない。読み取り成功、範囲外の拒否、根拠発見、指摘はそれぞれ別判定とする。
- runnerのPython構文確認と設定・依頼文の読み直しを実施した。これらは準備の静的確認であり、モデルからの操作の成功を意味しない。

## 起動要求と停止

`python .tmp/issue-0144-evidence-access/run.py`を通常認証・モデル通信のため権限昇格付きで要求したところ、自動承認レビューがプロセス作成前に拒否した。

拒否理由: privateリポジトリ由来の仕様・ADR等275ファイル（約2.2MB）のClaude送信が内部情報の外部送信に該当し、その私的データの送信への明示承認が不足しているとの判断。既存の進行了承を示したが拒否された。別経路での実行や送信範囲を隠した再試行は行っていない。

実体確認: `started.json`、`events.jsonl`はいずれも存在しない。実モデル起動0回、結果なし。3つの達成条件は未実証のまま。原本を変更できない構成が成立したとの結論は出せない。

## 次手

対象資料が内部情報である可能性を含め、275ファイルをClaudeの探索対象とし、必要な内容をAnthropicへ送信することへの明示確認を求める。承認後は準備済みの同じ1回を起動する。サンプルへの変更を選ぶ場合は別の試験条件であり、実プロジェクトでの成立を実証したとは扱わない。
