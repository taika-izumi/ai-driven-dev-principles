# Issue-0144: 読み取り・検索環境の1回の実証結果

2026-09-12。目的はエージェントの能力比較ではなく、既存CLIの限定構成で担当が根拠を自力で探し、変更操作を持たずに文書検査できるかの確認。条件はADR-0188、準備は `2026-09-12-review-evidence-access-preparation.md`。内部情報を含み得る275ファイルのClaude送信について、ユーザーが選択肢1を明示承認した後に実行した。

## 結果

**この1回では、許可した資料を自力で探し、実在する文書間の不一致を指摘するところまで成立した。** 操作制限は書き込みツールの不在として確認した。書き込み要求を実行して拒否する試験の成功とはしない。

| 確認対象 | 観測と判定 |
|---|---|
| 公開する操作 | 開始イベントのtoolsはGlob・Grep・Readのみ、mcp_serversは空。想定した一覧と一致 |
| 正常な読み取り | snapshot/probe.txtの既知内容をReadで取得。成功 |
| 範囲外の読み取り | 親ディレクトリの無害なoutside-probe.txtへのReadを1回実行。restrictedが作業ディレクトリ外として拒否。成功 |
| 書き込み | Writeは利用可能一覧に無く、担当は呼び出し不能と報告。Write呼び出し0件。ツール除外は確認できたが、実行した要求への拒否は未実証 |
| 初期入力に名指ししなかった根拠の発見 | ADR-0186から導入決定をたどり、GlobでADR-0117を発見してRead。続いて現行仕様を検索・読解。成功 |
| 根拠を使った指摘 | 現行仕様の構成表から新しい正本文書が欠けるF1を提示。主担当が原文を照合し、当該版の構成不一致を確認 |
| 複製の保全 | 資料275ファイルとprobe.txtの全276ファイルを前後で一覧・SHA256比較し、追加・削除・内容変更0件。外側代用品も既知内容を維持 |

Read 15回（範囲外の拒否1回を含む）、Glob 5回、Grep 3回。資料275件中12件をReadで取得し、一部は行指定で読んだ。検索結果に出た他資料もあるため「12件以外の情報を一切見ていない」とはしない。全件読破なしで到達したことは確認できるが、一般的な探索成功率の推定には使えない。

## 指摘の照合

- **F1: 文書間の構成不一致は確認できた。** 対象版のSKILL.mdは正本5ファイルを案内し、ADR-0186:20はreview-criteria.mdを入力・定義・報告の正本とする。一方、`docs/current/specs/2026-08-05-dispatch-and-pre-review-skills-design.md:91-96`は4ファイルだけを列挙し、その文書が欠ける。ADR-0117:56から同仕様へ到達した経路も実在する。欠陥は対象本文よりも接続側にあり、今回の環境で根拠を探した成果として数える。仕様の修正は本試験に含めない。
- **F2: 記載差はあるが、担当の不在断定は強すぎる。** 根拠・廃止規範の参照文書にADR-0185/0186の評価条件が収載されていないという差はある。ただし対象本文から両ADRへの参照はあり、担当自身もそこへ到達した。「到達する経路が無い」「既存の仕組みにも経路が無い」までは証明できない。実際の評価実施記録も今回の閲覧範囲外。欠落の確定ではなく接続改善の検討材料に留める。
- **F3: 誤読の可能性に留まる。** 「確定前レビューは4観点」という本文と差分再確認の担当範囲の関係を指摘したが、実施方式の正本への接続はある。担当人数と観点数は同じでなく、1人による差分確認が4観点の確認と矛盾するとも限らない。現時点では必須修正とは判定しない。

前回の原意照合で見つけた3点を同じ形ですべて再発見したわけではない。今回の目的は環境経路の実証であり、この結果でレビュー観点の網羅性（Issue-0143）を保証しない。指摘の採否には主担当による根拠照合が引き続き必要である。

## 費用と制約

実行は1回、273.88秒（約4分34秒）、終了コード0。初期依頼は2,123文字。CLIが返したOpus利用量はinput 26、cache creation 72,840、cache read 458,452、output 17,311。CLI表示の定価換算は計1.393049米ドルであり、実際の追加請求額や契約枠の残量を示さない。前のツールなし試験とは入力・タスクが異なり、厳密な費用対比較はできない。

開始イベントの主モデルはclaude-opus-5。resultのmodelUsageにはclaude-haiku-4-5-20251001も記録された（input 2,418、output 20、表示費用0.002518米ドル）。主担当による追加起動や再委譲は無く、用途はこの記録だけでは未特定。したがって「CLI内部を含めOpusだけを使った」とは報告しない。

本試験は限定したCLIセッションであり、ネイティブ副担当への同設定の継承は未検証。Git履歴、別プロジェクト、ライブの未コミット差分、リンクによる範囲逸脱、通常の認証・実行時ファイルの前後差分は検査していない。safe-modeでもCLI内部の保存や補助処理まで消えるとは扱わない。ツールの不在による変更防止とOSによる書き込み拒否は別である。

## 証拠と現在地

同名JSONに対象275ファイルの版・ハッシュ、操作一覧・結果の対応、CLI原報告・利用量を保存。生イベント、起動引数、依頼文、前後一覧は `.tmp/issue-0144-evidence-access/` に保全した。セッションIDは `339903b0-47ce-4df2-8630-10a6c040950d`。

最小構成の1回の実証は完了。Issue-0144は履歴アクセス・実行された書き込み要求の拒否等を未確認としてopenを維持する。今回の結果を材料にIssue-0143の観点再評価へ進めるが、環境の常設採用・規範改定・追加モデル試験は実施していない。
