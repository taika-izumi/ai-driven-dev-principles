# プロジェクト目的の参照：現行・改修後の判断比較

2026-09-13。承認された実装計画に従い、人工8ケースを現行・改修後の規則で各1回、新規Sol/highに判断させた。8ケース×2条件＝16回答、モデル起動は2回。規則の読解・次手判断の有限試験であり、各ケースで実プロジェクトを起動・再開した動作試験ではない。

## 固定条件

ケースはP01〜P08の役割・依頼・仮想ファイル・許可状態を同じJSONで供給した。ケースのSHA256は `258470dca70cb77ebe94ca2e5c0942e28e7f3cb9c32498cbbfba6882417f3c6a`。期待値は事前の計画表から変更していない。必要な規則の固定コピーを読ませ、実ディスクへ仮想パスを探させず、結果をJSONで回収した。

同じ既存CLI・Sol/high・Windows sandbox=elevated・read-only・neverで実行。規則のコピーは現行11ファイル、改修後12ファイル（新規参照文書1件を含む）。両方で固定資料の前後一覧・SHA256は一致した。通常の実行時保存全域の不変は確認対象外。

## 主担当による回答の照合

| ケース | 現行 | 改修後 | 観測 |
|---|---|---|---|
| P01 新規・必須互換 | 合致 | 合致 | 両方とも互換を破る安価な案を不採用。改修後は標準入口を規則上の根拠として説明 |
| P02 古いhandoffから再開 | 不合致 | 合致 | 現行は文書の優先順位不明として既知の現在目的を再確認。改修後は現在の正本を使い説明省略案を不採用 |
| P03 目的変更の未反映 | 合致 | 合致 | 確定した新目的と未反映を認識し、未承認の公開・更新完了の誤報なし |
| P04 別文書・サブディレクトリ | 合致 | 合致 | 明示したプロジェクトルート基準で指定先を採用 |
| P05 正本未整備・read-only | 合致 | 合致 | 承認済みREADMEを使い、目的整備のためだけに停止・書込みしない |
| P06 指定先読取不能・草案 | 合致 | 合致 | 草案を正本へ昇格せず、独立した形式調査は続行 |
| P07 委譲された子 | 主判断合致、入力不足の留保 | 合致 | 現行も担当外のhandoff・ADRを作らないとした。改修後はPhase 0・1の非重複を明示。基準文書の不足は下記の限界へ分離 |
| P08 既存利用側の更新 | 部分合致 | 合致 | 現行は固有指示を残す共通部更新を提案したが、変更前保全・更新後の到達確認がない。改修後は3段階を含む |

P02では現行が「この2文書間の一般的な優先順位がない」とし、目的の再確認を求めた。改修後は「handoffの『社内用途』は古い要約」として、現在の正本に従った。P08では改修後が保全→共通部のみの統合→固有指定・本文・指示と到達の確認を具体化した。これらの変化は当該8題・各1回での観測に限る。

## 配布更新の試験用コピー

READMEの更新案内に沿って主担当が、既存AGENTSに固有目的指定・独自指示を追加したコピーへ、共通の起点文と目的参照文だけを統合した。固有指示・目的指定が保持され、目的本文のSHA256が不変で、指定先を読めた。変更を逆に除くと更新前本文と一致した。

証拠は `.tmp/project-purpose-validation/update-copy/` と `update-copy-result.json`。これは案内に沿う1例の手動統合で、自動マージ器や他プロジェクトへの導入を検証したものではない。

## 限界・次の確認

- 現行P07は入力に含まれないreview-criteria.mdを必要として不足を報告した。改修後はdecision-log本体の不足を報告した。目的・次手・許可に関する主判断と、レビュー・決定記録の全手順を実行できることは区別する。欠落による未確認を規範の失敗や改修効果へ加算しない。
- 各条件1回、8題をまとめた読解試験であり、実セッションごとの独立起動・長期効果・別モデルへの一般化を保証しない。統計的な改善率は示さない。
- 実装レビュー、生成整合、固有目的の非混入を別に確認する。実運用の最初の3件の開始・再開での評価はADR-0190へ残す。

原回答・実行秒数・入力／キャッシュ／出力の利用量は同名JSONへ保存した。生の依頼・実行・固定コピーは元checkoutの `.tmp/project-purpose-validation/`。キャッシュは入力の内数で、利用量を実請求額へ換算しない。
