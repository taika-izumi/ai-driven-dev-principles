# Retrospective: 配布物からの出所識別子の除去

- **Subject**: 配布物生成体制の導入（配布先で解決できない参照の除去）
- **Branch**: feature/cross-repo-adr-reference（merge済み: 70365a5）
- **Period**: 2026-08-07 〜 2026-08-08
- **Plan**: docs/working/plans/2026-08-07-distributed-artifact-generation-plan.md
- **Spec**: docs/current/specs/2026-08-07-distributed-artifact-generation/（00-overview ＋ 01〜05）
- **Related ADRs**: ADR-0081, ADR-0082, ADR-0083, ADR-0084（ADR-0027 へ部分修正の注記）
- **Facilitator**: メインエージェント (claude-fable-5)

## 1. 達成サマリ

- **配布先で解決できない参照を配布物から除去する体制を新設**。ソースには保守者向けの注記を残し、除去した生成物を配布する。ソースの書き方を位置で規約化し、生成器が規約適合の検査を兼ねる（規約違反は生成の失敗として顕在化し、別途の lint を設けない）
- **共有ライブラリと生成器 2 本**: `scripts/lib/strip-provenance.ps1`（判定・変換・残存検査。自己テスト 21 ケース）、`scripts/build-dist.ps1`（新規。`skills/` → `dist/`）、`scripts/sync-template.ps1`（改修。判定・変換・`-Check` を組み込み、処理順を「読む→判定→削除→書き出す」へ）
- **移行**: 規約違反 36 件 → 0 件。あわせて規約適合だが除去後に文が壊れる 10 行も書き換え（計 14 ファイル）。両配布物（`dist/` 19 ファイル・`template/` 7 ファイル）とも出所識別子 0 件、実在の固有名・絶対パス・自己参照も 0 件
- **規約の文書化と配線**: `CONTRIBUTING.md` へ「全シナリオ共通: 配布対象ソースの記法規約」を新設し、配布対象ソースを編集する 6 シナリオへ確認項目を配線。`extend-guidelines` の手順 6 を配布物生成の案内へ拡張
- **配布元の切替を実機で確認**: `marketplace.json` の `source` を `./dist` へ変更し、`/plugin marketplace update` 後にスキルのベースディレクトリが `dist/skills/<name>` を指し本文から識別子が消えることを確認（コミット 43efad6）。過去に起きたインストール記録の消失は再発せず
- **Issue-0067 を close**。実装・移行・文書化・実機確認のすべてを完了（60 コミット・65 ファイル・約 5,300 行）

## 2. 課題（対象システム固有）

課題の抽出と分類まで（対策の設計・採否判断・ADR化は次サイクル）。このファイルには**対象システム固有**の課題のみを記載し、開発フロー/ガイドライン課題は `flow/<同名>.md` に記載してここにはポインタを残す。

本サイクルでは、対象システム固有の課題は**すべてサイクル中に随時起票済み**であり、振り返り時点の新規抽出はなかった。

- **サイクル中に起票（再掲）**: Issue-0070（機械判定がスクリプト内の散文に届かない）・Issue-0071（配布されるスキルが配布されない文書を必須参照にしている）・Issue-0072（括弧内判定が半角括弧を全角と同一視する）。3 件とも「ADR-0081 が立てた原理（配布先の読み手が解決できるか）と、ADR-0083 の機械判定の実装との隙」から生じている

> 開発フロー課題 4 件は `flow/2026-08-08-distributed-artifact-generation.md` 参照。worklog 送りとした delta 型候補 1 件（起票なし。振り分け規則による）。

## 3. 既存課題の再発・進展

- Issue-0067: 本サイクルで close。ADR-0081〜0084 と生成体制で対処し、実機での成立まで確認した
