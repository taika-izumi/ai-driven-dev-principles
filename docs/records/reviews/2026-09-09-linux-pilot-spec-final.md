# Linux試作詳細仕様の機械検証と確定

- 日付: 2026-09-09。
- 対象: `docs/current/specs/2026-09-09-isolated-verification/`の4ファイル、ADR-0151・0152。
- ユーザー判断: 「機械検証で締める。状態名・停止条件・参照の一致を確認し、問題なければ仕様を確定」への「１で」。提示した採否案をこの回答で確定する。
- 結果: 16項目すべて一致。追加の仕様修正なしで確定。変更したのは確定状態・記録・引き継ぎだけ。

## 機械検証

検証スクリプトは`.tmp/linux-spec-final/check.ps1`、結果と検証時の仕様SHA256は同ディレクトリの`result.json`。仕様書の実体を読み、ファイル数、3つの入出力契約、activationの保存先・参照、作業解放前の受付停止・対象停止・failure.stage、startedの意味、事後incomplete、状態優先順位、原本の再照合、固定設定パス、失敗保持、v1/v2成果物境界の分離を照合した。

`git diff --check`も合格。`scripts/verification/`の変更は無く、既存コードの再テストは行っていない。本検証は文書の一致を確認するもので、v2コード・Docker・ホストの権限制限を動作検証したものではない。

## 採否と反復の終了

第1回F1・F2のv1/v2誤認に基づく不具合判定は不採用、変換・回収責務の明記は反映。F3の確認記録と回収の接続、F4の失敗保持は採用。第2回R1は起動前停止と事後検出の区別を補完して採用、R2の原本照合削減とR3のパス項目追加は不採用で確定した。詳細は第1回・第2回のレビュー記録を参照。

レビュー履歴: フル実施（claude-sonnet-5・1回）＋差分再確認（claude-sonnet-5・1回）＋機械検証（1回・提示後確定（実質的な収束に至らず））。骨格安定・実質的収束は未認定のまま、ユーザーが選んだ方式でさらなる改訂が生じなかったため提示後確定とする。新たなモデルレビュー・外部送信は行っていない。モデルレビュー2回のCLI報告額の合計は約0.65米ドル。

## 決定と次工程

ADR-0151は先行順序、ADR-0152は専用接続とコンテナへの操作限定という別の問いを扱うことを確認し、実装前の設計承認としてAcceptedへ昇格した。全体のサイクル整合検査は実装完了時の後追い対象であり、今回済みとは記録しない。

次はv2実装計画の作成。最初に、ホスト側の全操作経路をどう列挙・制限・検証するかを確認する。公開プロトコル調査だけでは全実効ツール一覧の取得方法を確定できていない。取得・制限が成立しなければ自由な作業依頼を起動しない。

Docker execの実行証拠、非rootマウント、通信拒否、停止、両主担当からの往復は後続。イメージ準備・実コンテナ起動・モデル起動・新しい外部送信・導入の承認は、仕様確定へ含めない。
