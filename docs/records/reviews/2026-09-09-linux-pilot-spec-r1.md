# Linux試作詳細仕様の第1回レビューと対応案

- 状態: 後続の差分再確認・機械検証を経て採否と仕様を確定。最終状態は`2026-09-09-linux-pilot-spec-final.md`を参照。以下は第1回時点の対応案。
- 対象確定点: spec。成果物の型: 通常型。
- 方式: 新規1担当が敵対的・実装整合性・仕様適合・前提実在の4観点を兼務。静的確認のみ。
- レビュアー: Claude Code 2.1.265、claude-sonnet-5、effort=medium。
- 実測: 226,342ms、CLI報告額0.3383058米ドル（請求確定額ではない）。

## 対象・承認・実行条件

固定9ファイル約69KBをAnthropic Claudeへ送信する説明に対し、ユーザーが本会話で「1で」と承認した。仕様00〜03の4件、ADR-0151・0152、既存RequestCopy・Execution・Resultの3件をスクリプトで連結。親AIの本文再生成は行っていない。

一覧とハッシュは`.tmp/linux-spec-review/send-manifest.json`、本文は`send-payload.txt`、起動記録は`approved-process.json`、生の出力は`approved-response.jsonl`、抽出結果は`findings.json`（同ディレクトリ）。initでtoolsとmcp_serversは空、tool_useは0件。終了後の修正前ハッシュは9件とも一致。返却JSONの対象一覧も9件。冒頭の散文には「仕様3件」とあるが、一覧は正しく4件であり件数の誤記として扱った。

改訂前退避は`C:/Users/d12an/.ai-dev-review-snapshots/MakeAiInstructions/linux-spec-20260909-4dfb9b1c86b041f8a88857b453c6824d/`。仕様ディレクトリ・ADR2件・送信資料を保存した。コードやDockerコンテナは今回のレビューでは変更・起動していない。

## 指摘の照合と対応案

| 指摘・提示重大度 | 守るものと実体の確認 | 採否案・修正・費用 |
|---|---|---|
| F1: v1成果物の境界がv2仕様と違う（高） | v2はworkRoot内の成果物だけを受理する必要がある。既存ResultはschemaVersion=1、既存Get-VerificationArtifactはrunRoot相対であり、仕様はv2未実装と明記済み。これはv2実装済みコードの欠陥ではない | 不具合としての判定は不採用。v1ヘルパーを直接変更する提案も採らない。v2専用Get-LinuxWorkArtifactと版ごとの回収分岐の所有者を03へ明記。既存v1試験を壊す変更を避け、必要なv2実装の責務だけ具体化 |
| F2: Requestだけv2化・Settingsはv1の不整合（中） | request.schema.jsonはconst=1で、RequestCopyは同スキーマを検査している。共通キーが同じでもv2化済みではない。一方、変換の所有者を明示する余地はある | 部分v2化という根拠は不採用。New-VerificationRun内の版振り分け、v2入力の検査、共通6キーと旧Settings5キーへの明示投影、再検証入力の追加をRequestCopyの責務として01へ記載。追加の変換サービスは作らない |
| F3: 起動可否確認の保存先・回収との接続がない（中） | 02は確認記録を要求しているが保存先未定義、03に照合が無い。版や設定の違う確認を現在の実行へ誤適用し得る | 採用案。既存effectiveConfigPathをactivation.jsonへ対応付け、設定ハッシュ・実効ツール・拒否試験の証拠を保存し、03の回収条件1で検査する。既存の確認と記録を結び、別の常設台帳や承認工程は増設しない |
| F4: 途中失敗runの保持方針がない（軽微） | RequestCopyは失敗時runRootを例外へ残し、過去の仕様は自動削除しない扱いだった。新01では保持と返却が読み取りにくい | 採用案。診断用保持・自動削除なし・runRoot未作成時nullを01へ記載。03に準備失敗のJSON生成責務を明記。削除・一覧化の新機能を追加しない |

修正の分類: F1・F2は精度型（既存要求の実装責務の明記）、F3は既存確認と回収の接続補完、F4は保持方針の復元と失敗応答の具体化。いずれも判断を入れた修正であり、単なる文字置換ではない。既存コードは変更していない。主要構成・検証担当・原本保護・Linux先行は維持し、詳細仕様までの補助設計という分担の範囲。

## 追加の前提確認

標準.NETのNamedPipeClientStreamでローカルDocker EngineへGET /versionだけを送信し、HTTP 200、ApiVersion=1.55、MinAPIVersion=1.40、Os=linuxを確認。追加ライブラリ不要でローカル接続できた。`.tmp/linux-spec-review/api-read-probe.ps1`と`api-read-probe.json`が証拠。exec作成・ストリーム解析・コンテナ制限・モデルの全ツール制限の成功には一般化しない。

レビューの未検証欄に挙がった既存schema・README等はモデルへの固定資料に含めていなかった。主担当がrequest/resultのv1スキーマを読み、F1/F2の根拠を訂正した。未実装ContainerRuntime・VerificationMcpの動的実証、実際のMCPイベント、非rootのバインドマウントは今後の実装前確認として維持する。

## 反復方式の推奨

差分再確認を推奨する。新規1担当へ対応表・改訂差分・影響する01〜03と上流00を渡し、保存した確認が回収へ届くこと、v1を壊さずv2へ接続することを確認する。前回は永続セッションなしの固定入力実行なので継続セッションを前提にしない。全体の再走査より今回の補完へ確認を絞る。

最も強い反対材料は、ホスト操作制限・Docker実行・MCPの具体的イベントが未実証であり、静的レビューを重ねても成立は証明できない点。起動可否の機械検証は実装計画の最初へ置く。今回の差分の参照・項目一致は自己確認するが、F3の信頼境界の接続は独立確認の便益がある。骨格安定は、未実証の実行経路と今回の接続補完があるため現段階では判定しない。

追加レビューの費用は未実測。今回の1担当4観点は約0.34米ドルで、差分再確認は1担当1回分を見込む。外部送信は次の対象資料を固定して別途承認を得る。フル実施・機械検証・このまま確定も選択肢として提示する。

自己確認では、改訂対象が01〜03の3ファイルに限られること、版の振り分け・成果物専用処理・activationの保存先と回収参照・失敗時の保持方針が一致することを確認した。4ファイル構成とgit diff --checkも合格。これは仕様の自己確認であり、v2コードや実行制限の動的検証ではない。

後続: ユーザーの「１で」により差分再確認を実施し、F1〜F4の反映一致を確認した。追加指摘と採否案は2026-09-09-linux-pilot-spec-r2.mdへ記録。第1回の指摘を未確認として最初からやり直さない。
