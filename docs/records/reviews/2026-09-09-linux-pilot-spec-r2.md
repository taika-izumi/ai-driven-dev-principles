# Linux試作詳細仕様の差分再確認と対応案

- 状態: ユーザーが機械検証を選び、16項目の一致により採否と仕様を確定。最終状態は`2026-09-09-linux-pilot-spec-final.md`を参照。以下は第2回時点の対応案。
- 対象確定点: spec、通常型。新規1担当による差分再確認。
- 実行: Claude Code 2.1.265、claude-sonnet-5、medium、ツールなし。
- 承認: 修正版11ファイルと差分をAnthropic Claudeへ送信する選択肢に、ユーザーが本会話で「１で」と回答。
- 実測: 161,507ms、CLI報告額0.3128578米ドル（請求確定額ではない）。初回との合計報告額0.6511636米ドル。

## 実行条件と対象版

`.tmp/linux-spec-review-r2/send-manifest.json`の11ファイルと`send-payload.txt`（93,104バイト）を送信。本文連結はスクリプトで実施。initでtools=[]・mcp_servers=[]を確認し、tool_useは0件。レビュー終了後、修正前の11ファイルはハッシュ一致。reviewedFilesには11ファイルに加えてdiffの説明行が1件あるため、ファイル数は12と数えない。

生ログは同ディレクトリの`approved-response.jsonl`、抽出結果は`findings.json`。改訂前の退避は`C:/Users/d12an/.ai-dev-review-snapshots/MakeAiInstructions/linux-spec-r2-20260909-1b6f3e87dc574640875cb4f5246c7ebf/`。仕様4件・ADR2件・第1回レビュー記録・送信記録を保存してから修正した。

前回F1〜F4の対応はすべて反映一致と評価された。F3は今回R1として記述の区別を追加確認。新たな主要機能・役割・隔離方式の変更を求める指摘はなかった。

## 追加指摘の照合・採否案

| 指摘 | 守るもの・確認した実体 | 採否案と変更 |
|---|---|---|
| R1: activation不成立時の実行側停止と回収側incompleteが曖昧（中） | 02は作業解放前にverifiedでなければblockedと規定し、03は事後の記録欠落をincompleteとしていた。ただし03の優先順位がblockedを未起動だけに限定しており、初期化済みプロセスがある場合の表現が不足 | 採用案。既存failureのstage=activationで作業解放前の不成立を表し、自由な依頼・run_command受付を開始せず、作成済みの対象を停止する。startedは実際のプロセス開始状態を保持。事後に初めて記録不備を検出した場合はincomplete。コンテナを絶対に作成しないというレビュアー案は、実効マウント確認等の初期化を妨げるため採らない |
| R2: v1照合後は再検証ファイルだけ再照合する案（低） | 01はv1内部処理の完了後に再検証入力を作り、最後に原本も照合すると記載済み。再検証ファイルを準備する間に原本が更新される経路がある。既存比較が見た時間帯とv2最後の比較が見た時間帯は違う | 不採用案。原本の再照合を減らすと更新を見逃すため、現在の順序を維持。新しい分岐や照合省略条件は追加しない。順序を実装計画とテストに引き継ぐ |
| R3: effective-settings.jsonのパス項目を追加する案（低） | 02はeffectiveSettingsHashの対象をcontrol/execution/effective-settings.jsonと固定している。03は同じcontrol内の実効設定を照合する。子が任意の保存先を選ぶ要求は無い | 不採用案。固定パスを可変のデータ項目へ増やす必要はない。今の固定配置を維持し、パス解決のテストを実装時に行う |

R1は既存の停止条件と事後検出の違いを説明する精度型の補完で、判断を入れた修正。R2・R3は不採用案として実装時の順序・パス検査へ引き継ぐ。変更は02・03のみ。既存コード・主要構成・データ項目の一覧は変更していない。

## 増設と縮小の確認

前回は確認記録の保存先と回収への接続を具体化した。今回は新たな確認工程・常設台帳・パス項目を増設せず、既存failureで結果を区別する。R2の原本照合削減は保護を失い、R3の項目追加は現要求に不要。今回の修正は説明と状態判定の明確化に留める。

## 起動前確認に関する読み取り調査

モデルを起動せず、Codex CLI 0.153.4の`app-server generate-json-schema --experimental`でプロトコル定義を`.tmp/codex-protocol-linux-pilot/`へ出力した。ThreadStartResponse・TurnStartParams・McpServerStatusUpdatedNotificationの調査範囲では、全内蔵ツール一覧を返す項目を確認できなかった。dynamicToolsは追加するツールの定義であり、全実効ツールの一覧とは扱えない。CLI全体に確認手段が存在しないと断定せず、最初の実装前調査として引き継ぐ。

## 推奨する締め方

機械検証を推奨する。R1に関係する実行側の停止、failure.stage、回収側のblocked/incomplete、優先順位が一致することを確認する。差分再確認で前回対応が一致し、残る変更はこの局所的な説明補完であるため、もう1回のモデルレビューの便益は小さいと判断する。

反対材料は、状態の区別にも判断が入っており、単なる表記修正ではない点。ただし、実際のホスト操作制限・MCPイベント・非rootマウントは動的実証が必要で、静的レビューの追加では証明できない。そこは実装前確認へ渡す。骨格安定や実質的な収束の成立をこの差分再確認だけで認定しない。

フル実施・差分再確認・機械検証・このまま確定を並記し、採否と締め方はユーザーの回答で確定する。追加モデルレビューを選ぶ場合は、新しい対象資料への送信承認と利用料が必要。機械検証には外部送信はない。
