# 提案・再実行仕様の第1回独立レビューと指摘対応

## 実施範囲

ユーザーが新規1担当4観点のレビューへ「１で」と回答。gpt-5.6-sol（作成側と異なるモデル）の新規担当が、敵対的・実装整合性・仕様適合・前提実在を確認した。対象5仕様・332行、ADR-0157/0158、Issue-0136設計メモ、既存実装8件、sbx試験記録の該当節を読み取り。ユーザーへ提示した読み取り限定の範囲を維持し、コマンドの写経実行・VM・モデル起動は行っていない。静的フル1回であり、動的な保護検証ではない。

最終報告の取りまとめが長引いたため、主担当が一度中断して確定済み指摘の要約を受領し、既検査の機構一覧を補足受領した。再走査・第2回レビュー・改訂後の再レビューは行っていない。全数確認した新設・変更機構は14分類。指摘は高5件・中1件。

開始時の対象hashは`.tmp/sbx-socket-20260909-02/spec-v3-review-before.json`。主担当が受領時に5仕様＋ADR2件の変更0件を確認。改訂前5仕様は`C:/Users/d12an/.ai-dev-review-snapshots/MakeAiInstructions/20260909-v3-spec-r1/`に保存し、保存hashも一致確認した。以下の行番号はこの改訂前仕様に対応する。

## 指摘と主担当の照合・修正

全6件を補助設計の委任範囲（ADR-0158）として反映した。採否の確定と次のレビュー方式はユーザーへの反復提示で判断する。基本方針・ホスト保護・担当構成・新たな必須基盤は変更しない。

| ID | 重要度・前提 | 実体・到達経路 | 対応 |
|---|---|---|---|
| F1 | 高。再実行が共通runtimeだけを使い同じ実行条件を保つ | 02:7には搬入・cwd・環境の引数なし。04:5,19,23,31はそれらを必須とし、公開契約だけでは接続不能 | 搬入・実体照合APIとWorkingDirectory/Environment/RunBudgetを追加し、04の呼出しに対応付け |
| F2 | 高。stdin入力と出力容量を外側で制限する | 02:5,35,43の再利用先はExecution.psm1:79-86で無制限CopyToAsync、ProcessHost.ps1:8-20に対象stdin転送なし。主担当も実コードを再読 | v1を維持するInvoke-VerificationProcessV3拡張を必要実装として明記。対象stdinと制御JSONを分離し、合算出力上限・停止を定義 |
| F3 | 高。基準版検知と全体上限に権威値が要る | 01:17,57と03:49の要求に対し01:21のPreparedRunに期待hash・全体起点なし。後段がファイルから再計算すると改変を基準化する | PreparedRunへmanifest期待hash、startedAt/deadlineAt、recheckArtifactsを追加。外側の単調時計と全体起点維持も明記 |
| F4 | 中。既存の固定テスト再実行に余分なモデル送信を要しない | 00:16,41-44、04:9,39はready提案必須。02:63はrecheckの新提案を拒否。固定テストしか使わないのにモデル再起動が必要になる | recheckのテストを準備側で固定、提案関数は起動せずorigin=reused-testsを生成。model/profileをnull可とし、未作成と停止成功を区別 |
| F5 | 高。以前の能力証拠だけで別VMへ資料を解放しない | 02:17,27,29のcheckedAt/profileだけでは現run/VM idとの関係なし。04:27-31の実行記録へ古いpolicy/mount証拠を流用可能 | 能力試験と実VMのactivationRecordを分離。作成後搬入前・コマンド前の実体/起動世代を照合し、各記録へ参照を固定 |
| F6 | 高。提案失敗も正規の最終結果として返す | 02:59の非readyは04:9で拒否、一方03:5,7は3入力または準備失敗しか扱わない。提案失敗後に正規結果へ進めない | 型付きnot_run結果とNew-VerificationReplayNotRunを追加。途中例外・未作成・作成不明・停止未確認と最終statusへの写像を定義 |

F1/F2/F3/F5/F6は実装時にも必要な既存契約の補完で、単なる注意文ではデータや制御を渡せない。F4は不要な実行を減らす変更。独立した常駐サービスや汎用の監査機構は増設せず、既存4責務の内部で保持する。保守負担は引数・結果schema・否定fixtureの追加であり、保護を省略して負担を減らす案は採らない。修正は文言置換だけでなく契約を補う判断を含む。

## 前提実在の14分類

必要: v3入力検査、原本manifestと途中変更検知、独立baseline/Git、proposal-input分離、前回テストhash固定、runtime能力証拠、同一ID停止、Envelope容量/パス/型検査、CreateNewのaccepted固定、別VM通信なし同一テスト再実行。

欠落: 実VMへ束縛した実効状態証明、搬入/cwd/環境の共通API。manifest期待hash・全体deadline、途中失敗接続も関連機構内の欠落として指摘。

不要: recheck時の再提案。前提誤り: 既存Execution/ProcessHostに対象stdin・容量上限の能力がある扱い。重複判定はなし。

## 修正後の確認と残る判断

主担当の機械照合12項目は成功（`.tmp/sbx-socket-20260909-02/spec-v3-r1-fixcheck.json`）。改訂5仕様の差分を確認し、git diff --checkも成功。これは対応する契約が文書に存在することの確認で、改訂後の独立確認や実装テストではない。

残る動的未実証: pids強制、追加ホスト経路拒否、モデル認証・許可先、通信拒否、強制停止・出力洪水・敵対的回収物、両主担当の実往復。前提が不成立ならblockedとする方針は維持。

次は改訂箇所の差分再確認を推奨。影響がAPI・失敗経路・recheckへ及ぶため、機械的な文字列照合だけでは十分でない。一方、基本の4責務と信頼境界は維持しており、同じ全資料を最初からフル確認するより差分に集中する便益がある。対象は改訂5仕様と保存した改訂前版・本対応表。方式はユーザー判断待ちで、追加送信は未実施。初回の4観点兼務という人数は今回の規模で判断したもので、費用や所要時間の一般的な優位性を今回だけで主張しない。
