# 提案・再実行仕様の第2回差分再確認

2026-09-09。ユーザーが新規1担当の差分再確認へ「1で」と回答。gpt-5.6-solの新規担当が5仕様の改訂前後、r1対応表、ADR-0157/0158、必要な既存実装を読み、4観点で修正の整合と修正由来の欠陥を確認した。読み取り限定、コード・VM・モデル起動なし。静的フル1回＋差分再確認1回、通算2回。

## 結果

F1（runtime API）、F2（stdin/出力制限の拡張）、F3（hash/deadline）、F4（recheckのモデル省略）、F5（実VM固有activation）は解消と判定。F6（失敗結果接続）は部分解消で、関連する高1件N1が残った。前提実在を含め14分類を確認。基本方針・4責務・保護条件の変更はない。

N1の前提は、VM作成とID確定後にactivation検査が失敗しても、対象VMを追跡して停止する必要があること。改訂前02:7には成功時のSandboxHandleしか公開契約がなく、02:23,37は作成後失敗を許容する。03:9と04:43は作成済みVMを型付き結果へ保持すると要求するが、IDを受け取る失敗契約がない。主担当も該当箇所を再読し、前提と具体的な到達経路を確認した。

## 修正内容

N1をADR-0158の補助設計範囲で反映。新しい常駐監視や別サービスは作らず、既存の標準例外Data['runtimeFailure']に、creationState・部分handle・stopState・失敗理由を格納する契約を02へ追加した。ID確定後はactivation検査より先に外側へ保持する。失敗時も確定済みIDで停止を試み、03/04へ渡す。

部分handleは停止専用。activationRecordがnullでも未作成と扱わない。要求送信後の成否不明はunknown、未作成とは別にしてincompleteとする。停止できた起動失敗も成功にしない。

| 到達経路 | 引き継ぐ情報 | 最終扱い |
|---|---|---|
| 作成要求前に拒否 | not-created、handle=null | blocked |
| 作成要求送信後に成否不明 | unknown、unverified、creation-unresolved | incomplete（時間超過ならtimed_out） |
| ID確定後activation失敗、停止成功 | created、部分handle、stopped | blocked |
| ID確定後activation失敗、停止未確認 | created、部分handle、unverified | incomplete |
| ID確定後に時間超過 | IDと停止実施結果 | timed_out、停止未確認も併記 |
| activation成功 | 完全なhandle | 通常の搬入・実行へ |

2回続けて契約を補完しているため増設の必要性を点検した。今回の情報は既に要求する停止のために必須で、注意書きだけでは呼出し元がIDを取得できない。別の作成結果APIを増やす案と比べ、既存例外経路へのデータ追加を選んだ。維持負担は例外書式と各呼出し元の照合、作成後失敗fixtureであり、4責務の境界や保護を増減しない。新設機構の拡張を続ける判断ではない。

## 保全と確認

レビュー開始hashは`.tmp/sbx-socket-20260909-02/spec-v3-r2-before.json`。受領時の対象変更0件。改訂前5仕様は`C:/Users/d12an/.ai-dev-review-snapshots/MakeAiInstructions/20260909-v3-spec-r2/`へ複製し、hash一致を確認した。

変更は02/03/04の3仕様。主担当の文書照合6項目は成功（`.tmp/sbx-socket-20260909-02/spec-v3-r2-fixcheck.json`）。git diff --checkも成功。上記6経路の机上照合を行ったが、動的実行と修正後の独立確認は未実施。

次はこの1件に限定した差分再確認を推奨。停止対象の引き継ぎという重要な接続なので、文言の存在確認だけより独立確認の便益がある。一方、対象は例外情報と消費側3仕様に限られ、全体のフル再走査は重複が多い。基本方針と4責務は維持、設計縮小のために停止保証を落とす案は提示しない。次の方式と採否確定はユーザー判断待ち。
