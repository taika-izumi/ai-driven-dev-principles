# Issue-0140 実装確認と未完了事項

- 日付: 2026-09-10
- 作業ブランチ: `codex/issue-0140-review-cost`（基点 `535b7dc`）
- 要求: 本タスクの「作業を進めましょう」で確定設計の実装に着手。設計・分担はIssue-0140の承認基準を継承。
- 変更: `skills/pre-finalization-review/SKILL.md`の但し書き1段落、同スキルの配布物、ADR-0080の部分修正注記。設計変更なし。

## 実施済みの確認

- 変更前の現用スキルに実測追記の限定除外条項が無いことを確認。これは文書差分の確認であり、エージェントの失敗を再現した試験ではない。従来の誤判定の実測はIssue-0140の記録を参照。
- 設計の引用文とソース追加文の完全一致（出所注記のみ付加）、設計の引用文と配布追加文の完全一致をシェルで検証。追加条項は双方1件。
- `build-dist.ps1`で生成後、`build-dist.ps1 -Check`と`sync-template.ps1 -Check`が成功。記法規約違反0、生成物同期済み。
- 配布本文の推奨判定を読み直した。出所番号が本文の文法に混入せず、書式例の実在名・自己参照・括弧の残骸・表示文字列の残骸をこの差分に追加していない。
- 既存の提示、レビュー済み扱い、迷い既定、2型分類・反復規範に編集なし。対照6例の論理照合は、観測追記のみ除外候補、許容拡大・根拠撤回・手順混在・対応環境拡大・記録欠落は除外しないという設計と一致。
- `git diff --check`成功。動的なエージェント試験・費用低減効果の測定は実施していない。静的確認をその代用の実証とはしない。
- 公開元masterを`git ls-remote`で確認: `7b8258fb0cf7f433635fbfff1739a3e8b13dcfe3`。同コミットのプラグイン版は0.1.24、作業側2マニフェストは予定版0.1.25。未公開版への追加修正なので版番号は維持できる。公開・導入は未実施。

## 独立レビューの実施と採否

確定設計を写した実装の仕様適合・配布整合を新規Read限定Claudeで検査する準備をした。対象3件と参照3件を `.tmp/issue-0140-implementation-r1/manifest.json` に記録し、コピー・ハッシュ・プロンプトを保存。

初回は自動承認レビューが外部API送信を拒否した。前回12文書の送信承認は実装後6文書への承認には当たらないとの理由。ユーザーが6文書とプロンプトの送信を明示承認した後に実行した。

新規claude-sonnet-5・Readのみ・MCPなし、対象3件と参照3件を全数確認。終了コード0、約143秒、CLI表示0.3256526ドル。主担当が送信前ハッシュとの一致を確認し原本変更0件。原回答・費用・対象版は[JSON](2026-09-10-issue-0140-implementation-r1.json)に保存。

指摘F1（Minor）はADR-0080の部分修正注記を末尾へ移す提案。前提は注記が昇順に並ぶ慣行だが、既存のADR-0133注記が先頭にあり、後方に0087〜0124が並ぶ実体と一致しない。順序の明文要件・内容への影響も示されていない。既存の最新注記の前へ追加した現状を維持し、不採用とする。新機構・追加検証工程を設けない。承認済みの表現・整合判断の範囲内で採否を確定した。

レビューは実装と確定設計の一致を確認し、対照6例すべてを一致と判定。実装後に設計変更なし。コードの動的試験・複数サンプルによるエージェントの振る舞い試験を行ったとは主張しない。

## サイクル全体整合検査

対象は535b7dcの確定設計と作業ブランチの変更・新規記録。規範文書を変更するため検査対象。主担当が現用スキル・配布本文・設計・ADR・反復規範を読み直して照合した。

1. **仕様のスナップショット性**: 承認済みの設計はIssue-0140の文案と対照例で、スキル追加文と一致する。独立した仕様ファイルは小規模経路のため作成していない。
2. **規範の書き戻し**: 追加条件はADR-0164 Decisionに対応し、ADR-0080に部分修正を注記。旧決定の他部分は維持。
3. **数値・完了基準**: 文案追加はソース・配布各1件。対照例は6件。初回推奨を除外した成果物は他の未レビュー規範が無い場合に通常型となり、既存の分布外通知は通常型4回・規範改定型8回のまま。人数は型だけで固定しない。
4. **経路の閉じ**:
   - 機能ブロック設計経由・設計文書経由・設計兼用ADR経由・plan経由の全確定点は、既存の同じ推奨判定へ到達する。確定点や発動箇所を追加せず二重発火を増やさない。
   - 利用条件不変の実測追記のみ: 対象外、提示は維持。実施・見送りの判断後に既存review記録を確定する。
   - 許容拡大・根拠撤回・手順混在・環境拡大: 変更した規範を従来判定へ含め、初回フル推奨を維持。記録は実施・終了判断後。
   - 記録欠落・条件不変か不明: 例外不成立、既存の迷い時の規定へ。確認不能をレビュー済みにしない。
   - 反復: 追加例外を停止や提示免除に転用せず、既存の反復手順と終了後の記録へ。改訂のたびに初回推奨を新設しない。
5. **引用の整合**:
   - ADR-0164 Contextのcf46d94は許容変更を含むため除外しない。d0a83efの当該ファイル差分は条件不変の追記候補だが、コミット全体を免除する根拠にはしない。
   - 同Contextと案1: 分類の曖昧さを但し書きで減らす。費用削減の数値や全モデルへの効果を主張しない。
   - 同Contextと案2: 既存の方式・委任では曖昧な初回推奨条件が残るという比較で、既存規範が全く機能しないという主張ではない。
   - 同Contextと案3: 取りまとめで回数を減らす可能性と逐次判断の遅延を比較。未実測の削減率を根拠にしない。
   - ADR-0080の提示・未レビュー差分・迷い既定、ADR-0124の方式判断、ADR-0133の委任と操作承認を、現用スキルの条件つきのまま維持。

結果: 指摘なし。設計の意味・適用範囲の追加変更なし。独立レビュー後も規範差分がないため重ねて実施せず、ADR-0164をAcceptedへ昇格しIssue-0140をcloseする。残作業はコミットとmasterへの取り込み判断。公開・導入・費用低減効果の実測は未実施。
