# モデル裁量比較の仕様レビュー実行準備記録

- 日付: 2026-09-10
- 対象確定点: spec
- 状態: 対象22ファイルのClaude送信をユーザーが明示承認し、独立レビュー完了。結果と指摘対応は `2026-09-10-model-discretion-spec-r1.md`。
- 依頼: ユーザーは1人4観点の独立レビューを承認。作成者とは異なるClaude Opus 5を選び、既存契約で起動する準備をした。

## 対象と保護

仕様4文書、計画草案1、ロードマップ1、準備メモ1、ADR-0166〜0168の3、用語表1、ログ解析コード8、対応テスト3の計22ファイルをスナップショット化した。対象一覧とSHA256は `.tmp/model-discretion-review-r1/controller/before.json`。実会話履歴・市場データ・認証情報は対象に含めていない。

レビュアーにはReadと引数なしの固定検査だけを公開する起動を準備した。固定検査はSHA256固定済みスクリプトで、新規run/attempt領域へ限定版のコードを取得し、計画のPythonコード例を実行する。任意コマンド・書き込み先の引数を拒否すること、保護代用品の不変を確認した。Reviewerの起動イベントによるツール一覧の確認は未実施。

別のWindowsサンドボックス経路は `helper_unknown_error: setup refresh had errors` で失敗した。保護成立として数えず、固定操作のみを公開する経路へ限定した。この限定経路は任意の生成コードを実行する一般的な隔離の成立証拠ではない。中断中のIssue-0136は再開していない。

## 主担当がローカルで実行した確認

| 確認 | 結果 |
|---|---|
| 指定版のgit archive | コード8・テスト3を取得成功 |
| 計画内unittest・原版 | 2テスト中1失敗。タイムゾーンなし値を不明として扱わないことを検出 |
| 計画内unittest・正常例 | コピー内だけの修正後、2テスト成功 |
| 混在日時のCLI・原版 | session.pyのmin比較でTypeError |
| 混在日時のCLI・正常例 | 終了コード0、4CSV生成 |
| 原版のsession-id指定 | 終了コード2、未対応オプションとして拒否 |
| 既存テスト3本 | 同梱Pythonにpytestがなく実行不能。合格として数えない |
| 原本の前後比較 | 対象22ファイルすべてSHA256不変、保護代用品2件も不変 |

証拠: `.tmp/model-discretion-review-r1/controller/coordinator-check.json`、`fixed-last-result.json`、`endpoint-denial.json`。正常例の修正はレビュー用コピーだけで、元プロジェクトの修正ではない。

## 未実施と次の操作

自動承認レビューは、非公開の仕様・計画・ソースコード・テストをClaude外部サービスへ送信することへの明示承認がないとして起動を拒否した。レビュー自体の承認と、具体的な資料送信の承認を区別する理由だった。起動前に拒否され、レビュアープロセス・結果は存在しない。独立レビューの実施回数は0のまま。

ユーザーへ上記22ファイルのスナップショットをClaudeへ送信してレビューを続けるか確認する。送信承認が得られた場合は同じ固定コピー・同じ制限構成で起動し、実際のツール一覧がReadと固定検査以外を含めば停止する。拒否を別経路で回避しない。

## 送信承認後の進行

2026-09-10、ユーザーが「この22ファイルをClaudeへ送信してレビューを続行する」に「1で」と回答。同じ起動を再申請し、Claude Opus 5を起動した。開始イベントの実モデルはclaude-opus-5、実ツールはReadとmcp__inspection__run_inspectionのみで、許可リスト内であることを確認。証拠は `.tmp/model-discretion-review-r1/controller/init-proof.json`。

主担当は通常ユーザー権限の既存Python仮想環境で固定コピーの既存テスト3本を実行し、23件成功を確認した。キャッシュ無効・新規の一時出力先を指定。これは同梱Pythonでのpytest不足とは別の経路の結果である。証拠は `.tmp/model-discretion-review-r1/controller/coordinator-pytest.json`。
