# 合成題材の条件変更の差分再確認と対応

ユーザーが「１で」で新規1担当の差分再確認を承認。gpt-5.6-solが4変更仕様（当初16追加・5削除）、関連04、ADR-0160/0161、反映記録を読み取り、4観点で確認した。コード・VM・モデルは起動していない。

開始時hashは`.tmp/sbx-socket-20260909-02/pilot-scope-review-before.json`。主担当が受領時変更0件を照合。改訂前5仕様は`C:/Users/d12an/.ai-dev-review-snapshots/MakeAiInstructions/20260910-pilot-scope-r1/`に保存、コピー元とのhash一致を確認した。

## 指摘と採否

行番号は改訂前の仕様。主担当が前提と契約の不足を読み直し、3件を反映した。基本方針や例外の許容範囲は広げない。

| ID | 重要度・前提 | 実証された到達経路 | 修正 |
|---|---|---|---|
| F1 | Critical。例外は承認済みの合成入力だけ | 02:23は照合を要求するが01のRequestは任意sourceRoot、Profile/PreparedRunに承認対象hashなし。scopeだけで実プロジェクトを通せる | pilotInputPathに入力ID・sourceRoot・manifestHash・既存承認記録参照を固定。準備、VM作成前、結果で照合 |
| F2 | Major。試作の割当はCPU2/2048MiB | 01:19は正整数と初回基準だけ、02は設定値と実効値の一致だけなので8CPU/8192MiBも通せる | scopeの下では2/2048以外を準備・作成で拒否 |
| F3 | Major。当該pilotの同時稼働は1VM | 同名チェックだけでは異なるrunIdの並行CLIが各1VMを作れる | daemon接続先・ユーザー単位の外側排他を全runで共有し、取得/解放APIとCLIのfinallyを定義。ロック下で稼働VMを確認 |

画像読取無効、SSH拒否、原本・Git・control・認証の保全、採否用通信拒否は維持されていた。04はprofile/limitsを02から使うため直接の矛盾はなかった。

## 機構を追加する必要性と負担

F1はscopeの表示文言だけでは対象を比較できないため、既に必要な具体入力の承認記録を実行へ対応付ける情報を追加した。CLIが自分で承認記録を生成する構成にはせず、主担当が既存の具体試験承認時に作る。本文転記や常設承認サービスを追加しない。固定入力の版が変わった際のhash更新・照合は必要。

F2は既存の数値検査を固定値にするだけ。F3は単一run内の順次実行では防げない並行起動の具体経路に対応し、既存OSの名前付きMutexを内部で使う。別サービスや利用者によるロック操作は不要だが、同時依頼はblockedとなる。CLI外の手動起動まで強制防止する保証はなく、開始時に並行起動を避ける前提を示す。外部VMは勝手に止めない。

変更は内部データ・検証・排他の補完。新たな必須基盤は導入しない。保護を落として文言だけ整える案では承認した条件を満たさないため採用しない。

## 確認結果と次の工程

主担当の文書照合10項目は成功（`.tmp/sbx-socket-20260909-02/pilot-scope-r1-fixcheck.json`）、git diff --checkも成功。入力照合→作成前照合→結果、ロック取得→通常/再検証→停止→finally解放の接続を確認した。動的実装と修正後の独立確認は未実施。

新規1担当で3件の修正に限定した差分再確認を推奨。入力対象と並行起動の経路に関わるため、文字列検査だけより独立確認の便益がある。一方、過去のN1や他の既存設計をフルで再走査する必要はない。今回の条件改訂は規範改定型、方式の選択と採否確定はユーザー判断待ち。
