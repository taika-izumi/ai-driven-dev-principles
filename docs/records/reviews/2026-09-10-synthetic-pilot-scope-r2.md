# 合成題材の条件変更の限定再確認と確定

2026-09-10。ユーザーが3件だけの差分再確認を「1で」で承認。新規gpt-5.6-sol担当が00〜03の改訂前後、04の消費側、ADR-0160/0161とr1対応表を4観点で読み取り確認した。開始時hashは`.tmp/sbx-socket-20260909-02/pilot-scope-r2-before.json`。主担当が受領時変更0件を確認。

## 結果

| 指摘 | 判定・証拠 |
|---|---|
| F1 承認入力の束縛 | 解消。pilotInputPath/id/hashが01の準備、02のVM作成前、03の結果へ接続。CLIの自己承認生成と常設台帳追加を禁止 |
| F2 資源値の固定 | 解消。準備・作成前・実効値の照合でCPU2/2048MiB以外を拒否。04も02のruntimeとlimitsを使用 |
| F3 同時起動の排他 | 解消。ユーザー/daemon単位のMutexをrunId/runsRootに依存させず、通常・recheckの取得から停止後finallyまで接続。競合はblocked |

新規矛盾0件。主担当も対象と承認範囲を照合し、修正を追加せず確定する。今回の条件改訂は差分再確認2回で実質的な収束。元v3のフル1回・差分2回と、今回承認された保護条件改訂を区別する。

コード・schema・VM・モデルは未実装/未実行。Mutex競合、資源の実効性、固定入力の動的拒否、SSH転送拒否、負荷中停止・切断時処理等は後続で実証する。静的な仕様確定を起動profileのverifiedへ読み替えない。

## 確定範囲

ADR-0160の資源保護変更とADR-0161のclipboard例外を、合成題材の試作限定として仕様確定する。設計としてAcceptedへ昇格し、実装前のためサイクル全体整合検査は実装完了時に行う。再検討を選択したADR-0159も、検討方針の決定として確定する。

実プロジェクト送信・モデル認証/起動・ホスト設定変更・通常運用への拡張は未承認のまま。次はSSH等の残る能力試験を具体化し、全体実装計画へ接続する。
