# レビュー観点の整理と理解確認の設計

2026-09-11。ADR-0186に基づく主担当の文書照合と、会話履歴を渡さない確認の準備。独立レビュー・理解確認は未実行。

## 変更と境界の照合

`skills/pre-finalization-review/references/review-criteria.md`を正本として新設し、問い・比較対象・根拠・対比例・報告をまとめた。`review-procedure.md`から定義を移し、`SKILL.md`の参照表、`examples-and-evidence.md`、`subagent-dispatch/SKILL.md`の委譲先を揃えた。

| 確認 | 結果 |
|---|---|
| 全体目的との整合 | 目的・方針の原文を入力とし、仕様適合で局所範囲と照合。ADR-0185を維持 |
| 仕様適合と前提実在 | 要求と成果物の比較／対策理由と実体の比較を分け、必要な機構の最終採否を費用比較と混同しない |
| 実装整合性と前提実在 | 接続先APIの不一致／保護理由の誤認で例示。計画された将来部品の未作成を既存前提の誤りとしない |
| 敵対的との関係 | 失敗条件・経路の探索は他観点と併用可能。同じ欠陥は1件へ統合し、分類の迷いで落とさない |
| 検査範囲の保全 | 4観点、前提実在の5判定、対象の総数・全数走査、実証・保護・採否順序は維持。未確認は無理に5判定へ換算しない |
| 入力不足 | 不明な根拠・版・条件を明記。原文にアクセスできないときは必要な原文と出所を渡す |
| 負担 | 関係資料を指定し全資料走査・新しい台帳を要求しない。正本が1ファイル増え、読解量が増える点は残る費用 |

## 理解確認の依頼案

会話履歴を渡さない担当1体へ、新しい定義と以下6事例の「入力」だけを渡す。期待値と今回の経緯は渡さない。各事例について、根拠に基づく指摘・関係観点・未確認・追加作業の要否を答えさせる。観点名の唯一一致を合格条件にせず、実質的な欠陥・根拠・必要な保留が揃うかを採点する。

| 事例 | 渡す入力 | 主担当の期待値（担当へ渡さない） |
|---|---|---|
| 1 | 方針: 設計・実装・運用の判断負担を改善。要求: 今回は判断手順の共通案を作る。案: 運用開始前の準備だけに適用。限定の承認・理由は資料にない | 目的と局所範囲の不整合候補を仕様適合で報告。自動で範囲を拡大せず限定理由の不足を示す |
| 2 | 方針: 未公開の合格基準を保護。案: 基準の推測を防ぐ新しい応答制限。実体: 同じ基準を公開ヘルプで既に説明。ほかの未公開情報はない | 保護理由の不成立を前提実在で指摘。制限実装の巧拙だけを評価せず、根拠と公開状態を示す |
| 3 | 現API契約: save(record)。計画: 引数変更なしと明記し、save(record, overwrite=true)を呼ぶ。別の変更タスクはない | 実装整合性で契約不一致。接続が未作成の予定だから問題ないとはしない |
| 4 | 要求: 複数担当が更新しても値を失わない。設計: 全担当がread→加算→write。初期値0、2担当が同時に加算。排他・版確認はない | 敵対的にread0/read0/write1/write1を構成し、期待2に対し1の経路を示す。仕様適合との重複を1件にまとめてよい |
| 5 | 全体目的: 複数OS対応。承認: 初回は1OSで確認し次段階で拡大。計画: 初回対象だけを実装。拡大条件と保留対象は明記 | 初回に全OSがないことだけで不適合としない。限定と目的へのつながりを確認する |
| 6 | 設計理由: 既存の安全機構がすべてを保護する。根拠資料は未提供で参照先も不明。対象コードは部分抜粋だけ | 必要・不要・欠落のいずれも断定せず未確認とする。必要な根拠・確認範囲を示す |

これは少数事例で誤解を探す確認であり、LLM全般の理解保証・効果の統計評価ではない。新旧を比較していないため改善の因果は断定しない。規範未適用との比較や反復試験へ自動拡大しない。

## 実施判断前の状態

主担当の検証: 生成・両Checkは終了0、39ソースの記法違反0、サイズ警告なし。`git diff --check`成功。配布された新定義全文を読み直し、観点・根拠・未確認・限定・5判定・報告例が残ることを確認。古い定義先・正本4ファイルの記載はスキル内で0件。新規定義の版0.1.26での作業用生成であり、公開・導入はしていない。

ADR-0185・0186はProposed。新定義と今回追加した接続は未独立レビュー。今回のユーザー指示は観点の整理であり、追加モデル利用の承認として読み替えない。

次の推奨は1体4観点の独立レビューと上記理解確認を同じ担当で行うこと。先に事例回答を得てから、今回の差分と方針の原文を渡して文書全体をレビューすれば、正解例を先に教えずに確認できる。概算10〜20分（未実測、反復は別）。追加読解費用はあるが、文書の作成者と別の担当で解釈の違いを調べられる。見送り時は未実証のまま確定する判断を明示する。

## 実施承認と送信先確認待ち

ユーザーは上記の実施案に「1で」と回答。履歴を引き継がない担当で6事例→4観点レビューを行う許可として受領した。

ネイティブの担当起動には個別のツール制限設定がないため、既存Claude CLI 2.1.267でツールなしの静的確認を選択。ヘルプで`--tools`の空指定、`--safe-mode`、`--strict-mcp-config`、`--restricted`の作用を確認。Claude Opus 5・mediumを指定し、開始イベントのツール一覧が空でなければ停止する補助を用意した。確認内容は文書のみで、追加の環境構築は不要。

起動申請は自動承認レビューで拒否された。理由は、独立レビュー実施の承認だけでは内部の新定義を外部Claudeへ送信する宛先指定付き承認を確認できないこと。CLI未起動・モデル実行0・理解確認未実施。別経路で回避しない。

送信候補は`.tmp/review-criteria-20260911/first-prompt.txt`（新定義全文と6つの人工事例、期待値なし）、`second-prompt.txt`（対象ソース5文書・ADR-0185/0186の全文、ロードマップ先頭44行、CONTRIBUTING先頭97行）。一覧は同`source-manifest.json`。会話ログ・認証情報は含めない。後半の送信は前半の結果を回収した後に行う。ツール空の実効確認は未起動のため未確認であり、保護成立とはまだ報告しない。
