# 目的・方針の参照設計：初回レビューと修正案

2026-09-12。ユーザーが2体のフルレビューを「1で」と承認し、「モデルはFableやAstra以外」と指定した。その後、内部資料285件のOpenAI Sol送信も「1で」と明示承認した。起動設定の補正を経て、gpt-5.6-sol/high指定の2体によるフルレビューを完了。7指摘を5論点へ統合し、分割仕様とADRに修正案を反映した。再確認・採否の確定は次の回答待ち。

## 対象と分担

- 対象: ADR-0190全文1文書。SHA256: `c3acc6d73cf527b5690cc13bfb74f0495b75e7b2b3592afe324bad45ce0b7a41`。
- 1体目: 目的・要求、根拠・設計判断、動作・検証・運用の3領域を兼務。
- 2体目: 他担当の初回所見を渡さず、独立して反例・失敗経路を探索。
- 全体目的の原文: ロードマップ冒頭・4.0節、AGENTS.mdの作業方法、ADR-0130・0183。旧案の中断・今回の承認範囲も両担当へ渡す。
- 根拠を自力で検索・閲覧できるよう、skills、docs/overview、docs/current、docs/records/decisionsのMarkdownと、README・AGENTS・CONTRIBUTING・template.manifest、今回の照合メモと関連課題2件を複製した。
- 複製は285ファイル・2,389,196バイト。全件読破を課さず、対象設計は全文、根拠は必要箇所を調べる。Git履歴・認証・個人設定・会話ログ・他プロジェクトの実体は複製していない。

## 準備と保護

資材は元checkoutの `.tmp/adr-0190-sol-review/`。review.py、2件のprompt.txt、snapshotとbefore.json（全ファイルのSHA256）を保存した。作業worktreeにある未コミットのADR本文を含む。

組み込みの委譲ツールには、この呼出側から子の操作を読み取り専用へ制限する引数がないため、既存Codex CLIの別セッションを使う構成とした。execではread-only、approval_policy=never、ignore-user-config、ephemeral、apps/plugins/multi_agent/browser/computer-useの無効化を指定する。これらは起動案であり、実モデルのツール一覧や操作の実測ではない。

非モデルのsandbox確認は、現在のヘルプで構文を確認し、同CLIのread-only指定で新規代用品probe.txtの読取り成功・書込み拒否を確認した。代用品の本文は不変。これはシェル経路の限定確認であり、実モデル起点の全操作経路・通常実行時保存の不変を実証したものではない。モデル起動後は実際のツール使用と対象状態を照合する。

両実行は別の出力先へ回収し、再委譲せず、各実行を20分で停止する設定。通常のOpenAI認証を使用し、キーの読出し・コピー・追加課金への切替はしない。2実行の起動前に資料送信の承認で停止しているため利用量は未取得。

## 起動拒否と現在地

`python .tmp/adr-0190-sol-review/review.py run` を権限付きで要求したところ、自動承認レビューがプロセス作成前に拒否した。

理由: レビューの実施は承認されているが、内部リポジトリの285ファイルをOpenAIのSolへ送信する具体的なデータ・宛先の明示承認がないとの判断。285件は探索可能な資料集合であり、実際の送信は初期依頼と担当が読んだ内容になる。未読ファイルの本文を初期依頼へ全件埋め込む構成ではない。

domains/started.jsonとchallenge/started.jsonはいずれも存在しない。モデル起動0回、独立レビュー0回、指摘0件（未実施のため）。別経路での送信・再試行は行っていない。

次は、この探索対象の内容が必要に応じてOpenAIのSolへ送信されることへの承認を得る。承認後は同じ対象版・2実行を使い、初回所見の独立性を維持して結果を回収する。Fable・Astraへ置換しない。

## 承認後の実行と結果

上記の送信承認をユーザーの「1で」で得た。対象ADRと固定コピーのSHA256一致、開始ファイル不在を確認して起動した。通常設定を除外した最初の2体はPowerShell読取りが拒否され、Gitに固定した同内容を使う次の2体も読取りが拒否された。本文の検査はできず、いずれもレビュー不能として終了した。git statusの成功を資料読取りの成立とは扱わない。

通常設定のwindows.sandbox=elevatedが除外されている差を確認し、その値だけを引数へ明示した。read-only・never・不要な連携や再委譲の無効化は維持し、通常設定は変更していない。補正後は両担当の対象読取り・必要な根拠探索が成立した。レビュー前の非モデル確認が、この設定を除外した実モデル起動まで保証するという見立ては誤りだった。

| 実行 | 結果 | 時間 |
|---|---|---|
| 最初の2体 | 資料未取得、検査不能 | 59.92秒、203.95秒 |
| Git読取りへ変更した2体 | 資料未取得、検査不能 | 53.73秒、60.71秒 |
| Windows設定を明示した3領域担当 | 対象全文1/1と根拠を検査、4指摘 | 421.01秒 |
| 同設定の独立反証担当 | 対象全文1/1と根拠を検査、3指摘 | 390.05秒 |

各組は並行のため、時間を足して経過時間とはしない。有効なフルレビューは2体で1回。先行4実行も消費へ含めるが、設計を検査した回数には数えない。

- 有効レビュー2体の利用量: 入力2,191,271（うちキャッシュ1,962,880）、出力31,880トークン。
- 検査不能4実行の利用量: 入力426,728（うちキャッシュ361,344）、出力11,459トークン。キャッシュは入力の内数。金銭費用・契約枠の消費率へ換算しない。
- 実際の完了項目はcommand_executionとagent_message、未安定機能の警告。file_change・MCP・子起動は記録なし。固定資料285件の前後SHA256・一覧は一致。CLI内部の通常実行時保存全体の不変を主張しない。
- 生の所見・起動結果・利用量は同名JSON、元イベント・引数は `.tmp/adr-0190-sol-review/` の各役割ディレクトリ。初回所見を相互に渡していない。

## 主担当の照合と対応案

| 論点と指摘番号 | 根拠・必要性の確認 | 対応案・費用比較・採否案 |
|---|---|---|
| 機能ブロック分割：ADR0190-F01 | 現行しきい値は新設だけでなくコンポーネント3つ以上を含む。既存4スキルの契約変更を含み、初稿の非適用理由は不足 | 採用案。目的の取得・変更、ワークフロー接続、導入更新・検証の3仕様に分け、ADRは理由へ絞る。文書数は増えるが、詳細の複写は避ける。新しい実行工程・サービスは作らない |
| 参照内容の識別・変更保全：ADR0190-F02、CF-01 | パスだけでは可変ファイルの参照内容を特定できず、正本更新が途中で止まった場合の引継ぎが曖昧。decision-delegationの承認版要件は許可の根拠であり、そのまま全目的へコピーする規範ではない | 趣旨を採用案。既存コミットと箇所、または内容を保持した記録・必要な原文で参照内容を識別し、未反映変更を保存する。全目的のハッシュ採取や専用版台帳は導入しない。必要な根拠の受渡しを具体化する負担に限定 |
| パス基準・未定義時の探索：CF-02 | サブディレクトリ開始で標準相対パスをcwd基準にすると不存在扱いになる。既知目的の再探索・再質問を増やす経路がある | 趣旨を採用案。プロジェクトルートを明示し、指定→標準入口→案内と直接参照の合意を調べ、判断可能になれば終了。下位AGENTSを無条件に最優先の目的とする提案は採らず、明示された適用範囲に従う。新しい検索基盤・件数上限は作らない |
| read-only時の初期整備：ADR0190-F03、CF-03 | 現行start-workはレビュー・調査にも適用される。目的の取得と文書作成を同じ必須処理とすると、権限超過または不要停止が生じる | 採用案。参照・判断と永続化を分け、今回の範囲・権限内の場合だけ整備。既存目的が確定していれば根拠として使い、不明・草案とは区別する。レビューのための書込み許可の取得を新たな前提にしない |
| 既存利用側の更新：ADR0190-F04 | manifestはAGENTS全体を配布し、READMEにはプラグイン更新と固有追記の案内がある。単なる丸ごと上書き禁止だけでは共通接続をどう届けるか不足 | 採用案。変更前保全→共通部だけの差分統合→固有正本指定の保持と到達確認をREADMEへ定義する。手動統合は利用者・担当AIの作業になる旨を示す。自動マージ器・一括書換えより維持対象を少なくし、衝突箇所だけ報告 |

修正先は `docs/current/specs/2026-09-12-project-purpose-context/`（概要＋3ブロック）とADR-0190。指摘のレイヤは5論点とも設計骨格（接続・条件・保存契約）として扱う。修正は判断を入れたもの。反復による機構の増設は今回1回で、2回連続増設の点検条件には未到達。

## 退避・残る確認と次手

修正前ADRは `C:/Users/d12an/.ai-dev-review-snapshots/MakeAiInstructions/2026-09-12-adr0190-r1/0190-before.md` に保全し、元SHA256との一致を確認した。生の入力snapshotも保全する。Accepted済みADRは変更していない。

現行スキル・目的の正本・配布物の実装は未着手で、代表作業の判断比較・配布更新の実動作は未確認。今回のスナップショットに生成スクリプト・配布物を含めていなかった制限も原報告に保存した。配布契約の指摘はmanifestとREADMEの原文によるもので、生成器の失敗を実証したものではない。

初回フル1回で設計骨格の指摘があり、骨格安定・実質的な収束には達していない。修正の境界と指摘対応を追えるため、新規Sol/high 1体で差分再確認する案を推奨する。対象は元ADR・修正後ADR・分割仕様4文書・本対応表と関係する既存契約。分割と保存契約が複数箇所へ影響するためフル再実施にも便益はあるが、同じ根拠の再読・集約費用が増える。次の実行・確定はユーザーの反復選択に従う。

起動設定の欠落・回復はworklog `MakeAiInstructions-2026-09-12-01`へ記録した。
