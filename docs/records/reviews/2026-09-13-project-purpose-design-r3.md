# 目的・方針の参照設計：起点分担の差分再確認と設計確定

2026-09-13。ユーザーが、修正した起点分担1点をSol/high 1体で再確認する選択肢1を承認した。当該差分・対応表・関係する既存指示のOpenAI送信も提示済みの範囲で実行した。

## 結果

新規gpt-5.6-sol/high指定1体が、変更3/3文書とADR0190-R2-01の1/1点を確認した。前回の指摘は解消、範囲内の新規Major・Minorは0件。主担当の新規開始、目的が揃った子、目的が不足する子、read-onlyの子の4分岐について、既存の委任範囲・権限を拡張せず、子の目的取得を免除しないことを原文と照合した。主担当もR2の衝突箇所と修正先を照合済み。

今回はこの1点の差分再確認であり、R2で解消した5論点をフルで再審査していない。実装・配布同期・実モデルの開始動作は未確認。静的レビューを動作検証の成功としない。反例探索は1体の兼務で、独立反証ではない。

## 実行と証拠

- 固定入力21ファイル・210,320バイト。前回のWindows sandbox=elevated、read-only、neverと不要連携無効化を再利用し、最初の差分読取りから成功した。
- 149.59秒（約2分30秒）、終了0。入力269,175（うちキャッシュ193,536）、出力6,793トークン。金銭費用や契約枠の消費率には換算しない。
- 固定資料の前後一覧・SHA256は一致。担当は編集・子起動を行っていないと報告。通常のCLI実行時保存全域の不変は確認対象外。
- 原所見・利用量は同名JSON。生イベント、依頼、引数、前後一覧は元checkoutの `.tmp/adr-0190-sol-review-r3/`。

## 確定と次手

2026-09-12〜13のフル実施（Sol/high・1回、2体）＋差分再確認（Sol/high・2回、各1体）を通じ、指摘はすべて解消し、今回の新規設計欠陥・残指摘は0件。停止判定の第1経路により実質的な収束とする。初期の読取り不能4実行はレビュー回数へ含めず、R1に消費を保存済み。

ユーザーが選択した再確認の結果、追加の改訂が生じなかったため、反復を再提示せず設計を確定する。確定対象はADR-0190と `docs/current/specs/2026-09-12-project-purpose-context/`。実装を伴う決定なのでADRのStatusはProposedのまま実装完了・検証後の昇格を待ち、設計確定と実装反映済みを区別する。

次は確定仕様から実装計画を作る。worklogは棄却（既存の起動・差分確認手順を再利用、追加deltaなし）。
