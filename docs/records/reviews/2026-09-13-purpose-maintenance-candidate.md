# 正本整備候補の提示：設計レビューと実装確認

- 対象: ADR-0191、開始・再開時の整備候補提示。ユーザーが追加仕様とSol/high 1体のレビュー後の実装を選択。
- モデル・方式: gpt-5.6-sol/high、新規1体のフルレビュー。3領域と反例探索を兼務し、別担当による独立した反証とは称さない。規範改定型、通算フル1回。
- 実行: 482.26秒、終了コード0。対象全文1件、根拠を含む固定13ファイル。read-only、approval_policy=never、windows.sandbox=elevatedを明示した既存CLI経路。元ファイルを複製し、主担当による前後ハッシュ比較で13ファイルすべて一致。実際の読取り成功を確認。
- 原報告: [JSON](2026-09-13-purpose-maintenance-candidate.json)。起動引数・生出力・改訂前本文: `.tmp/adr-0191-review/`。

## 指摘と採否

Major 0件、Minor 1件（F01）。「正本未指定」だけでは、明示指定がなく標準入口で確認できる正常状態も含み得た。既存の特定手順2～4とユーザーの「確認できない場合」を対比し、指摘を採用。標準入口まで調べた結果に基づく条件へ修正し、正本も既存合意もない状態を検証表へ追加した。

構造・権限・要求範囲は変わらず、承認済みの実装に必要な条件の明確化として進めた。修正後の原文照合と機械チェックを主担当が行い、追加モデル実行は行っていない。独立再レビューで収束したとは扱わない。

## 実装範囲と分割判断

start-work本体のPhase 0から参照手順の候補提示へ接続し、分岐前に提示する。詳細条件はreferences/project-purpose.mdへ置いた。READMEと現行仕様00・02・03を同期し、ADR-0190へ部分修正の参照を追記した。

主要機能1件、変更する実行上の責務はstart-workの目的確認と候補提示。既存のhandoff・委譲・レビューへの契約は変更しない。既存構成も数えたうえで、2機能または3コンポーネントの追加分割しきい値には達しない。既存の3ブロック仕様を更新し、新たな仕様分割や別の差分仕様書は作成していない。

## 主担当による机上確認

ソースと生成済みdistを読み直して以下を確認。これは規範に従った経路の照合であり、新規エージェントの実行試験ではない。

| 状態・経路 | 確認結果 |
|---|---|
| 明示指定なし、標準入口で読める | 特定手順3で正本確認済みとなり、提示対象外 |
| 別文書の正本指定が読める | 特定手順2で確認し、標準パスへの作成を提示しない |
| 正本なし、既存合意あり | 既存合意を使えるが、正本未確認なので候補を提示 |
| 正本・既存合意ともになし | 候補を提示し、不明が影響する判断のみ相談 |
| 指定先不存在・読取不能 | 理由を添えて参照先確認・復旧を候補に含める |
| read-onlyで正本未確認 | 候補を提示、書込みや整備への着手は強制しない |
| 前回見送り後の次回開始・再開 | Phase 0の分岐前で再取得・再提示。前回の見送りで恒久抑止しない |
| 同一実行内のPhase 2ループ | Phase 0へ戻る接続はなく、参照手順も重複提示を抑止 |

## 配布・機械確認

- 変更前と変更後のbuild-dist -Check、sync-template -Checkは成功。変更後の両生成器を実行し、distの候補提示本文と入口を読み直した。ソース40ファイル、dist42ファイル、template10ファイル。固有目的本文は配布に追加していない。
- build-distの最初の生成は.agents/plugins/marketplace.jsonの保護で停止。権限付きで既存生成器を再実行して成功。通常実行の失敗を成功とは数えない。
- start-work本体19,961バイト、サイズ警告なし。AGENTSは9,844バイト・29箇条で変更なし。git diff --check成功。
- 公開前の追加修正なので予定版0.1.29を維持。公開・利用環境更新は未実施。

## サイクル全体整合検査

1. 仕様のスナップショット性: 全体仕様00～03を読み、候補提示は02、検証境界は03、全体完了条件は00へ反映。01の特定・変更契約は維持。
2. 規範の書き戻し: start-work本体と参照手順の追加はADR-0191と仕様02へ対応。READMEも同じ条件。
3. 数値・完了基準: 追加の検証表8行と上の机上結果8行は対応。独立モデル実行は設計レビュー1回のみで、実運用評価3件は未実施。
4. 経路の閉じ: 新規／再開はPhase 0の分岐前で発火。未確認は提示、標準／指定正本を確認済みなら非発火。同一Phase 2反復は非発火、次回start-workは再判定。handoff単独read／子の目的確認は非発火。提示時点では作成・採用を確定せず、選択後の保存は既存権限に従う。
5. 引用の整合: ADR-0190の特定手順を経てから判定し、明示指定なしを失敗扱いしない。read-onlyの権限と主担当限定を保持。Contextの根拠はユーザー要求と再現可能な未提示経路で、代替案1は既存手順補足、2はその経路の受容、3は要求を超える強制として評価しており、実測効果の主張はない。

## 限界

設計レビューは文書上の確認。改修前後の独立エージェントによる行動比較と実際の未整備プロジェクトでの動作は未実施。機械チェックは配布の同期・書式を検査し、候補が必ず提示されることを保証しない。実運用評価はADR-0191の既存記録を使う観測へ残す。
