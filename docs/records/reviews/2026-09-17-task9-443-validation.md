# タスク9: 443番限定への訂正・検証経過

## 承認と実装

利用者は初回実測の訂正案に「1で」と回答し、443番限定への設計・判定処理の修正と、その検証後の新規VM2台による再試験を承認した。ADR-0199へ反映済み。作成するVMの追加allow・専用キット・daemon操作・既存6台の起動/削除・モデル往復は含めない。

変更は、profileの通信条件、実効規則のポート照合、policy check専用の終了コード/JSON/対象照合、probeの専用経路、および対応する試験・仕様。汎用JSON照会は終了0のみを受理する。正常拒否を受理する変更によって既存のdeadline-reached/query-timed-out分類が変わらないことも追加確認した。

## 完了した確認

- 実測3ファイルを試験fixtureへ保存。原文は初回run `f107bb84` のpolicy ls・許可・拒否の応答。秘密値は含まない。
- RED: `.tmp/task9-probe-approval/443-policy-red.log`。旧コードがポート付き規則を拒否し、意図した失敗になった。
- GREEN: 同ディレクトリ`443-policy-green.log`。正常許可・正常拒否、終了コード不整合、型不正、対象VM/接続先不一致、重複キー、実行失敗、probe/製品の専用経路、一般照会の非ゼロ拒否を確認。
- 分類維持のRED: `443-policy-status-red.log`。deadline-reachedがblockedへ変わることを検出。修正後は`443-policy-final-green.log`で全項目成功。
- `git diff --check`はエラーなし。主担当の確認を独立レビューとは扱わない。

## 既存11群（完了）

記録は `.tmp/task9-probe-approval/443-independent-tests.log`。2026-09-17 09:37 JST開始、主担当の実行セッション52556。通常ユーザーで、既存のテスト用codex.cmd（呼ばれたら97で失敗する）をプロセスPATHへ置き、Run-IndependentTests.ps1を実行した。実VM・実モデルは起動しない。

10:12 JSTに全11群が終了0で完了した。最後の出力は Independent verification: 11 suites passed (no agent launched)。試験内の意図的な失敗を示す診断行は、群全体の成功と区別して記録に残した。

実行中に追加した変更は、専用応答検査の実行不能/時間超過の分類維持、レビュー指摘のallow scope照合と、それぞれの対象試験。SbxRuntimeV3本体は変更前にモジュールを読み込んでいるため、11群の結果と最終版の焦点試験を組み合わせて記録する。新たな懸念がなければ関係のない全群を重ねて実行しない。

## 独立レビュー（完了）

OpenAI Codexへ修正資料を送る起動は、具体的なペイロードと宛先未承認として自動承認レビューが拒否した。その後、利用者へ一覧を提示して追加の「1で」を得て送信し、初回レビューと差分再確認が完了した。両指摘解消・新規指摘なし。詳細は2026-09-17-task9-443-independent-review.md。送信対象と方法は `2026-09-17-task9-443-review-export-request.md`。18ファイル相当のコピーと依頼文を用意し、`.tmp/443-review/payload-manifest.json`へ内容ハッシュを保存した。再試験は独立レビュー後に実施する。

その前の無害な代用品だけの保護確認は完了したが、read-only実行でシェル読取り自体がpolicy rejectionとなり、正常読取りと書込み拒否の一組を確認できなかった（guard-events.jsonl、guard-stderr.txt）。guard.txtはoriginalのまま。レビュー本番はシェルを無効にし添付本文で確認する案とし、apply_patchが公開される場合には代用品への拒否を確認し、もし書き込めたら中断する。代用品試行の成立を、資料レビュー完了や原本保護の実証と扱わない。

## 最終の追加照合

- 443-review-scope-red.log/green.logで別スコープallowの受理漏れを修正したRED/GREENを確認。
- 443-all-real-responses-green.logで、保存済み実測規則と16件の認可器応答を最終版へ入力し、すべて整合することを確認（実VM呼出しなし）。
- 新規4ファイルのindex本文が作業ファイルと改行正規化後に一致することを主担当が確認。
- 既存11群に、最終変更を覆う焦点試験を組み合わせた結果であり、全11群を最後の1行変更後に重複実行したものではない。
