# Issue-0033: サブエージェント委譲の起動プロンプトに入れるべき定型項目が未定義

- **Status**: open
- **Opened**: 2026-07-31
- **起票元**: `worklog-extract` 走査（2026-07-31、初回実行）候補2。中央ストア 94 エントリ中 10 件が該当し、単一クラスタとして再発回数が最多
- **関連**: `superpowers:dispatching-parallel-agents`、`superpowers:subagent-driven-development`、`superpowers:requesting-code-review`、Issue-0034（実証を課した独立レビュー。委譲先へ課す要求の中身として隣接）、原則3（コンテキスト管理）、原則4（人間の関与）

## 課題内容

サブエージェントへ作業を委譲するとき、AI のデフォルト挙動は「タスク内容だけを書いた起動プロンプト」を渡すことである。実際にはそれだけでは足りず、毎回同じ種類の追加制約を後から補う必要が生じていた。既存スキル（`dispatching-parallel-agents` / `subagent-driven-development` / `requesting-code-review`）は「委譲する」判断と分割の仕方は扱うが、**起動プロンプトに毎回入れるべき定型項目**を持たない。

worklog の根拠エントリから抽出された、実際に不足していた定型項目:

1. 指示と実態が食い違ったら実態を優先し、理由を添えて報告すること
2. 既存（pre-existing）のファイル・ディレクトリの削除は事前承認を要すること
3. 本タスクの範囲外の作業（handoff 更新・ADR 作成など）はしないこと
4. 長い作業は複数コミットへ分割し、中断時の復旧単位を作ること
5. 実証を伴わない指摘は採用しないこと
6. 検査対象は全数走査すること（「全ガードへ削除ミューテーションを当てる」等、母数を明示する）
7. 副エージェントを起動しないこと／起動する場合は制約を継承させること
8. 並列でミューテーション（ファイル改変）を伴う観点には、worktree または `git archive` による隔離コピーを与えること
9. 計画に書かれた期待値のうち古くなっているものは、補正情報として明示的に渡すこと
10. 「この箇所へこの変異を当てよ」と書くなら、対象の実在を grep で確認してから書くこと

10 件中 9 件が `friction`（AI 自力の手戻り）であり、人間の指摘を待たずに再発している。

## 根拠エントリ

- 代表 id: `LoopForAlpha-2026-07-27-04`
- 全根拠 id: `LoopForAlpha-2026-07-22-09`, `-2026-07-23-01`, `-2026-07-23-03`, `-2026-07-27-03`, `-2026-07-27-04`, `-2026-07-27-05`, `-2026-07-28-05`, `-2026-07-28-08`, `-2026-07-29-06`, `-2026-07-30-01`
- model 分布: claude-opus-4-8 ×1 / claude-opus-4-7 ×2 / claude-opus-5 ×6 / claude-opus-5[1m] ×1（不明 0）

## 検討状況

- 2026-07-31: `worklog-extract` 初回走査で採用（`processed.jsonl` へ `adopted` 追記済み）。`worklog-skillify` へ受け渡し予定。既存スキルとの関係上、新規スキル作成より **`subagent-driven-development` / `dispatching-parallel-agents` への定型チェックリスト追加**が適する見込み
- 2026-08-05: 配布先からの申し送り（ADR-0061 / ADR-0063）。`LoopForAlpha#Issue-0069`（独立レビューを同一ワーキングツリーで並列に走らせるとミューテーションが相互汚染し測定の意味が失われる）が本課題の定型項目リスト**項目8**に該当する。3 観点すべてが独立に事象を報告しており、汚染中に測った Pester が `281 passed / 1 failed` を返してクリーンな木の `282 passed / 0 failed` と食い違い、担当が「回帰」と読み違える寸前だった。2026-08-05 に再発条件を再度作っており（サブエージェント 2 体の同時起動）、汚染は起きなかったものの**汚染の不在を事後に証明する手段が無い**という核心がそのまま再現している。同じく `LoopForAlpha#Issue-0063`（検証用の足場がプロセス名で子プロセスを一括停止し、エージェント自身の Claude Code プロセス 9 件を落として作業セッションごと消失させた）は**項目7**（副エージェントを起動しない／制約を継承させる）と隣接し、「停止対象は起動時に得た PID で指定し、プロセス名での一括停止をしない」という形で項目の追加候補になる
- 2026-08-05: 本課題の定型項目リストを、`worklog-extract` の走査サブエージェント起動プロンプトへ試験的に適用した（項目 1 / 3 / 5 / 6 / 7 を明文化して渡した）。スキル化前の実地確認。**項目1（指示と実態が食い違ったら実態を優先し理由を添えて報告）が実際に発火**し、走査担当が「指示は v1 行 3 件・`friction` が文字列 3 件としているが、実測では文字列は 2 件（1 件は `friction` 欄自体が無い）」と報告した
- 2026-08-05: `worklog-extract` 走査（2回目）候補7 を本課題へ合流（ADR-0063 の「一般形が既存課題に含まれるなら追記」）。根拠 8 件（friction 23 / corrections 4、全件 LoopForAlpha 出所）。**本課題の既存 10 項目に含まれていない 3 面**が抽出された:
  - **項目候補11**: 計画に書かれた期待値（テスト件数など）は、タスクの増減で以降すべて陳腐化する。委譲のたびに「現在の実測値」と「計画に書かれた古い期待値」の両方を渡す（`LoopForAlpha-2026-07-28-08`。**10 タスク中 7 回ずれた**）
  - **項目候補12**: ミューテーション表の各行に「その変異を検出できる入力」を併記させる。書けない行は等価変異であり、検出力の証明にならない（`LoopForAlpha-2026-08-01-03`）
  - **項目候補13**: throw を投げるだけの検査を RED 件数から除外する（同上）
  - あわせて既存の項目1（実態優先の報告）の重要性が裏付けられた。コーディネータが計画に書いたコード・検証手順の欠陥が 6 件（`-08-01-03`）／4 件（`-08-03-02`）検出されており、いずれも実装者の事前実測とレビュアーの変異実験で判明している。**逆に実装者が黙って従っていれば品質が下がる指示が 2 回あった**（`-07-22-09`）
  - 全根拠 id: `LoopForAlpha-2026-07-22-02`, `-07-22-09`, `-07-23-01`, `-07-27-03`, `-07-28-08`, `-07-29-06`, `-08-01-03`, `-08-03-02`
- 2026-08-05: **適用条件を ADR-0066 で確定**（ADR-0065 に従い、根拠一覧をそのまま規範へ写さず設計を挟んだ）。「委譲先の作業量を増やすか」を基準に、A 群 = 常時適用 4 件（実態優先の報告 / 既存ファイル削除の事前承認 / 範囲外作業の禁止 / 副エージェント起動の禁止）、B 群 = タスクの型で条件発火 8 件へ分割。項目 9 と 11 は同一主題のため統合し、規範として書く項目は 13 → 12 件になった。**スキル本体の authoring は次サイクル**（ADR-0065）
- 2026-08-05: **残る未決事項** — B 群の発火条件（「このタスクは検査か」「並列でファイルを書き換えるか」）の判定は委譲側の判断に残っており、誤判定すると発火しない。A 群と違い取りこぼしうる。判定を機械化できるか、あるいは判定自体を委譲プロンプトの定型に含めるかは authoring 時の設計事項
- model 分布が opus-5 系に寄っている（10 件中 7 件）が、走査エージェントの評価では「その時期にサブエージェント駆動を集中的に使っていたことの反映であり、モデル特性より作業様式に相関」。モデル固有の躓きとしては扱わない

## 結論

（open）
