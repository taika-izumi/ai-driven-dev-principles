# Issue-0042: 検査・検出器の「検出力」を実証してから「守られている」と言う規律がない

- **Status**: open
- **Opened**: 2026-08-05
- **起票元**: `worklog-extract` 走査（2026-08-05、2回目）候補1。走査対象 118 件中 16 件が該当し、単一クラスタとして根拠数が最多
- **関連**: `superpowers:test-driven-development`（新規テストの RED 実測は義務化済み。既存検査の検出力は射程外）、`superpowers:verification-before-completion`（主張の前にコマンドを走らせるが、その検査が何を固定しているかは問わない）、`superpowers:requesting-code-review`、ADR-0064（本課題の一部を先行実装）、`LoopForAlpha#Issue-0084`（配布先での同一課題）、`LoopForAlpha#Issue-0081`（対象システム側の断面）、原則5（漸進的検証）

## 課題内容

AI のデフォルト挙動は「検査を書く → 緑（または本番 0 件）→ 完了」である。実際に必要だったのは、**その検査が RED になる変異が実在するかを毎回確かめる**ことだった。

本番で 0 件・全緑が出た状態は「**欠陥が無い**」と「**検出器が壊れている**」を区別できない。走査では、この区別が付かないまま「守られている」と報告する事象が 5 世代のモデルにまたがって 16 件再発している。

根拠エントリから、繰り返し要求された派生規律が 3 つ抽出できる:

1. **対策前の実装へ差し替えて落ちるか**を確かめる（`LoopForAlpha-2026-07-22-03`。`git show HEAD:<path>` で過去版を取り出して当てる）
2. **変異が no-op でないか**を、対象行を直接読んで確認する（`LoopForAlpha-2026-07-29-03`。等価変異は「検査に検出力が無い」ことの証明にならない）
3. **検出器には既知欠陥を含む入力を正の対照として同梱する**（`LoopForAlpha-2026-08-04-07`）。あわせて負の対照（正常入力を誤検出しない）で粒度を固定する

### 既存スキルとの境界

`test-driven-development` は新規テストについて「失敗を見てから書く」を義務化するが、**既存検査の検出力**と**等価変異の切り分け**は範囲外。`verification-before-completion` は「主張の前にコマンドを走らせる」ことを求めるが、その検査自体が何を固定しているか（書かれ方か、振る舞いか）は問わない。`requesting-code-review` のレビュアーテンプレートに変異・対照の要求はない。

**どのスキルにも無い面**: ミューテーションの全数走査 / 等価変異の切り分け / 検出器への正負対照の同梱 / 変異が実際に適用されたかの確認。

## 根拠エントリ

- 代表 id: `LoopForAlpha-2026-08-04-07`
- 全根拠 id: `LoopForAlpha-2026-07-22-01`, `-07-22-03`, `-07-22-07`, `-07-23-04`, `-07-26-03`, `-07-26-11`, `-07-26-12`, `-07-26-14`, `-07-27-05`, `-07-29-03`, `-07-29-07`, `-07-30-05`, `-08-01-02`, `-08-04-01`, `-08-04-07`, `MakeAiInstructions-2026-08-05-02`
- delta 内訳: friction 44 / corrections 1（**人間の指摘をほとんど待たずに AI 自身が繰り返し踏んでいる型**）
- model 分布: opus-5 ×11 / opus-4-8 ×3 / opus-4-7 ×1 / opus-5[1m] ×1
- 出所: LoopForAlpha 15 件 / MakeAiInstructions 1 件 → scope: `general-candidate`

## 観測された実害

- 全数走査で「**死んだ検査 8 個**」「検出力の無いガード 5 件」を検出（`LoopForAlpha-2026-07-27-05`）
- 旧実装へ戻しても PASS する空振りテスト（`-07-22-03`, `-07-23-04`）
- 削除した旧テストが守っていた fail-closed 3 経路が無検査化（`-07-22-07`）
- `flush` 1 行を消しても 23 件全緑のまま、実プロセスは 8 秒デッドロック（`-07-26-11`）
- 大小衝突を探す検出器が `Sort-Object -Unique` を使い、**探している当のものを潰して常に 0 件**を返していた（`-08-04-07`）
- ADR-0092 が導入した台帳識別子が、ソース文字列一致の検査に守られたまま**導入以来一度も機能していなかった**（配布先 ADR-0107。1 回限り・不可逆の操作の記録が事実と逆に記録され、本番実行まで誰も気づかなかった）

## 検討状況

- 2026-08-05: `worklog-extract` 走査で採用（`processed.jsonl` へ `adopted` 追記）。`worklog-skillify` へ受け渡し予定
- 2026-08-05: **本課題の一部は ADR-0064 で先行実装済み**。`skills/worklog-extract/scripts/check-store-health.py` に正の対照 5 種・負の対照 2 種を同梱した。その実装過程で、初版が課題本文の要求（中途 BOM）を満たさないまま self-test が PASS していたことが判明しており、**対照の網羅性は「元の要求が挙げている失敗の形」から導く必要がある**という知見が加わった（`MakeAiInstructions-2026-08-05-02`）
- 対策の方向（未決）: (a) `test-driven-development` を既存検査・検出器へ拡張 (b) `requesting-code-review` のレビュアー要求に変異・対照を足す (c) 新規スキルを立てる、の 3 案がある。採否は `worklog-skillify` で判断する
- 2026-08-07（ADR-0080 サイクル）: **再発、および有効な検査方式の実測**（ADR-0031）。実装計画に書いた検証コマンド 24 本のうち複数が検出力を欠いていた。節見出しの存在だけを見る検証は、**規範の中核段落（「フルレビューを選択肢の先頭に置く」）を丸ごと削除しても緑を返した**。ほかに、2 語句を正規表現の OR でまとめたため同一行にある場合に `Select-String` が 1 行としか数えず**正しい実装でも期待値に届かない**検証、総件数のみを見るため追記先を間違えても緑になる検証、Status の行数を数えるため値が `Superseded` に変わっても検出できない検証があった。**有効だった検査方式は「正しい実装後の状態と、意図的に不完全な状態 28 件を scratchpad に構築し、全検証コマンドを両方に実行して結果が分かれるかを見る」写経実行**で、Critical 2・Major 5 を検出した。対策設計時はこの方式（欠陥状態の構築と対照実行）を候補に含めること。是正後の検証は「節の構成要素を 1 つずつ固定して数える」形（該当タスクでは 20 要素）へ書き換えた
- 2026-08-07: **本 repo の計画検証で再発**（ADR-0079 サイクル）。計画の検証コマンド 7 本を確定前に実行して実行可能性は担保した（Issue-0056 の対策を手動先行適用）が、それは「構文が通るか・現状値が期待の前提と合うか」の確認にとどまり、検出力は未検査だった。確定前レビューが**実装後状態を scratchpad にシミュレーション構築して写経実行**したところ、検証 1 の grep が節見出しを特定せずチェックリスト参照行にもヒットするため、**横断節の新設を丸ごと忘れてもチェックリスト 7 行さえ入れば合格する**ことが判明した（下限方式の期待値も同様に部分欠落を見逃す形だった）。対処として計画の期待値を全て等号に改め、節見出し・定型ブロック・参照行を分離検査する形へ修正した。**「検証コマンドの事前実行」は検出力を担保しない**点が、本課題の対策設計における具体的な要求として追加される
- 2026-08-16: **Issue-0092 の全数監査が本課題の型を判定規則に組み込み、検出力の実測を初めて体系的に取得**（ADR-0100 決定 2・ADR-0101）。比判定トラックの検査型 12 行に検出力実証を前置きした結果: 実証済み 9 / 未実証 3（B-01 = start-work Phase -1 依存検出・C-09 = 消化記録の欠落検査〈76 機会で検出 0・偽陽性 1 件のみ実測〉・F-09 = worklog id 重複検証）。未実証 3 行は比を判定に使わず保留とし「検出器が壊れている型」を退役誤判定から遮断。加えて検出力が**反証**された行 1（A-13 = 計画の期待値突合。捕捉失敗の実測 2 件 → Issue-0095 で再設計へ）。正本: `docs/records/audits/2026-08-16-guideline-process-audit/ledger.md` の検出力実証欄

## 結論

（open）
