# Issue-0056: 計画に書いた検証コマンドが「実行可能か」を確定前に誰も確かめていない

- **Status**: open
- **Opened**: 2026-08-07
- **起票元**: `retrospectives/flow/2026-08-07-rename-to-ai-driven-dev-guideline.md` 課題#1
- **関連**: ADR-0034 / Issue-0013（plan の検証ステップと編集内容の期待値突合）、`superpowers:writing-plans` の Self-Review、`pre-finalization-review`（実証を課す独立レビュー）、worklog `MakeAiInstructions-2026-08-07-02`

## 課題内容

ADR-0034 / Issue-0013 が定める plan の検証は「各検証ステップの**期待値**が、そのタスクの編集内容と矛盾しないか」という意味的な突合であり、「**検証コマンド自体が構文的に実行可能か**」は対象外になっている。計画の確定前にコマンドを一度も走らせない工程設計のため、実行不能なコマンドや、意図と違う対象を走査するコマンドが計画に残る。

振り分け判定: **構造観察型**（個々のコマンド誤りは delta として worklog に記録できるが、「確定前に実行可能性を検査する工程が無い」ことは体系の俯瞰で初めて見える欠落であり、delta の再発待ちでは捕捉できない）。

詳細（事象/原因/影響）は起票元の振り返りファイルが正。

## 検討状況

- 2026-08-07: 起票。改名サイクル（ADR-0078）で実測。`Select-String -Recurse`（存在しないパラメータ）を 7 箇所書いて全て実行時エラーになり、修正後もルート直下ファイルとディレクトリを混ぜて `Get-ChildItem -Recurse` へ渡したためファイル名がフィルタ解釈され、書き換え対象外のファイルを拾って偽陽性 2 件を出した。同セッション内で回収できたが、計画をそのまま別セッション・別エージェントが実行していれば「検証が全て落ちる計画」が渡っていた

## 結論

（open）
