# Issue-0107 検討経緯ログ

推奨と実選択の乖離事例の一次記録。形式: 日付・局面 / 状況の具体 / 規範側の推奨（由来） / 実選択 / 判断要因 / 結果。

- 2026-08-25: 【事例 1・フル巡の体数と照準】
  - 局面: feature/codex-support（Codex 対応）の spec 確定点 (b)。フル巡 1 巡目（3 観点 3 体）が Critical 5・Major 13・Minor 10 の計 28 件を検出し、全件採用で spec を全面改訂（10.7KB→19.1KB・差分 167 行）した直後の巡選択。
  - 状況の具体: 1 巡目の指摘の出どころを分析すると、(a) 既存インフラとの相互作用の見落とし（CLAUDE.md をファイル名参照する既存 48 箇所・上流要求 33 件・事実主張 40 件をレビュアーが全数走査して検出）と (b) 未実測の仮定（Codex の native 経路・@AGENTS.md 展開等）の 2 系統に集中していた。(a) は全数走査済みで再走査の限界効用が低く、(b) は補助実測 6 件で事実化済み。残るリスクは「28 件を 1 人で一括反映した AI 自身の転記ミス」と「改訂で新設された設計記述 4 箇所（誰のレビューも通っていない）」の 2 点に局在していた。
  - 規範推奨: フル巡（2 段型第 1 分岐「改訂が新規の規範文・設計・配線の追加を含む」・内容判定由来。反復のフル巡は体数 1〜3 に縮小可だが、既定の体制は 3 観点 3 体）。
  - 実選択: フル巡 1 体＋重点指定（前巡指摘 28 件の対応表検証＋新設設計記述 4 箇所の敵対的読解＋軽い全数走査）。
  - 判断要因: ボトルネックが「見る目の数」ではなく「まだ誰も読んでいない箇所に照準を向けること」だったため。同型 3 体の並列は同じ検出面の重複走査になる。
  - 結果: 対応表で反映ずれ 8 件（漏れ 0）、新設 4 箇所起点で新規 Major 7 件（build-dist 一般化の stale 判定矛盾・二段フォールバックのファイル有無判定の穴・分割コミット中間状態の二重読み込み等）を検出。照準どおりの場所から出た。コストは 3 体巡の約 1/3（約 14 万 vs 約 42 万トークン）。
- 2026-08-25: 【事例 2・設計縮小の一括推奨と候補の性質差】
  - 局面: フル巡 2 巡目の指摘 22 件を反映した直後。前置 1（指摘対応の改訂が機構の増設を 2 回連続で含む）が発火した。計数の実体: 1 巡目改訂で「version 一致検査」「build-dist の出力先一般化」「二段フォールバック」等を新設（1 回目）、2 巡目改訂でそれらへ「ホワイトリスト条件」「両モード冒頭指定」「バイト同一複写制約」を追加（2 回目）。
  - 状況の具体（縮小候補 2 件の中身）:
    - 候補 (i) version 一致検査: `.claude-plugin/plugin.json` と `.claude-plugin/marketplace.json` が同じ version 文字列（"0.1.11"）を二重保持しており、生成器 build-dist.ps1 は plugin.json しか読まず marketplace.json はどの検査にも掛からない。version bump 忘れ（ADR-0090 違反）が起きても「コマンドは正常終了するのに旧本文が配布され続ける」型で外形上検出されない——という**実在の欠陥**を、フル巡 1 巡目と 2 巡目のレビュアーが**独立に**指摘した。それを塞ぐために生成器へ入れた数行の比較検査で、維持コストはほぼゼロ。ドキュメント上も箇条書き 1 個。
    - 候補 (ii) 分割コミット機構: AGENTS.md 作成と CLAUDE.md ポインタ化を別コミットに分け、間に再実測を挟む工程。ADR-0110 の「設計確定前の実機確認」要求を実装時にも満たす手段として 1 巡目改訂で導入した。ところが 2 巡目レビューで「中間コミットでは AGENTS.md（3 ツール化済み）と CLAUDE.md（旧全文）が非同一内容で並び、Copilot CLI の重複除去（同一内容条件）が効かず二重読み込みが再現する」と指摘され、対策として「中間コミットの AGENTS.md はバイト同一複写とする」制約を追加した——**機構が自ら生んだ問題を追加制約で治療する構造**になっていた。しかも再実測（`codex debug prompt-input`・`claude -p`）に必要なのはディスク上のファイルでありコミットではないため、「1 コミット＋コミット前に作業ツリーで再実測」にすれば中間状態の管理ごと不要になる。
  - 規範推奨: 設計縮小を 5 択の推奨側に置く（前置 1 の既定由来。**候補ごとの性質は区別されず、直近 2 巡で増設された機構が一括で縮小の俎上に載る**）。
  - 実選択: (ii) のみ縮小、(i) は維持。
  - 判断要因: (i) は「実測で確認された欠陥を塞ぐ・維持コスト極小・削っても文書の複雑性は実質減らない」。(ii) は「機構の目的が機構なしで達成できる・追加制約は機構が無ければ不要・前置 1 が検出したい増設の悪循環の実物」。同じ「増設」でも、欠陥駆動の最小機構と自家治療型の過剰機構は縮小判定で真逆に評価されるべきだった。
  - 結果: (ii) の除去で制約 1 件・工程 1 件・関連記述（影響一覧 2 行）が消え spec は縮小。(i) は維持され、後続の差分確認巡でも問題指摘なし。
- 2026-08-25: 【事例 3・縮小適用後の締め方】
  - 局面: 縮小 (ii) を適用した直後の巡選択。
  - 状況の具体: 縮小はテキストを**削除する**改訂（分割コミット関連の記述除去＋1 コミット方式への置換のみ。spec は 22.7KB→22.5KB）で、新設テキストが無い。同時に反映した 2 巡目指摘 22 件の大半は列挙追加・数値差し替えの機械的調整。下流には plan 確定点（もう一度確定前レビューの提示機会がある）と実装時検証 10 項目が控え、残存リスクの主体（生成器の実装詳細）は実装の初回実行で騒がしく露見する型（`-Check` が落ちる等）。
  - 規範推奨: フル巡側（前置 1 の「設計縮小は設計骨格に触れる改訂であり、次巡の推奨は 2 段型によりフル巡側になる」という既定由来）。
  - 実選択: 差分確認巡 1 体（対応 23 項目の全数判定＋改訂前退避との diff 全行の意図外変更走査）。
  - 判断要因: フル巡の照準（未読の新設テキスト）が存在しない改訂に、フル巡は形が合わない。確認すべきは「22＋1 件が意図どおり反映されたか」「削除に矛盾残存がないか」で、これは対応表方式の守備範囲そのもの。
  - 結果: OK 22 / ずれ 1（軽微・数値の引用符込み計数）/ 漏れ 0・骨格新規指摘 0・意図外変更 0。軽微 3 件を機械修正し機械検証齟齬ゼロで実質収束（第 1 経路）。
- 2026-08-25: 【一般観察】現行規範が推奨判定に使うのは改訂の「型」（新規規範文・設計・配線の有無）と機械的計数（増設 2 回連続・フル巡 2 巡連続）のみで、次の状況要素を考慮しない: (a) 前巡指摘の出どころが枯れているか（同じ検出面の再走査の限界効用）、(b) 改訂が新設テキストを増やす方向か減らす方向か、(c) 下流の安全網（後続確定点・実装時検証）の有無、(d) 残存欠陥の露見特性（実行で騒がしく露見する型か、静かに潜伏する型か）、(e) 縮小候補の性質差（欠陥駆動の最小機構か、自家治療型の過剰機構か——事例 2）。本サイクルで状況適合的な推奨に到達したのは、いずれもユーザーが「規範を置いて分析せよ」と明示的に求めた場面のみ（2 回とも）。規範側の 3-2（由来明示・反対材料併記）は乖離の可視化には機能したが、推奨の向き自体は補正しなかった。
- 2026-08-25: 【事例 4・欠陥が改訂差分に局在した状態でのフル巡推奨】
  - 局面: 同じ feature/codex-support サイクルの **plan 確定点**（spec 確定点 (b) の後続）。フル巡 2 巡（各 3 観点 3 体・claude-opus-5）で指摘 31 件（Critical 1・Major 13・Minor 17）を全採用し、plan を 2 度改訂（+264/−31 行、+143/−54 行）した直後の巡選択。
  - 状況の具体: 2 巡の Critical はいずれも**AI が前巡の指摘に応じて書き足した検証コマンドが機能していない**型だった。第 1 巡の Critical は「網羅性 grep の検索語が、計画自身の正しい 3 ツール表記の部分文字列になっており、期待『出力が空』が原理的に成立しない」。第 2 巡の Critical は「その修正として除外フィルタを `check-claude-md-size` から `check-claude-md-size\.ps1` へ変えたが、`grep -rn` はパス接頭辞を出すため両形式の挙動は同一で、対象ファイル全行が盲点のまま残った」。いずれも書いた時点で 1 度実行していれば即座に露見する型。一方、成果物の元々の設計に対する Critical は 2 巡通して 0 件で、生成器の実装・生成物・期待値は延べ 6 体が写経実行して全件一致していた（`.agents/plugins/marketplace.json` と `dist/.codex-plugin/plugin.json` は計画記載の JSON とバイト一致）。残存リスクは「直近の改訂差分の作り込み」に局在していた。
  - 規範推奨: フル巡（2 段型第 1 分岐「改訂が新規の規範文・設計・配線の追加を含む」・内容判定由来）。
  - 実選択: 機械検証（アンカー実在 57 件・行番号引用 46 件・件数期待値 12 件を全数実測。齟齬ゼロ）＋差分確認巡 1 体。
  - 判断要因: 欠陥が「成果物全体」ではなく「直近の改訂」に局在しており、それは差分確認巡の設計そのもの。フル巡が再走査する全数対象は 6 体が三重に検証済みで限界効用が低い。ユーザーが「あなたはどう思いますか？フル巡が必要でしょうか？」と実態評価を求めたことが契機。
  - 結果: Critical 1・Major 2・Minor 5 を検出。Critical は「native / legacy の判別に `interface.displayName` を使ったが、この値は両経路が共有する `dist/` 側の生成物にも入るため判別力ゼロ」で、**またも前巡の AI 修正が原因**。差分確認巡がこの型を捕捉できることが実証された。コストは 3 体巡の約 1/3（約 25 万 vs 約 70 万トークン）。
- 2026-08-25: 【事例 5・改訂規模が単調減少する局面でのフル巡推奨】
  - 局面: 事例 4 の指摘 8 件を反映した直後（+49/−16 行・17 領域。当時の最小）。
  - 状況の具体: 3 巡連続で最重要指摘が「直前の AI 修正」由来という型が固定していた。改訂規模は +264 → +143 → +49 と単調減少。今回の改訂の中心は誤った判別法の**削除と置換**（`codex plugin list` が manifest 絶対パスを表示することを実機で確認し、spec 検証 3 の元の書き方がそのまま使えた＝回避策自体が不要だった）で、増設は限定的。ただし「同期対象を拡張したときに隣接ファイルを取りこぼす」型が 2 巡続いていた（第 2 巡: 生成器 spec 01/04/00、第 3 巡: `worklog-skill-pipeline/00-overview.md`）。
  - 規範推奨: フル巡（2 段型第 1 分岐）。
  - 実選択: 差分確認巡 1 体（改訂 17 領域の全数対応表つき・新規レビュアー）。
  - 判断要因: リスク型が「自分の修正の作り込み」に固定しており、それは差分確認巡の射程内。フル巡が全数走査する未検証対象がほぼ残っていない。ユーザーが再度「あなたはどう思いますか？差分確認巡で良いでしょうか？」と求めたことが契機（2 度目）。
  - 結果: Major 1・Minor 4。Major は「新設した目視 grep が配布物側で `(ADR-NNNN)` を探しているが、生成器が半角括弧を全角と同一視して識別子だけ除去するため、配布物に現れるのは `()` 残骸であり原理的に空振り」で、**3 巡連続で前巡の AI 修正が原因**。この巡で初めて「成果物の実体への新規の設計欠陥ゼロ」に到達した。
- 2026-08-25: 【事例 6・残存リスクが機械判定可能に落ちた局面でのフル巡推奨】
  - 局面: 事例 5 の指摘 5 件を反映した直後（+29/−11 行・9 領域）。
  - 状況の具体: 未検証で残る実質的な新規要素は grep 2 本のみで、その検出力は「意図した違反を注入して grep を走らせる」だけで白黒がつく＝**判断を挟まない機械判定に落ちていた**。実質収束（第 1 経路）の 2 条件のうち「実体への新規の設計欠陥ゼロ」は事例 5 で達成済みで、未達は「残指摘が機械検証可能な型のみ」の 1 点のみ。反対材料として「自分の修正を自分でテストする＝レビュアーの独立性が失われる」ことがあった。
  - 規範推奨: フル巡（2 段型第 1 分岐。改訂が配線の変更と工程の適用条件変更を含む）。
  - 実選択: 機械検証（注入テスト）。**ユーザーに求められる前に、AI が自発的に規範既定と自分の判断を分けて提示し、機械検証を推奨側に置いた**（事例 4・5 で 2 度求められたことによる行動変化）。
  - 判断要因: 残存リスクが機械判定可能な型に絞られたこと。独立性の喪失は反対材料欄に明示したうえでユーザー判断へ委ねた。
  - 結果: 正常系 2 本・注入 2 本とも期待どおり（半角括弧つき識別子はソース側 grep が、生成後の `()` 残骸と自己参照は配布物側 grep が検出。いずれも生成器は `Convention violations: 0` で通り抜ける）。対照テストで「全角括弧内に識別子以外の語が同居した型（R1-a）は両 grep とも検出せず、生成物は実際に `（の型）` と壊れる」ことも実測し、機械が見ていない境界を確定した。齟齬ゼロで改訂は発生せず、終了状態は `提示後確定（実質収束せず）`。
- 2026-08-25: 【事例 7・外部で実証された欠陥を塞ぐ増設を、受容で済ませなかった判断】
  - 局面: plan 確定点フル巡 1 巡目の指摘 15 件を反映するときの応じ方の選択。前置 1 の計数上はこれが「増設 1 回目」にあたる。
  - 状況の具体（何を増設するか / 受容案は何か）:
    - 指摘の中身: 敵対的レビュアーが、正本 `.claude-plugin/marketplace.json` の `plugins[].source` をオブジェクト形式（`{"source":"github","repo":"o/r"}`）にした場合、生成器の `ConvertTo-JsonStringValue` が `param([string]$Value)` で受けるため PowerShell の暗黙変換が働き、`"path": "@{source=github; repo=o/r}"` という**壊れた値を持つ構文的に妥当な JSON** が出力されると実証した（計画のコードを逐語転記した probe を写経実行。ケース D）。構文が妥当なので `-Check` も自己一致して `exit=0` を返し、Codex 側でのみ解決不能になる。同じ probe で `plugins` が空配列なら空マーケットプレイスを無警告出力（ケース A）、`plugins[0].name` 欠損なら `"name": ""` を無警告出力（ケース E）することも確認された。
    - 動機の実在性: 本計画自身が README（Task 6 Step 3）で GitHub 経由配布 `codex plugin marketplace add taika-izumi/ai-driven-dev-principles` を正式手順に掲げている。つまり正本の `source` を object 形式へ変える動機が**体系の内側に存在する**。仮定の入力ではない。
    - 受容案（増設しない選択肢）: spec と plan に「`source` は文字列形式のみ対応。object 形式へ変える場合は生成器の改修が必要」と 1 行書くだけにし、コードは足さない。
    - 増設案の中身: `New-CodexMarketplaceContent` に中断分岐 3 本（`plugins` 0 件 / `name` 空 / `source` が非文字列）＋診断 3 行、および Task 2 Step 13 の検証手順（異常入力 3 通りの実行）。
  - 規範推奨: 前置 1 は「増設 2 回連続」で発火するため、1 回目のこの局面では設計縮小は推奨側に載らない。ただし「修正の既定」（骨格安定時は削る/受容を既定とし増設を既定にしない）は、骨格安定でないため非適用。**規範はこの局面で増設の当否を何も言わない**。
  - 実選択: 増設。ただし「増設で応じた」旨を反復提示へ明示し、前置 1 の計数 1 回目であることも添えた。
  - 判断要因: 失敗様式が「静かに壊れて外形上検出されない」型で、`-Check` が緑を返し続ける。受容案が塞げるのは「正本 JSON を触る人がその文書を読んでいれば」という条件つきだが、生成器を触る人と正本を触る人は別のタイミングで動くため条件が成立しない。維持コストは分岐 3 本・診断 3 行で、削っても文書の複雑性は実質減らない。ADR-0054（検査は silent tolerance をせず、検出したら報告して停止する）と同型の判断。事例 2 の候補 (i)（version 一致検査）と同じ「欠陥駆動の最小機構」型。
  - 結果: 第 2〜4 巡を通じて**型ガードそのものへの指摘はゼロ**（関連指摘は「上流 spec の中断条件が version 不一致 1 本のままなので書き足せ」という記述の追随 Minor 1 件のみ）。第 5 巡の注入テストで 3 通りとも計画記載どおりの診断つき `exit=1` を確認。維持コストの増加は観測されなかった。
- 2026-08-25: 【事例 8・「削除が中心の巡」を増設回数に数えるかで前置 1 の計数が割れた 2 局面】
  - 局面: 第 2 巡末と第 3 巡末の 2 回、前置 1（設計縮小を推奨側に置くか）の計数が割れた。
  - 状況の具体（各局面で何が増え、何が減ったか）:
    - 第 2 巡末: 当該巡の改訂の**中心は削除**だった——Critical の解消が Task 11 Step 6 の除外フィルタの撤去（`grep -v` を丸ごと外す）で、規範文も 1 段落削られた。一方で周辺に配線が増えた: Task 9 へ spec 同期ステップ 2 つ（01/04/00 の同期、他 spec 3 ファイルの同期）を新設、Task 11 Step 2 に「出力形式が manifest パスを示さない場合」の判別分岐を追加。「増設 2 回連続（事例 7 ＋本巡）」と読むことも、「本巡は正味で減っている」と読むこともできた。
    - 第 3 巡末: 同じく**中心は削除と置換**だった——誤った判別法（`displayName` による判定）を丸ごと削り、`codex plugin list` が manifest 絶対パスを表示することを実機で確認した結果、spec 検証 3 の元の書き方がそのまま使えると分かって**回避策自体が不要になった**。一方で工程が 1 つ増えた: 配布物の目視を 1 段（Task 11 に一括）から 2 段（Task 4・8 のコミット前＋Task 11 の独立工程）へ。
    - 目視 2 段化の性質: これは新機構の導入ではなく**既存規範の充足**である。CONTRIBUTING「機械判定が届かない領域」は「目視は 1 回で終わらせず、生成後の配布物を読む工程を別に置くこと」と定めており、根拠として 2026-08-08 の実測（1 巡目で書式例の固有名 1 件を取り逃がし、自己参照が 3 度にわたり別の箇所で見つかった）を挙げている。私は当初この規範を「Task 11 へ一括する」根拠として引いていたが、レビュアーが原文を実読し「規範は逆に 1 回で済ませるなと要求しており、1 回に集約する根拠にならない」と指摘した。減らすと明文に反する。
  - 規範推奨: 前置 1 は候補ごとの性質を区別せず、直近 2 巡で増設された機構を一括で縮小の俎上に載せる（事例 2 と同じ構造）。ただし「増設」の定義（正味増か・既存規範の充足か・削除中心の巡を数えるか）が規範に無いため、発火の当否そのものが確定しなかった。
  - 実選択: 2 局面とも設計縮小を推奨側に置かず、**計数の当否が割れる旨を反復提示へそのまま書いてユーザーへ委ねた**（3-2 の由来明示の適用）。目視 2 段化は増設のまま維持。
  - 判断要因: 両局面とも「中心は削除、周辺で配線が増えた」構造で、前置 1 が捕まえたい「機構が自ら生んだ問題を追加制約で治療する」悪循環（事例 2 の候補 (ii)）とは形が違った。目視 2 段化は既存規範の充足であり、削ると明文違反になる。
  - 結果: 型ガード・目視 2 段化とも、以降の巡で**是非への指摘はゼロ**（目視 2 段化については、2 巡目が読む対象の書き方が「差分をもう一度」と弱く Task 2・3・5 の生成差分を覆わない、という Minor 1 件が出て修正した）。設計縮小を見送ったことによる肥大は観測されなかった。一方で、規範として機能したのは 3-2 の可視化部分だけで、前置 1 の計数規則自体は 2 局面とも判定を出せなかった。
- 2026-08-25: 【一般観察 2・巡選択の照準と、規範が見ていない 2 要素】plan 確定点の 4 巡すべてで、最重要指摘（Critical / Major）は**成果物の元設計ではなく、直前の巡で AI 自身が入れた修正**だった（4 巡 4 件）。共通原因は「検証コマンドを書いたが一度も実行しないまま提示へ回した」こと。この状況では照準は「全数」ではなく「直近改訂」にあり、実際に差分確認巡が 3 回とも当該欠陥を検出した。にもかかわらず現行の 2 段型は「改訂が新規の規範文・設計・配線を含むか」だけで向きを決めるため、4 巡すべてで一貫してフル巡を推奨し続けた。一般観察 1 の (a)〜(e) に加えて考慮されていない状況要素: **(f) 残存リスクが機械判定可能な型に落ちているか**（事例 6。落ちていれば巡を立てずに白黒がつく）、**(g) 直近の指摘の原因が成果物の元設計か AI の直近改訂か**（後者に固定しているなら照準は差分側）。あわせて行動面の観察: 3-2（由来明示・反対材料併記）だけでは推奨の向きは補正されなかったが、**「規範既定」と「AI 自身の判断」を分けて併記する提示形式**に変えたところ補正が働き、事例 6 ではユーザーの要求前に AI 側から自発的に適用された。提示形式の変更が規範本文の変更より先に効いた事例として記録する。
- 2026-08-29: 【事例 9・実装時の独立レビュー（確定前反復の外側の局面。実質収束と実装時レビューの補完関係の初実測）】
  - 局面: feature/start-work-responsibility-split（start-work 責務分割・ADR-0116）の実装完了直後の次手選択。確定前レビューは spec 確定点 (b)（フル 2 巡＋差分確認 1 巡＋機械検証 1 回）・plan 確定点（フル 1 巡＋差分確認 1 巡＋機械検証 1 回）とも**実質収束**で確定済み。実装は plan Task 0〜10 を検証 3 種（退避 diff・残存 grep・重複 grep）全通過で完遂し、サイクル全体整合検査（5 観点）も実施済み（指摘 2 件検出・修正: 41087d1）だった。
  - 状況の具体: 検証は plan の機械検証（grep 期待値 21 箇所の実測突合）と整合検査 5 観点で多重に掛かっており、AI 側の残存リスク認識は低かった。一方で対象はガイドライン配信元リポジトリの配布対象ソースであり、不備は導入先プロジェクトへ波及する。
  - 規範推奨: 完了処理（superpowers:finishing-a-development-branch）を推奨側に提示（start-work 次手ナビゲーション由来。実装時レビューを必須とする規範は無く、requesting-code-review は選択肢 2 として並記）。
  - 実選択: 独立実装レビュー（ユーザー選択。理由: 「ガイドラインの不備は他プロジェクトに波及する」「独立レビューのデータを取る」）。claude-opus-5・1 体・変更 32 ファイル全数走査＋plan 検証コマンドの独立再実行＋dist 破損の独自走査。
  - 判断要因: （ユーザー）成果物の波及性と、独立レビューの実測データ取得。
  - 結果: Critical 0・Important 1・Minor 5。Important 1（spec 完了基準 6 と ADR-0116 Consequences が、全数走査で注記対象外と確定済みの ADR-0067 を「判明分 8 件」と列挙したまま＝除外確定の書き戻し漏れ）は、**直前のサイクル全体整合検査（観点 3・5 実施済み）が見落とした型**で、独立レビューのみが検出した（delta 記録: worklog `MakeAiInstructions-2026-08-29-01`）。両確定点が実質収束していても実装時レビューで Important が出た＝停止判定の但し書き「実質収束は実装時の独立レビューを代替しない（補完関係）」の初の実測例。採用 3 件（Important 1・表記 2: 8319204 / 6cbfa7e）・plan 見落とし由来の Minor 1 件は次サイクル送り・観察 2 件は対応不要。コストは約 17 万トークン（3 観点 3 体フル巡の約 1/3 強）。無改変移設の中核（退避 diff・複写 grep）はレビュアーの独立再実行でも列挙外差分ゼロと立証され、「退避 → 機械抽出 → `git diff --no-index`」の移設検証手順が 21KB 規模で機能した実測も得た。
- 2026-08-25: 【一般観察 3・前置 1 の「増設」に定義が無い】前置 1 は「機構の増設（条件・分岐の追加）を 2 回連続」で発火するが、「増設」の数え方が定義されていない。事例 8 の 2 局面はいずれも当該巡の改訂が**正味では減っている**（規範文・機構の削除が中心）にもかかわらず、周辺の配線追加を拾えば「増設」と読めた。区別されていない軸: **(h) 当該巡の改訂が正味で増えたか減ったか**、**(i) 増設が既存規範の充足（減らすと明文違反）か新機構の導入か**、**(j) 増設が「機構が自ら生んだ問題の自家治療」型か「外部で実証された欠陥を塞ぐ最小機構」型か**（(j) は事例 2 の候補 (i)/(ii) と同じ軸で、2 サイクル連続で同じ区別が必要になった）。今サイクルでは 2 局面とも AI が計数の当否を提示へ書いて委ねる形になり、規範側は判定を出せなかった。
- 2026-08-30: 【事例 10・実証済みの穴を封鎖する増設 2 巡連続での前置 1 発火（LoopForAlpha 側事例 1・3 と同型の初の本リポジトリ実測）】
  - 局面: feature/review-records-reflux（Issue-0110 対策・ADR-0119）の spec 確定点 (c) 反復。フル 2 巡（各 4 観点 4 体・claude-opus-5）で通算約 96 指摘を反映し、2 巡とも改訂が機構の増設（検出層・実証要件・記録書式固定など）を含んだため前置 1 が発火した。
  - 状況の具体: 第 2 巡の指摘の相当部分（「2 層」の読み違え・命題すり替え・A 群 1 との衝突など）は第 1 巡の AI 改訂自身が持ち込んだ欠陥で、「増設した機構そのものに欠陥が入る」型が継続していた。増設はいずれも 4 観点が実証した穴（計数不能・基準の恣意化・循環不採用）の封鎖で、縮小はその再開放と等価。第 2 巡改訂には削減側修正 3 件（重複条項の解消）も含まれ、一般観察 3 の (h)〜(j) の軸では縮小推奨に馴染まない構成だった。
  - 規範推奨: 設計縮小を 5 択の推奨側に置く（前置 1 の既定由来。3-2 で既定由来である旨と内容判定の逆向きを併記して提示）。
  - 実選択: フル巡（ユーザーが「素の判断」を求め、AI の内容判定〈増設への欠陥混入が続く間はフル巡続行が正解 = LoopForAlpha#Issue-0109 事例 3 の教訓〉を採択）。
  - 判断要因: (1) AI 改訂自身の欠陥混入率が実測で高い（第 2 巡指摘の相当部分が第 1 巡改訂由来）、(2) 増設は実証済みの穴の封鎖で縮小は再開放と等価、(3) 骨格レベルの新規文が多く誰も読んでいない。
  - 結果: 第 3 巡フル巡（4 観点 4 体・新規レビュアー）は Critical 2・Major/重要級 20 超を検出。**いずれも第 1〜2 巡の AI 改訂が持ち込んだ配線・計数・書式の欠陥または細部未定義で、決定構造（型分類・採用基準・宣言・記録の骨格中核）への指摘は 0 件**。フル巡続行の選択は「増設への欠陥混入の継続」を確認する形で機能した（縮小を選んでいれば封鎖済みの穴の検証機会を失っていた）。同時に、指摘の型が「検証設計・配線の欠落」へ収束したことは LoopForAlpha 側事例 6（骨格指摘が全て検証設計の欠陥なら差分確認巡続行が妥当）の局面への移行を示す。コスト実測: 巡あたり 4 体 ≈58〜69 万トークン・通算 3 巡 ≈192 万トークン。その後の第 4 巡は内容判定（事例 6 型への移行）どおり差分確認巡（1 体 ≈9 万トークン。対応表 41/42 OK・新規指摘は細部 6 件）→ 機械検証 15 項目齟齬ゼロで実質収束（第 1 経路）。通算 4 巡＋機械検証 1 回 ≈201 万トークン
