> 出所: LoopForAlpha#Issue-0130 の全文コピー（2026-09-03 申し送り。実施方式は ADR-0118 に準拠）。本文中の Issue-NNNN / ADR-NNNN / worklog id / ファイルパスは LoopForAlpha 側を指す。原本: `docs/working/issues/flow/0130-guideline-vocabulary-uses-jargon-the-guideline-itself-forbids/0130-2026-09-03-session114-f4-implementation.md`（LoopForAlpha リポジトリ）

# Issue-0130 時点資料: 追加観測（2026-09-03 第114回。Task F4 の実装セッション）

- **記録日**: 2026-09-03
- **測定の機会**: 第114回（Task F4） の実装セッション
- **性質**: 書き切りの実測（作成後は原則不変。現在地の判断は課題ファイルが正）

走査対象 = `29264f9..HEAD` の追加行 **181 行**（コード・テスト 136 / 計画 31 / handoff 14）。

### (1) 🔴 `B群判定:` が 3 セッション連続で利用者の目に触れた

第111回・第112回に続く 3 例目。本回はレビュー委譲の直前に、利用者向けメッセージへ次の 1 行をそのまま出した。

```
B群判定: 検査=yes / ミューテーション=yes / 並列書き換え=yes / 計画実装=no / 長時間書き込み=no / 破壊的検証=yes
```

第112回に「2 セッション連続で利用者の目に触れた」と記録した形が、**記録後さらに 1 回再演した**。`subagent-dispatch` がこの書式を証跡として要求している以上、本文の語を言い換えても消えない（第113回 (1) の ⚠️ と同じ構造）。⚠️ **3 回とも「委譲の直前」という同一の発火点である** — 対策を打つなら語の言い換えではなく、この 1 行を利用者向け出力に含めるかどうかの判断点に置くのが直接的である。

### (2) 配信物由来の語の分量を文字単位で測ると、経路の比重が第113回の結論より重い

第113回 (1) は「配信物由来 → 計画の逸脱記録・完了記録」という経路を行数で示した。本回を**文字数**で測ると、その経路の比重はさらに大きい（測定単位の詳細は Issue-0131 の第114回 (1)）。

| 単位 | コード・テスト | 文書（計画・handoff） |
|---|---|---|
| 行 | 136（75%） | 45（25%） |
| **文字** | 12,819（31%） | **27,288（69%）** |

本回の文書側 45 行に含まれる配信物由来語 = `逸脱記録` 26 / `委譲` 18 / `採用基準` 4 / `検出資産` 3 / `統合採用` 3 / `確定点` 2 / `機械的な適応` 2 / `前提が偽` 2 / `射程外` 1。**計画への追加 31 行のうち 9 行が行頭 `逸脱記録:` で、1 行あたり平均 606 字**である。

### (3) 記号的ラベルの再演が 4 例目 — `(v8)` を対話で裸の主語に使った

`例外 c` / `フェーズ2型` / `採用基準 (i)` に続く 4 例目。`(v8)` は仕様06 §3.4 が定める由来照合 (v1)〜(v8) のラベルで、**文書内では節番号で引けるが、対話では引けない**。本セッションの利用者向けメッセージには「**(v8) が読まれる位置**」「(v8) の配線」のように、説明を添えずに主語・目的語として出た（追加行にも 10 回）。

⚠️ これまでの 3 例（`例外 c` / `フェーズ2型` / `採用基準 (i)`）はいずれも**配信ガイドラインまたは計画が定めたラベル**だったが、本例は**仕様書が定めたラベル**である。記号的ラベルの供給源が 3 系統（配信物・計画・仕様）あることになり、対策を配信物側だけに打っても残る。

### (4) 委譲プロンプトへ規範語をそのまま書いた

レビュー委譲プロンプト 2 通に、配信物由来の語を意図的に書いた（`A 群` の 4 制約・`機械的な適応` / `事実誤り・期待値の陳腐化の訂正` / `設計の変更` / `計画の射程外の既存欠陥` の 4 型分類を型名のまま提示）。**委譲先の報告 2 通は同じ型名で返り、その報告を計画の完了記録へ引用した**ため、語彙が恒久文書へ定着した。

→ 型分類は委譲先に採否を決めさせないための共通語彙なので**言い換えると機能を失う**が、この経路が語彙を増幅していること自体は記録に値する（Issue-0131 の第114回 (4) と対）。⚠️ **委譲プロンプトは git に残らないため、追加行の走査ではこの経路の入口が見えない。**

