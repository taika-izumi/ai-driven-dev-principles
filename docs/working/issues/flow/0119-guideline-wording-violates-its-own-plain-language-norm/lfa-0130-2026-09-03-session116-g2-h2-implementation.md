> 出所: LoopForAlpha#Issue-0130 の全文コピー（2026-09-03 申し送り。実施方式は ADR-0118 に準拠）。本文中の Issue-NNNN / ADR-NNNN / worklog id / ファイルパスは LoopForAlpha 側を指す。原本: `docs/working/issues/flow/0130-guideline-vocabulary-uses-jargon-the-guideline-itself-forbids/0130-2026-09-03-session116-g2-h2-implementation.md`（LoopForAlpha リポジトリ）

# Issue-0130 時点資料: 追加観測（2026-09-03 第116回。Task G2〜H2 のセッション）

- **記録日**: 2026-09-03
- **測定の機会**: 第116回（Task G2 = 4b の実走 → G3 の一部 → G4・G5 の実測 → H1 の監査 → H2 の文書追随）
- **性質**: 書き切りの実測（作成後は原則不変。現在地の判断は課題ファイルが正）

これまでの 5 回はいずれも**実装タスク**のセッションだった。第116回は**実走・実測・監査・文書追随**が中心で、
コードへの追加が 800 字（17 行）しかない。**タスクの型が違うセッションで初めて測った回**である。

測定の母数は本セッションのコミット範囲（`4efa0ac..HEAD`）の**追加行のうち散文のみ**（4b の産物 = JSON 6 万行は除外）。
合計 **56,801 字**。語の一覧は本課題の第111〜115回の時点資料が挙げたものを用いた。

## (1) 🔴 `B群判定:` は 0 件 — 4 セッション連続の記録が止まり、発火点が特定できた

| 語・書式 | 第112〜115回 | 第116回 |
|---|---|---|
| `B群判定:` | 4 セッション連続で利用者の目に触れた | **0 件** |
| `A 群` / `B 群` | 各回で出現 | **0 件** |

**理由は明確である**: 本セッションは**サブエージェント委譲を 1 件も行わなかった**（実走・実測・監査・文書追随はいずれも
委譲を伴わない）。過去 4 回の発火点はすべて「委譲の直前」であり、その仮説がここで否定されずに残った。

🔴 **これは第115回の「新しい書き込み先へは 1 語も入らなかった」より強い負の対照である** — あちらは
「特定の書き込み先に入らなかった」だが、こちらは**セッション全体で供給が 0 になった**。
対策を考えるときの含意は「`A 群` / `B 群` の改名は委譲スキルの 1 箇所を直せば供給が止まる」ことである。

## (2) 伝播の主経路はタスクの型で入れ替わる — 第112回の結論の射程

配信物由来語 **147 件 / 56,801 字（2.59 件/千字）**。着地先の内訳:

| 着地先 | 追加字数 | 語の件数 | 密度（件/千字） | 上位の語 |
|---|---|---|---|---|
| 計画（`docs/working/plans/`） | 16,891 | **58** | 3.43 | 逸脱記録14 / 突合10 / 発火9 / 昇格6 |
| ハンドオフ | 6,211 | **33** | 5.31 | 正本6 / 昇格4 / 突合3 |
| 仕様（`docs/current/specs/`） | 17,893 | **29** | 1.62 | 突合6 / ラッパー5 / 正本5 |
| 監査記録（`docs/records/audits/`） | 8,881 | **18** | 2.03 | 逸脱記録7 / 母数4 / 正本2 |
| `loop/README.md` | 6,125 | **9** | 1.47 | ラッパー3 / 母数2 / 突合2 |
| コード・テスト | 800 | **0** | 0.00 | — |

🔴 **第112回は「伝播の主経路はコード内コメントである（対話ではない）」と結論した**。本回はコードをほとんど触らない
セッションであり、**コード経路は 0 件**だった。つまりあの結論は誤りではなく**タスクの型に条件づけられている**。
密度で見ると最も高いのは**ハンドオフ（5.31/千字）**であり、これは過去 4 回では上位に出ていなかった。

## (3) 新しい着地先が 2 つ増えた — うち 1 つは人間の唯一の手順書

- **監査記録**（`docs/records/audits/2026-09-03-stage7-part2-audit.md`。本回で新設）: 18 件。
  読み手は将来の監査者（人間）である
- 🔴 **`loop/README.md`**: 9 件。**README は AI 読み取り拒否側に置かれた「人間の唯一の手順書」**であり
  （仕様04 §6）、ここに配信物由来語が入ると、読み手は語の出所（プラグインのスキル）へ辿る手段を持たない。
  本回は「フロー B」節と「フロー A (4b)」節を新設した際に `ラッパー`（3）・`母数`（2）・`突合`（2）・
  `射程`（1）・`素の`（1）が入った

## (4) 記号的ラベルの供給源は増えていない

第113回に `採用基準 (i)`、第114回に `(v8)` が加わって 2 系統になったが、**第116回で新しい系統は増えなかった**。
本回に出た記号的ラベルは `(u13)〜(u18)`（監査チェックリストの項目番号）と `(v8)` の再演で、
前者は**本プロジェクトの既存の採番規約**であり配信物由来ではない（Issue-0131 の表 C 側）。

## この回で確認できたこと（対策検討への含意）

1. `A 群` / `B 群` / `B群判定:` の供給は**委譲スキルの 1 箇所に集中**している（本回の 0 件が示す）
2. 「主経路はコード内コメント」は**実装タスクに限った話**である。文書中心のセッションでは計画とハンドオフが主経路になる
3. **README への流入は本回が初出**であり、置き換えの優先度を考えるときは「読み手が語の出所へ辿れるか」で
   重みを変えるべきである（README の読み手は辿れない）
