# 隔離検証の共通起動処理の設計レビュー

- 対象: `docs/current/specs/2026-09-09-isolated-verification/` の4ファイル、ADR-0145〜0147。
- 初回の予定: 1担当4観点兼務。2026-09-09、ユーザーが独立レビューの実施を選択した。
- 状態: 初回の4観点の静的レビューと、新規1担当による差分再確認を実施。軽微1件の反映と機械確認後、2026-09-09にユーザーが採否案を含む仕様の確定を選択。動的検証を含む厳密なフル実施の完了とは扱わない。

## 起動と送信範囲

作成側とは異なるモデルとしてClaudeのsonnetを指定し、Claude Code 2.1.263の`--restricted --safe-mode --tools Read`、空のstrict MCP構成、フック無効化、`dontAsk`、承認プロンプトなし、セッション非保存で起動を準備した。書き込み・コマンド実行を持たない読取専用の文書照合として4観点を依頼し、動的試験は未確認事項として報告させる。厳密な写経実行を含むフルレビューの完了とは区別する。

送信対象は仕様4件、関連ADR4件、規範2件、既存の固定検査接続のコード・説明2件、過去の実測に関する参照知識1件、Issue-0136の本文1件、計14ファイル（連結後112,969バイト）。補助資料はCodex CLIのヘルプ、版、仕様のGit列挙、未実装の確認結果。認証ファイルやユーザー設定の内容は含めない。原文・元の行番号付きの複製を渡し、任意のユーザーファイルを読む指示は出していない。

通常の制限下での起動ではログが実行中にファイルへ反映されず、当該レビューのPIDと実行ファイルを照合してそのプロセスだけを停止した。停止後に保存されたログを読み直すと、開始イベント1件（モデル`claude-sonnet-5`・ツールReadのみ）とAPI再試行9件があり、ツール呼出・最終結果は0件だった。開始イベントが無かったのではなく、途中のログ確認方法に不足があった。通常ユーザー側で同じRead限定起動を行う申請は、自動承認審査に「レビューの承認だけでは資料をClaude外部サービスへ送信する明示承認が不足する」と拒否された。別経路による送信は行わず、明示承認を得てから再開する。

## 委譲元が先に確認した事実

原本配下に新しく作った空の作業フォルダで`git rev-parse --show-toplevel`を実行すると、原本リポジトリが返った。仕様は`.git`を複製せず、実行領域を原本配下に置くことを許しているため、独立したGit作業領域として扱う方法の確認が必要。これは書き込み制限の突破を実証したものではない。

実出力: `.tmp/isolated-verification-review-20260909-01/git-probe-result.json`。独立レビュアーの指摘ではなく、委譲元の再現結果として渡す。

独立した空のGit管理領域を持つコピーについても対照を取り、Gitの最上位ディレクトリがコピー自身となることを確認した。実出力は`git-independent-probe-result.json`。原本のGit管理領域は変更していない。

## 送信承認後の実施結果

2026-09-09、ユーザーが「この資料のClaudeへの送信を許可し、レビューを続行する」を選択した。承認済みの資料14件とCLI確認結果を、通常ユーザー側の新しい試行名`host-*`で実行した。既存のログは保持した。

開始イベントのモデルは`claude-sonnet-5`、ツールはReadのみ、MCPは0件。ツール呼出は資料の読取3件で、コマンド実行・書き込み・再委譲は無い。仕様4件・参照資料10件の確認結果、指摘6件、機構の判定8件、動的確認候補5件を返した。初回は1担当4観点兼務。CLIの表示費用は0.4888704米ドルで、実請求額や契約枠消費の実測とは区別する。

出力原文は`.tmp/isolated-verification-review-20260909-01/reviewer-result.txt`、解析済みJSONは同フォルダの`reviewer-result.json`、起動・ツール・費用の集計は`review-summary.json`。改訂前の対象14ファイルは起動前後のハッシュ一致を確認した。

レビューの限界: 実行コードは未実装であり、レビュアーはCLIの写経実行をしていない。機構の判定は8項目で、仕様に含まれる個別条件すべての独立した列挙まで確認できたとは言わない。スキーマとREADMEは新規実装予定の名前であり、本文が無いこと自体は欠陥ではない。以下は独立指摘を委譲元が前提検査した採否案で、採否は反復提示への回答で確定する。

## 指摘の前提検査と反映案

| 指摘 | 守る対象・実体での確認 | 採否案と修正 | 分類 |
|---|---|---|---|
| R1: sandbox・add-dirフラグ欠落（原報Critical） | work/tempだけへの書き込み。既存`limited-execution/server.ps1`の8行目は`permissions.inspection`を生成し、`-c`で適用する。仕様も`-c`を列挙していた | 特定のフラグが必須との断定は不採用。プロファイルと選択キー、許可先、controlと.gitの読取限定を02へ具体化。組合せの実証済みという誤読も除去 | 周辺の具体化。特定フラグ欠落による保護不成立は未実証 |
| R2: Git起動条件（原報Critical） | コピーの対象を取り違えないこと。空のコピーが親の原本を認識する対照と、独立Git管理領域でコピーを認識する対照を委譲元が実行 | 採用。ただし常に非Gitで起動拒否されるという断定ではなく、独立したGit管理領域を01へ追加する案。単なるskipフラグでは親への探索を止めない。ADR-0148 | 設計骨格 |
| R3: 検査とイベントの対応（Major） | 自己申告だけでの合格を避ける。既存`2026-09-08-codex-subagent-inheritance.json`のpersistent実行に`item.completed/command_execution`とid・command・exit_code・aggregated_outputが実在 | 対応するイベントとフィールドの具体化を採用。eventsPathはJSONL全体であり、単一パスだから終了イベントしか保存できないとの理由は不採用。02・03へ保存範囲と照合条件を記載 | 設計骨格の詳細化 |
| R4: 通信禁止設定（Major） | テストコマンドの通信禁止。既存server.ps1の8行目に`network={enabled=false}`が実在 | 明示値の記載を採用しR1と同じプロファイルへ統合。設定機構そのものが欠落しているとの断定は不採用。別経路非公開の既存要求は維持 | 周辺の具体化 |
| R5: sourceRootの再解析ポイント（Minor） | 原本外への参照を拒否。01の「入力・出力・それらの祖先」はsourceRootを含む。sourceRoot内の入力の祖先でもある | 不採用。既存条項とV1の再解析ポイント検査で覆う。文言や新工程を足さない | 周辺 |
| R6: 追加起動停止の範囲（Minor） | 時間超過後に制御不能のプロセスを残さない。並列ジョブ管理・常設制御はスコープ外 | 採用。当該CLI呼出の自動再起動を止め、停止未確認を主担当へ報告すると02へ明記。恒久的な全ジョブ停止機構は追加しない | 周辺 |

機構判定の「Claudeの許可リスト方式が重複」という指摘は、同じ文中で「実質的な重複ではない」とされ、上流ADR-0146も別経路として既存規範を維持しているため不採用。Codexの隔離機構へClaude固有の規範を移植しない。

R2の追加は既存Gitの初期化と確認だけで、原本の履歴・設定・フックをコピーしない。注意書きや非Git許可フラグだけでは再現した探索経路を閉じられない。新しい依存・常設管理を増やさず、準備時のGit起動と空の管理領域を維持する負担を持つ。R3は既存の結果照合要求を実際のJSONLへ対応付け、独立した監視・評価基盤を作らない。

## 残る実行確認と次の確認案

- 既存プロファイル機構を今回の`codex exec`構成へ適用し、work/tempの成功とcontrol・原本・.gitへの拒否を確認する必要がある。引数や既存別構成の成功だけでは完了にしない。
- コマンドの通信拒否は、到達不能ホストへの接続失敗だけを証拠にしない。対照の成功とポリシーによる拒否を区別する試験が必要。
- コピー内で新規作成されたリンク経由の操作は、受付時の既存リンク検査とは別である。実行制限が届くことをV4の試験で確認する。新しい許可範囲へ拡張しない。
- JSONL照合は保存済み実測で構造を確認したが、任意のテスト内容の正しさを機械的に保証しない。今回のexec出力との対応と一意に照合できない場合のincompleteを実装時に検査する。
- 反映は仕様4ファイル、ADR-0148と索引。改訂前退避を保持。独立再確認は未実施で、骨格安定・実質的な収束とは判定しない。
- 次は修正箇所とR1〜R6の対応、上流の完了基準V2・V4・V5を新規1担当が差分再確認する案を推奨する。規範改定型。既存の静的レビューとは別に、動的検証の残りを明示して引き継ぐ。再確認では更新仕様・ADR-0148・本対応表・Git対照結果を資料へ含める。

## 差分再確認（2回目）

2026-09-09、ユーザーが「更新仕様・ADR・対応表・Git試験結果を読み取り専用Claudeへ送信する差分再確認」を選択した。新規の`claude-sonnet-5`をRead限定、MCPなしで起動し、現在の仕様4件・ADR4件・対応表1件、改訂前仕様と補助証拠を渡した。ツール呼出は当該資料のRead6件のみ。起動前後に対象9ファイルのハッシュ一致を確認した。

結果はR1〜R6の採否対応と修正文言が全件一致。R1・R3・R4は実機確認待ちという意味でpartially_resolved、R2・R6はresolved、R5はnot_applicableとされた。新規指摘D1は周辺のMinor1件で、設計骨格の新規指摘は0件。CLI表示費用0.4663286米ドル、実行時間約229秒。初回と合わせた表示費用は約0.96米ドル（実請求額ではない）。

| 指摘 | 守るもの・根拠の照合 | 採否と反映 |
|---|---|---|
| D1: Git管理領域の読取制限を行う主体が曖昧 | 01の準備処理に「読み取り専用にする」とあり、実際の指定先は02の`workRoot/.git=read`。01自身の別機構は定義していない | 採用。01の1行を、02の実行ブロックが起動時の権限プロファイルで行うという参照へ置換。新しい機構は追加しない |

D1の反映前に現在版9ファイルを`C:/Users/d12an/.codex/visualizations/2026/09/08/01a081e7-2e3b-7481-a2e5-dfa949de9edc/isolated-verification-review-20260909-02/`へ退避した。反映後は仕様の変更が01の1ファイルだけであること、02への参照と`.git`読取指定が一致すること、`git diff --check`が通ることを機械確認した。

差分再確認が残したV2の「原本外配置」は、委譲元が追加の無害な試験で確認した。原本配下・原本外の両方で、Gitの最上位ディレクトリと管理領域の絶対パスがコピー内へ一致した。試験結果は`.tmp/isolated-verification-review-20260909-02/git-inside-outside-result.json`。この結果を権限制限の成功とは扱わない。

実行方式に関する留保は「採用する新構成での確認が未完」として引き継ぐ。過去の記録の`persistent`というラベルだけから、`exec`と異なるイベント形式だとは断定しない。イベントの構造は既存実測で確認済みだが、採用構成全体の実効適用は未確認である。

静的な仕様検討については、設計骨格の新規指摘0件、残ったD1も参照の明記と機械確認で解消しており、実質的な収束に相当する。ただし厳密な動的実証付きフル実施が完了したとは扱わない。残る実行確認を実装計画へ引き継いで仕様を確定する案を推奨する。骨格安定の目安であるフル実施2回の実績はない。

差分再確認の原文・解析済みJSON・開始イベントは`.tmp/isolated-verification-review-20260909-02/`に保存。再確認後の追加AIレビューは行っていない。

## 現在の再開点

2026-09-09、ユーザーが「仕様を確定し、実装計画へ進む」を「1で」と選択した。R1〜R6・D1の採否と現行仕様を確定し、ADR-0145〜0148を設計承認時のAcceptedへ昇格する。実装前のためサイクル全体整合検査は非該当で、実装完了時に実施する。

初回の静的4観点確認は動的実証付きフル実施の完了に算入しない。handoffの`review=`は完了した差分再確認1回と機械確認1回を記載し、初回の限定された確認の経緯は本書で保持する。確定主体はユーザー、終了状態は提示後確定。原本配下と原本外のGit試験は完了したが、新しいexec構成の書き込み制限・通信拒否・実行中リンク・終了管理・両依頼元からの一連の実行は未検証で、実装計画へ引き継ぐ。

- 依頼・複製・CLIヘルプ・実行ログ: `.tmp/isolated-verification-review-20260909-01/`。
- 改訂前の原文14件: `C:/Users/d12an/.codex/visualizations/2026/09/08/01a081e7-2e3b-7481-a2e5-dfa949de9edc/isolated-verification-review-20260909-01/`。
- 対象14ファイルの起動前後の一致を確認してから修正案を反映した。変更前後の比較には外部の退避ファイルを使う。
- ユーザーの反復方法・採否の回答から続ける。レビュー実施と最初の資料送信は承認済みだが、今回の修正を独立に確認した結果はまだ無い。
