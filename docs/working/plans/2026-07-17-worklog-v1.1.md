# worklog v1.1 改訂 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ADR-0048〜0053 の設計決定（スキーマ v2: model 必須・v 必須・friction string[] / id 採番強化 / 運用ガイド 3 点 / 記録単位規範）を worklog パイプラインのスキル doc・spec・Issue 群へ反映する。

**Architecture:** スキーマの正典は `skills/worklog-record/references/store-format.md`（ここを先に v2 化）。スキル doc（worklog-record / worklog-extract）と spec（01/02）は正典に整合するよう書き換えで更新する（差分ファイルは作らない）。既存ストアデータ（v1 行 1 件）は書き換えず、読み側規約「v なし = v1」で互換を保つ。

**Tech Stack:** Markdown ドキュメントのみ（実行コードなし）。検証は grep / Read による実体確認と、プラグイン更新後のスモークテスト（v2 エントリの実記録）。

**前提知識:**

- 本リポジトリはメタ・ガイドライン（スキル定義・spec・ADR）が「プロダクト」であり、実装 = ドキュメント編集
- `skills/` を編集してもプラグインに反映されるのはユーザーが Claude Code 上で `/plugin marketplace update ai-driven-dev-principles` を実行した後（AI からは実行不可）
- 中央ストア実体: `$HOME/.ai-dev-worklog/`（`MakeAiInstructions/log.jsonl` に v1 エントリが 1 件。**書き換え禁止**）
- コミットメッセージのマルチライン渡しは `git commit -F <file>`（Bash=POSIX sh。PowerShell here-string 不可）。1 行メッセージなら `-m` でよい

---

### Task 1: store-format.md を v2 へ改訂（スキーマ正典）

**Files:**
- Modify: `skills/worklog-record/references/store-format.md`

- [ ] **Step 1: エントリスキーマ節を差し替え**

「## エントリ（log.jsonl、1行1エントリ、追記専用）」節の冒頭 3 行（必須/delta ペア/任意の列挙）とフィールド表、および「### id 採番アルゴリズム」節全体を、以下の内容へ書き換える:

```markdown
## エントリ（log.jsonl、1行1エントリ、追記専用）

必須（いずれか空なら記録を弾く）: `v` / `id` / `date` / `project` / `model` / `scope` / `title` / `context` / `procedure`
delta ペア（`friction` または `corrections` の少なくとも一方が必須）: `friction` / `corrections`
任意: `skillification_hint` / `outcome`（success/partial/failed＝作業結果）/ `tools` / `applied_rules`（逸脱注記可）/ `refs`

| フィールド | 型 | 説明 |
|---|---|---|
| `v` | number | スキーマ版数。現行は `2`（ADR-0049） |
| `id` | string | `<project>-<date>-<NN>`。台帳との突合キー |
| `date` | string | YYYY-MM-DD |
| `project` | string | 出所プロジェクト名（連結後も行内で出所が分かる） |
| `model` | string | 記録時の AI モデル ID（例 `"claude-fable-5"`）。モデル固有か全モデル共通かの判別材料（ADR-0048） |
| `scope` | enum | `project-specific` / `general-candidate`（record 時は暫定） |
| `title` | string | 動詞句15文字程度 |
| `context` | string | 何をしていてなぜ発生したか |
| `procedure` | string[] | 実際に踏んだ手順 |
| `friction` | string[] | 躓き型 delta。複数の躓きは要素を分ける（ADR-0051） |
| `corrections` | string[] | 注入型 delta（人間の指示・修正を発言に近い形で） |

### スキーマ版数と互換読み（ADR-0049 / ADR-0051）

- 書き込み側は常に現行版数 `"v":2` を必ず書く（台帳レコードも同様）
- 読み側規約: **`v` フィールドなしの行 = v1（初版スキーマ）と解釈する**。v1 行は `model` なしを許容し、`friction`（string）は 1 要素配列として読み替える
- 既存の v1 行は書き換えない（追記専用）
- 注: サイクル名「worklog v1.1」はパイプライン全体の改訂名であり、スキーマ版数 `v`（現行 2）とは別物

### 記録単位（ADR-0053）

1 エントリ = **同一 `context`（作業テーマ）を共有する delta の束**。

- 同一作業内の複数の躓きは `friction` の要素として列挙する
- 節目に独立した作業テーマの delta が複数あれば、テーマごとに複数エントリを記録してよい
- 「どれを捨てるか」の優先順位判断はしない（ノイズ抑制は記録ゲート＝delta の存在が担う）

### id 採番アルゴリズム（ADR-0050 で強化）

1. `date` = 今日（YYYY-MM-DD）、`project` = フォルダ名
2. **追記の直前に** `<folderName>/log.jsonl` を読む（無ければ NN=01）
3. 同一 `project` かつ同一 `date` のエントリ数を数え、その数+1 を2桁ゼロ埋めして NN とする
4. `id = "<project>-<date>-<NN>"` で 1 行追記する
5. **追記後に log.jsonl を読み直し、自行の id が他エントリと重複していないか検証する**（ADR-0038 の読み直し規範と整合）。重複を検出した場合、**自分が書いた行のみ id を再採番して書き直す**（追記専用原則の唯一の例外。台帳突合レコードが発生する前・自分が書いた直後の行の修復に限るため安全）

> 注: ADR-0045 / spec 01 の旧記述「末尾を見て採番」の改善版（ADR-0050）。並行セッションの採番競合（Issue-0027）に対し、直前再カウントで競合窓を縮め、読み直し検証で検出・回復する。
```

- [ ] **Step 2: 台帳スキーマ節へ v を追加**

「## 台帳レコード（processed.jsonl、1行1レコード、追記専用）」節の例と表を以下へ書き換える（`v` 列の追加と例への `"v":2` 付与。他フィールドは変更なし）:

```markdown
```jsonl
{"v":2,"id":"MakeAiInstructions-2026-07-16-01","outcome":"adopted","date":"2026-07-16"}
{"v":2,"id":"MakeAiInstructions-2026-07-16-01","outcome":"skillified","ref":"skills/xxx","date":"2026-07-20"}
{"v":2,"id":"LoopForAlpha-2026-07-18-03","outcome":"deferred","evidence_count":2,"date":"2026-07-20"}
```

| フィールド | 型 | 説明 |
|---|---|---|
| `v` | number | スキーマ版数。現行は `2`。`v` なしの行は v1 と解釈する（ADR-0049） |
| `id` | string | 対象エントリまたはクラスタ代表の id |
| `outcome` | enum | `adopted` / `skillified` / `rejected` / `merged` / `deferred`（**採否結果**。エントリ側 outcome とは別物。状態遷移: `adopted` → `skillified` または `merged`） |
| `evidence_count` | number | `deferred` のときのみ。保留時点のクラスタ根拠数 |
| `ref` | string | 任意。skillified/merged 時の作成先 |
| `date` | string | 台帳追記日 |
```

- [ ] **Step 3: 実体確認**

Run: `grep -c '"v":2' skills/worklog-record/references/store-format.md`
Expected: 4 以上（台帳例 3 行＋互換読み節の記述）

Run: `grep -n 'model\|string\[\]' skills/worklog-record/references/store-format.md | head -20`
Expected: `model` 行と `friction` の `string[]` 行が表に存在

- [ ] **Step 4: コミット**

```bash
git add skills/worklog-record/references/store-format.md
git commit -m "feat(worklog): store-format をスキーマ v2 へ改訂（ADR-0048/0049/0050/0051/0053）"
```

---

### Task 2: worklog-record SKILL.md の更新

**Files:**
- Modify: `skills/worklog-record/SKILL.md`

- [ ] **Step 1: 手順節の更新**

「## 手順」の 2（delta 抽出）・4（id 採番）・6（エントリ構築と検証）を以下へ書き換える:

```markdown
2. **delta 抽出**:
   - `friction`（string[]）: 躓き型 = エラー・手戻り・非自明な試行錯誤を要素ごとに1〜2行で。複数の躓きは要素を分ける。損失が大きかった場合は規模感（手戻り回数・時間ロス等）を本文に含めてよい（ADR-0052）
   - `corrections`（string[]）: 注入型 = 人間が注入した指示・修正を発言に近い形で1〜2行ずつ。**AI の当初挙動（指示がなければ何をしようとしていたか）が自明でなければ `context` に 1 行添える**（ADR-0052）
```

```markdown
4. **id 採番**（`references/store-format.md` の採番アルゴリズム。ADR-0050）:
   - **追記直前に** log.jsonl を読み直し、同一 `project` かつ同一 `date` のエントリ数 + 1 を2桁ゼロ埋めして NN
   - `id = "<project>-<date>-<NN>"`
```

```markdown
6. **エントリ構築と検証**:
   - 必須フィールド（`v`＝現行 `2` / `id` / `date` / `project` / `model`＝記録時の AI モデル ID / `scope` / `title` / `context` / `procedure`）を埋める
   - delta 必須（`friction` または `corrections` の少なくとも一方）を検証
   - 任意フィールド（`skillification_hint` / `outcome` / `tools` / `applied_rules` / `refs`）は関連あれば付ける
   - `<folderName>/log.jsonl` へ1行 append
   - **追記後に log.jsonl を読み直し、自行の id 重複がないか検証する**。重複時は自行のみ再採番して書き直す（ADR-0050）
```

- [ ] **Step 2: 記録単位規範の追加**

「## 記録ゲート判定」節の直後に以下の節を新設する:

```markdown
## 記録単位（ADR-0053）

1 エントリ = **同一 `context`（作業テーマ）を共有する delta の束**。同一作業内の複数の躓きは `friction` の要素として列挙し、節目に独立した作業テーマの delta が複数あれば、テーマごとに複数エントリを記録してよい。「どれを捨てるか」の優先順位判断はしない（ノイズ抑制は記録ゲートが担う）。
```

- [ ] **Step 3: エントリ例の v2 化**

「## エントリ例」の JSONL を以下へ書き換える:

```jsonl
{"v":2,"id":"MakeAiInstructions-2026-07-17-01","date":"2026-07-17","project":"MakeAiInstructions","model":"claude-fable-5","scope":"general-candidate","title":"AskUserQuestion 前に環境変数を確認","context":"start-work Phase 1 で構造化質問ツールを使う前に、ADR-0036 の運用チェックを実行","procedure":["Bash: echo $CLAUDE_CODE_DISABLE_MOUSE_CLICKS","値が 1 であることを確認してから AskUserQuestion 発行"],"corrections":["構造化質問ツール使用前に環境変数の値を確認すること"],"applied_rules":["ADR-0036"]}
```

- [ ] **Step 4: 関連 ADR 節へ追記**

「## 関連 ADR」に以下を追加する:

```markdown
- ADR-0048〜0053（v1.1 改訂: model 必須・スキーマ版数 v・id 採番強化・friction string[]・運用ガイド・記録単位）
```

- [ ] **Step 5: 実体確認**

Run: `grep -n '"v":2\|model\|記録単位' skills/worklog-record/SKILL.md | head`
Expected: エントリ例に `"v":2` と `"model"`、「## 記録単位（ADR-0053）」節が存在

- [ ] **Step 6: コミット**

```bash
git add skills/worklog-record/SKILL.md
git commit -m "feat(worklog): worklog-record を v2 スキーマ・記録単位規範・運用ガイドへ更新"
```

---

### Task 3: worklog-extract SKILL.md の更新（読み側互換）

**Files:**
- Modify: `skills/worklog-extract/SKILL.md`

- [ ] **Step 1: 手順 2（サブエージェント走査）へ互換読みを追記**

手順 2 を以下へ書き換える:

```markdown
2. **サブエージェント走査**: 全 `log.jsonl` を1パスで読み、類似エントリをクラスタリングする（メインコンテキストに載せない。原則3の関心分離）。読み込み時はスキーマ版数の互換規約に従う: **`v` なし行 = v1 と解釈し、v1 の `friction`（string）は 1 要素配列として読み替える**（ADR-0049/0051）
```

- [ ] **Step 2: 手順 3（クラスタ評価）へ model 観点を追記**

手順 3 を以下へ書き換える:

```markdown
3. **クラスタ評価**: 横断再発回数・出所プロジェクト数・`friction` / `corrections` の重みを集計する。`model` フィールドにより「特定モデル固有の躓きか、全モデル共通か」を判断材料に加える（ADR-0048。v1 行は model 不明として扱う）
```

- [ ] **Step 3: 関連 ADR 節へ追記**

「## 関連 ADR」に以下を追加する:

```markdown
- ADR-0048/0049/0051（読み側互換: model 材料・v 版数判別・friction 読み替え）
```

- [ ] **Step 4: 実体確認**

Run: `grep -n 'v1\|model' skills/worklog-extract/SKILL.md | head`
Expected: 手順 2 に v1 読み替え、手順 3 に model 観点が存在

- [ ] **Step 5: コミット**

```bash
git add skills/worklog-extract/SKILL.md
git commit -m "feat(worklog): worklog-extract へ v1/v2 互換読みと model 評価観点を追加"
```

---

### Task 4: spec 01/02 の書き換え更新（スナップショット維持）

**Files:**
- Modify: `docs/current/specs/2026-07-17-worklog-skill-pipeline/01-worklog-store.md`
- Modify: `docs/current/specs/2026-07-17-worklog-skill-pipeline/02-skill1-record.md`

- [ ] **Step 1: 01 のエントリスキーマ表を v2 化**

必須フィールド列挙・表を Task 1 Step 1 と同内容（`v` / `model` 追加、`friction` は string[]）へ書き換える。「delta ペア」表の `friction` 型も `string[]` に変更し、説明へ「複数の躓きは要素を分ける（ADR-0051）」を追記する。

- [ ] **Step 2: 01 の台帳スキーマ表と例を v2 化**

Task 1 Step 2 と同内容（`v` 行追加・例へ `"v":2` 付与）へ書き換える。

- [ ] **Step 3: 01 のインターフェース表と制約節を更新**

- インターフェース表の「id 採番」行を以下へ書き換える:

```markdown
| id 採番 | 02 | 追記直前に `<folderName>/log.jsonl` を再読みしてその日の連番 NN を決め `<project>-<date>-<NN>` を生成。追記後に読み直して自行の id 重複を検証（ADR-0050） |
```

- 「このブロック固有の制約・前提」の「すべて**追記専用**で…」の箇条書きに以下を追記する:

```markdown
- **スキーマ版数**: 全レコードに `v`（現行 `2`）を必須で書く。読み側規約「`v` なし = v1」。v1 行は `model` なしを許容し `friction`（string）を 1 要素配列と読み替える（ADR-0049/0051）。サイクル名「worklog v1.1」とスキーマ版数 `v` は別物
- **追記専用の唯一の例外**: id 採番の読み直し検証で自行の id 重複を検出した場合に限り、自分が書いた直後の行を再採番して書き直してよい（ADR-0050）
```

- 「関連 ADR」へ `ADR-0048〜0053（v1.1 改訂）` を追記する。

- [ ] **Step 4: 02 のサブ機能・制約を更新**

- サブ機能 2（delta 抽出）へ「friction は string[]。誤答側・損失規模の運用ガイド（ADR-0052）」を反映（Task 2 Step 1 と同旨）
- サブ機能 4（id 採番）を「追記直前の再カウント＋追記後の読み直し検証（ADR-0050）」へ書き換え
- サブ機能 6（エントリ構築と検証）の必須フィールドへ `v` / `model` を追加
- サブ機能 2 の直後に「記録単位: 1 エントリ = 同一 context の delta 束。テーマ単位で複数記録可（ADR-0053）」を追記
- 「関連 ADR」へ `ADR-0048〜0053（v1.1 改訂）` を追記する

- [ ] **Step 5: 実体確認**

Run: `grep -n 'string\[\]\|"v":2\|model\|ADR-0050\|ADR-0053' docs/current/specs/2026-07-17-worklog-skill-pipeline/01-worklog-store.md docs/current/specs/2026-07-17-worklog-skill-pipeline/02-skill1-record.md | head -20`
Expected: 01 に v2 スキーマ・台帳 v・採番強化、02 に v/model・記録単位・採番強化が存在

- [ ] **Step 6: コミット**

```bash
git add docs/current/specs/2026-07-17-worklog-skill-pipeline/01-worklog-store.md docs/current/specs/2026-07-17-worklog-skill-pipeline/02-skill1-record.md
git commit -m "docs(spec): worklog spec 01/02 を v2 スキーマへ書き換え更新"
```

---

### Task 5: Issue-0028 への据え置き判断の追記

**Files:**
- Modify: `docs/working/issues/system/0028-worklog-multi-user-org-deployment.md`

- [ ] **Step 1: 検討状況へ追記**

「## 検討状況」の「（未着手）」を以下へ書き換える:

```markdown
- 2026-07-17（worklog v1.1 改訂サイクル）: Issue 本文の想定どおり **v2 据え置きを確認**（今回は変更なし）。職場での複数人運用が具体化した時点で着手する。なお v1.1 でスキーマ版数 `v`（ADR-0049）が導入されたため、将来の author 追加等のスキーマ拡張は版数インクリメントで安全に行える
```

- [ ] **Step 2: コミット**

```bash
git add docs/working/issues/system/0028-worklog-multi-user-org-deployment.md
git commit -m "issues: Issue-0028 に worklog v1.1 サイクルでの v2 据え置き判断を追記"
```

---

### Task 6: プラグイン更新とスモークテスト（ユーザー協働）

**Files:**
- 変更なし（検証のみ。中央ストア `$HOME/.ai-dev-worklog/MakeAiInstructions/log.jsonl` へ 1 行追記される）

- [ ] **Step 1: ユーザーへプラグイン更新を依頼**

ユーザーに Claude Code 上で `/plugin marketplace update ai-driven-dev-principles` の実行を依頼し、`√ Updated 1 marketplace` 相当の出力を確認してもらう（AI からは実行不可）。

- [ ] **Step 2: v2 エントリのスモークテスト**

`worklog-record` スキルを手動起動し、本サイクルの実 delta（記録ゲートを満たすもの）を 1 件記録する。ゲートを満たす delta が無い場合はスモークテストを「記録なし判定の確認」で代替し、v2 書式の検証は次の実 delta 発生時に行う（合成テストデータでストアを汚さない）。

- [ ] **Step 3: 実体確認**

Run: `tail -1 "$HOME/.ai-dev-worklog/MakeAiInstructions/log.jsonl"`
Expected（記録が発生した場合）: 追記行に `"v":2` / `"model":"..."` が含まれ、`friction` があれば配列型。既存 1 行目（v1）は無変更のまま

Run: `wc -l < "$HOME/.ai-dev-worklog/MakeAiInstructions/log.jsonl"`
Expected: 記録発生時 2、記録なし判定時 1

---

### Task 7: 完了処理（ADR 昇格・Issue close・注記）

**Files:**
- Modify: `docs/records/decisions/0048-worklog-entry-model-field-required.md` ほか 0049〜0053（Status を Accepted へ）
- Modify: `docs/records/decisions/0045-worklog-entry-schema-and-lifecycle.md`（一部改定注記）
- Modify: `docs/records/decisions/README.md`（ステータス更新）
- Modify: `docs/working/issues/flow/0023-worklog-record-entry-count-and-priority-norm.md`
- Modify: `docs/working/issues/system/0025-worklog-entry-model-field.md`
- Modify: `docs/working/issues/system/0026-worklog-schema-version-field.md`
- Modify: `docs/working/issues/system/0027-worklog-id-collision-concurrent-sessions.md`
- Modify: `docs/working/issues/system/0029-worklog-entry-schema-refinements.md`
- Modify: `docs/working/issues/README.md`（close 反映）

- [ ] **Step 1: ADR-0048〜0053 を Accepted へ昇格**

各 ADR の `- **Status**: Proposed` を `- **Status**: Accepted` へ変更し、`docs/records/decisions/README.md` の該当 6 行の `Proposed` も `Accepted` へ更新する（実装完了・検証後のチェックポイント昇格。ADR-0019）。

- [ ] **Step 2: ADR-0045 へ一部改定注記**

ADR-0045 の Status 行を以下へ書き換える（既存注記フォーマットに従う。ADR-0042 の一次経路特定分）:

```markdown
- **Status**: Accepted（ADR-0048/0049/0050/0051 で一部改定: エントリへ model・スキーマ版数 v を必須追加、friction を string[] 化、id 採番へ追記直前再カウント＋読み直し検証を追加）
```

- [ ] **Step 3: Issue 5 件を close**

0023 / 0025 / 0026 / 0027 / 0029 の各ファイルについて:

- `- **Status**: open` → `- **Status**: closed`
- `- **Opened**: 2026-07-17` の直後に `- **Closed**: <実施日>` を追加
- 「## 検討状況」の「（未着手）」を worklog v1.1 改訂サイクルでの検討要約 1〜2 行へ書き換え
- 「## 結論」の「（open）」を対応 ADR 番号へ書き換え（0023→ADR-0053、0025→ADR-0048、0026→ADR-0049、0027→ADR-0050、0029→ADR-0051/0052）

`docs/working/issues/README.md` の該当 5 行の Status を closed へ更新する。

- [ ] **Step 4: 実体確認**

Run: `grep -l 'Status.*: closed' docs/working/issues/flow/0023*.md docs/working/issues/system/0025*.md docs/working/issues/system/0026*.md docs/working/issues/system/0027*.md docs/working/issues/system/0029*.md | wc -l`
Expected: 5

Run: `grep -c '| Accepted | 2026-07-17 |' docs/records/decisions/README.md`
Expected: Task 開始前より 6 増（0048〜0053 の昇格分。0045/0047 の既存 Accepted 行に注意して前後比較で確認する）

- [ ] **Step 5: コミット**

```bash
git add docs/records/decisions/ docs/working/issues/
git commit -m "adr+issues: ADR-0048-0053 を Accepted へ昇格・ADR-0045 一部改定注記・Issue-0023/0025/0026/0027/0029 を close"
```

---

## 検証整合の突合（ADR-0034）

- Task 1 期待値 `"v":2` ≥4: Step 1 の互換読み節（1）＋ Step 2 の台帳例（3）= 4。整合
- Task 6 期待値 `wc -l` = 2 or 1: 既存 v1 エントリ 1 行＋スモークテスト追記 0〜1 行。整合
- Task 7 期待値 closed 5 件: close 対象 Issue は 0023/0025/0026/0027/0029 の 5 件（0028 は open のまま）。整合

## 実施後（本計画のスコープ外・セッションフローで実施）

- handoff 更新（start-work Post ラッパー）
- master への merge（superpowers:finishing-a-development-branch）と merge 後の retrospective
- inbox 残置ファイルはユーザーが手動移動予定（触らない）
