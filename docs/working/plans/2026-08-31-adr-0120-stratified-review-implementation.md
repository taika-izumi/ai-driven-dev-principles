# ADR-0120 実装計画: 初回体数の型別既定と通算巡数の分布外検知

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ADR-0120（確定前レビューの初回体数を成果物 2 型で層別し、通算巡数の分布外検知を反復提示に常設）の Decision 1〜4、および Decision 5 の根拠・再較正の向きの配布先向け記録（Step 1-8）をスキル・spec・関連 ADR・handoff へ反映し、plugin 0.1.16 として配布物を更新する。

**Architecture:** 規範の正本は `skills/pre-finalization-review/SKILL.md`（2 型分類・体数既定・発火条項）と `skills/session-handoff/SKILL.md`（反復継続記載の項目追加）。現行 spec・関連 ADR 4 本・master handoff は正本への追随（写しの同期・部分修正/注記の記録）。全変更を 1 コミットで確定し、コミット前に執行点 4 手順（生成器・両 -Check・dist 同コミット・配布物目視）を実施する。

**Tech Stack:** Markdown 規範文書の編集（Edit）、grep による検証、PowerShell 生成器（`scripts/build-dist.ps1` / `scripts/sync-template.ps1`）。

**設計の正本:** [ADR-0120](../../records/decisions/0120-stratified-initial-review-bodies-and-round-outlier-detection.md)（実装対象一覧は Consequences「実装対象」が正）。

---

## 逸脱判断の既定（ADR-0119 宣言欄）

- 既定どおり: `skills/start-work/references/plan-deviation-defaults.md`（plugin 0.1.15 時点の既定で判断）
- 前倒し型の判定 3 条件: 設計が確定している（ADR-0120 Proposed・コミット `55d199d`）＝成立 / 実コード全文（本計画では規範文の全文）を計画に載せられる規模＝成立（各編集の置換後全文を本計画に記載） / 実行による安全網が無い（規範文書）＝成立。**3 条件すべて成立 → 逐語厳守＋設計の変更に至ったら当該タスクをタスク別独立レビューへ格下げ**を既定とする
- 確定前レビューからの引き継ぎ一覧（実装時レビューへ委ねた不採用指摘）: なし（spec 確定点 (c) のレビューは実質収束で終了し、Issue-0103 / Issue-0114 に引き継ぎ対象の記録は確認されない）
- **本 plan 確定点の初回体数: 1 体 4 観点兼務**（写像通常型・ADR-0120 Decision 3 の先行適用。レビュアーは claude-opus-5。同 Decision の「初回体数の正本記録」の 1 行として本行を残す）

---

## 編集上の共通注意

- 規範文の変更は本計画の逐語どおりに行う（前倒し型・逐語厳守）。実体と計画が食い違う場合は ADR-0119 の型分類に従い報告する
- 未追跡ファイル（`docs/inbox/` 配下 3 件・`docs/conversation_log.md`）を `git add` で巻き込まない（コミットは Task 9 の pathspec 指定のみで行う）
- 配布対象ソース（`skills/`・`.claude-plugin/`）の識別子参照は既存の house style に従う: 識別子（ADR-NNNN / Issue-NNNN 等）は全角括弧内の文末に置き、識別子より前の同一括弧内に半角括弧・入れ子の全角括弧を挟まない（生成器の R2 判定が半角括弧を全角と同一視し、入れ子で識別子を括弧外と誤判定するため。CONTRIBUTING.md の 5 パターン表・Issue-0072）

---

### Task 1: `skills/pre-finalization-review/SKILL.md` — 2 型分類・体数既定・発火条項

**Files:**
- Modify: `skills/pre-finalization-review/SKILL.md`

- [ ] **Step 1-1: 提示規則へ 2 型分類の小節「2-2」を挿入**

「**2. 推奨判定**」の但し書き末尾（`- 該当しない場合も提示は行う。…（AGENTS.md「ユーザーへの質問と意思決定要求」の推奨提示規範に従う）` の行）と「**3. 差分明示**」の間に、空行を挟んで以下を挿入する:

```markdown
**2-2. 成果物の 2 型分類（ADR-0120）**

確定点の成果物を「**規範改定型**」（上記 2. の推奨判定が真になる確定点＝未レビューの規範・手順内容を含む）と「**通常型**」（それ以外）に分ける。型は「未レビューの規範改定を含むか」という確定点の分類であり、文書種別ではない（推奨判定が偽なら内容が規範文書でも通常型になる。定義は推奨判定への参照で与え、再定義しない）。型を使うのは 2 箇所: 初回フル巡の体数の既定（下記「手順」1）と、通算巡数の分布外検知の閾値（下記「4. 指摘反映後の反復」の発火条項）。

- **判定の凍結**: 判定は推奨判定と同一材料で、確定点到達時（初回提示の時点）に一度行い、当該確定点の反復中は固定する。反復中に指摘反映以外の追記で新規の規範内容が持ち込まれても型は動かさない（推奨判定〈真〉と乖離しうるが、その向きは分布外検知の発火が早まる側であり受容する。設計縮小で規範内容が削られても閾値はそのまま——発火が遅れる側も同様に受容する。初回体数は消費済みで、反復の体数は既存規定に従うため体数への影響は無い）
- **提示への明示**: 初回の確定点提示に、判定した型・対応する初回体数の既定（下記「手順」1）・その概算コスト（実測が無い型では見積りである旨を添える）を明示する
- **記録**: 判定結果は `session-handoff` update の反復継続記載の列挙へ、独立項目「成果物の型」として記録する（値は「規範改定型／通常型／規範改定型（迷い判定・閾値は通常型の値）」の 3 値。既存項目「対象確定点の型」〈spec (a)〜(c)／plan〉とは別項目——同名の「型」の衝突を項目名で区別する）
- **迷い既定**: 型の判定に迷う場合、体数の既定は規範改定型側（観点分離）を、分布外検知の閾値は通常型の値（4 巡）を適用する（型そのものは規範改定型として一意に扱い、閾値の適用にのみ特則を置く——体数は検出力の安全側〈分離〉へ、閾値は歯止めの安全側〈早い発火〉へ、2 軸それぞれの安全方向へ倒すため）。迷い判定はその旨ごと記録する（閾値特則がセッション跨ぎで失われ 8 巡へ静かに戻るのを防ぐ）
- **記録喪失時の再判定**: 記録が失われた場合（反復継続記載は反復終了時に除去されるため、打ち切り後の再開でセッション記載が無い場合を含む）は、現時点の成果物を読み直して同じ規則で再判定し、以後はその結果で凍結する（再判定でも迷う場合は迷い既定を適用する。確定点到達時と成果物が変わっていれば型が移りうるが、稀な回復経路に限るため受容する）
```

- [ ] **Step 1-2: 手順 1 の体数規定を型別既定へ改める（実測注記の両側更新＋初回体数の正本記録の 1 文を含む）**

現行（手順 1 の末尾行）:

```
   観点は 4 つを維持する。確定前レビューの反復（指摘反映後の再レビュー）のフル巡に限り、担当を兼務させて体数のみ 1〜4 体へ縮小してよい（「反復の実施」節参照。4 体構成・1 体 4 観点兼務の実測は無い）。
```

置換後:

```
   観点は 4 つを維持する。体数は担当を兼務させて 1〜4 体へ縮小してよい（反復のフル巡の縮小は「反復の実施」節参照）。初回フル巡の体数の既定は成果物の型（提示規則 2-2）で層別する: 規範改定型は観点分離（4 観点・4 体）を、通常型のうち**内容の大部分がレビュー済み上流からの写像である場合**に限り **1 体 4 観点兼務**を、それ以外の通常型は観点分離を既定とする。写像か否かの判定に迷う場合は観点分離側へ倒す。「レビュー済み上流」は提示規則 2. 但し書きの「上流の spec がその確定時点で独立レビューを受けている場合」の概念を用い、設計文書兼用 ADR——spec 確定点 (c) の型——は spec 確定点の一型として但し書きの範囲内で扱う。1 体兼務は初回の体数既定であって巡数を約束しない（写像 plan を 3 体分離で 2 巡・指摘 31 件を要した対抗実測がある。Issue-0107）。体数の増減の判断材料は「反復の実施」節の適用例を参照する（実測の出所: 観点分離が効いた非写像 2 件・1 体兼務が機能した写像 plan 1 件。Issue-0114 / Issue-0103。ADR-0120）。1 体兼務を適用した確定点では、初回体数（1 体 4 観点兼務である旨）を当該サイクルの正本（plan または課題ログ）へ 1 行残す（`review=` の記録形式は変えない。いずれも無いサイクルでは記録先が無く、当該確定点は評価可能性の帰属母数から除かれる——記録の不在を「兼務でなかった」と推定しない）。
```

- [ ] **Step 1-3: 手順 2 の委譲既定へ手順 1 への参照を追加**

現行（手順 2 冒頭）:

```
2. **委譲プロンプトの組み立て**: 観点ごとに独立サブエージェントへ委譲する（既定。反復の実施方式による体数の縮小・レビュアーを立てない方式は「反復の実施」節が特則として定める）。
```

置換後:

```
2. **委譲プロンプトの組み立て**: 観点ごとに独立サブエージェントへ委譲する（既定。初回フル巡の体数の型別既定は手順 1 が、反復の実施方式による体数の縮小・レビュアーを立てない方式は「反復の実施」節が特則として定める）。
```

（同行の後続文は変更しない）

- [ ] **Step 1-4: 「反復の実施」節のフル巡定義を更新**

現行:

```
- **フル巡**: 4 観点（敵対的・実装整合性・仕様適合・前提実在）を全数走査で覆う巡。観点は 4 つを維持し、反復のフル巡に限り担当を兼務させて体数のみ 1〜4 体へ縮小してよい（既定は 4 観点・4 体。初回の「フルレビュー（4 観点）」はフル巡と同一方式である）。レビュアーは新規とする
```

置換後:

```
- **フル巡**: 4 観点（敵対的・実装整合性・仕様適合・前提実在）を全数走査で覆う巡。観点は 4 つを維持し、担当を兼務させて体数のみ 1〜4 体へ縮小してよい（反復のフル巡の既定は 4 観点・4 体。初回の「フルレビュー（4 観点）」はフル巡と同一方式であり、初回フル巡の体数の型別既定は手順 1 が定める）。レビュアーは新規とする
```

- [ ] **Step 1-5: 「指摘反映後の反復」節へ分布外検知の発火条項を挿入**

「指摘の重大度（前巡 Critical の有無）は…実装時レビュー由来の改訂は対象外とする。」の段落と「**停止判定（ADR-0107）**: …」の段落の間に、空行を挟んで以下を挿入する:

```markdown
**通算巡数の分布外検知（ADR-0120）**: 当該確定点の通算巡数——`review=` の縮退規定と同じ「巡を立てる方式の和」（フル巡・差分確認巡の和。機械検証は数えない）——が**通常型 4 巡・規範改定型 8 巡**（型は提示規則 2-2。迷い既定の適用時は通常型の値）に達した状態で行うすべての反復提示で、方式を問わず毎回発火する（機械検証は通算巡数を増やさないが、既に閾値に達した状態での反復提示では方式を問わず発火する）。計数の復元は、当該確定点の `review=` 行 → handoff「進行中のタスク」の反復継続記載（実施済みの方式と巡数） → 直近の改訂差分（改訂前退避を含む）・git 履歴からの概算、の順に当たり、なお判定に迷う場合は発火側へ倒す。型が不明で巡数が概算できる場合は、先に型の再判定または迷い既定（提示規則 2-2）を適用し、その閾値で概算巡数を判定する。巡数が復元・概算とも不能な場合は型を問わず発火側に従う。発火時は提示に次の 2 点の発火時注記を添える（2 点に限定し、既存の提示材料と重複させない）: (1) 実測分布の外に出た旨（値は「正しい予算」ではなく、消耗事例を除く観測最大からの分布外の合図であることを明記する） (2) 累積巡数と「続行で何を得ようとしているか」1 行（直近巡の骨格新規指摘の有無・当該巡の概算コストは既存の提示材料をそのまま用いる）。選択肢の集合は変えない（「このまま確定」は反復提示が常に並記する既存の選択肢であり、発火は打ち切りでなく提示である。続行は常に選べ、発動判断は毎回ユーザーであり上限ではない。ユーザーの打ち切りは従来どおり提示の外での明示指示として常に可能）。
```

- [ ] **Step 1-6: 停止判定へ非抵触の但し書きを追加**

現行（停止判定段落の冒頭）:

```
**停止判定（ADR-0107）**: 反復の停止は件数・巡数でなく指摘の分類で判定する。
```

置換後:

```
**停止判定（ADR-0107）**: 反復の停止は件数・巡数でなく指摘の分類で判定する（分布外検知〈上記〉の発火は停止判定ではなく提示義務であり、通算巡数を停止判定・骨格安定の判断へ流入させない。ADR-0120）。
```

- [ ] **Step 1-7: 適用例段落の改称と出所条件の拡張＋初回体数の判断材料の収容**

現行（見出し行）:

```
**反復の適用例（非拘束。出所は単一プロジェクトの一次記録（Issue-0098）に限られ、網羅の保証はない）**:
```

置換後:

```
**体数・反復の適用例（非拘束。出所は 2 プロジェクトの一次記録（Issue-0098・Issue-0103・Issue-0107・Issue-0114）に限られ、網羅の保証はない）**:
```

さらに、同見出し直後（`- **重点指定**: …` の前）へ以下の 1 項目を追加する:

```markdown
- **初回体数の判断材料**: 体数を増やす判断材料はコード量・未レビュー差分の大きさ・写経実行の要否（コードを含む計画は写経実行が必須級）。1 体 4 観点兼務が機能した実測は写像 plan 1 件（Major 2・Minor 10 を検出・観点分離の実測比で約 1/3 のコスト・レビュアーは claude-opus-5。Issue-0103）に限られ、非写像の手順書型計画・コードを含む計画では観点分離が効いた実測が各 1 件ある（Issue-0114）
```

- [ ] **Step 1-8: 「根拠と世代（ADR-0073）」節へ根拠・観測世代・再較正の向きの 1 行を追記**

同節の最終 bullet（`- 前提実在観点・手順 5 の前提検査・3-2 (b) の探索先併記の根拠: …判断はユーザーが行う` の行）の直後へ、以下の 1 行を追加する（本ステップは plan 確定点の確定前レビューで採用した補完であり、ADR-0120 Consequences の実装対象へ追記済み）:

```markdown
- 初回体数の型別既定・通算巡数の分布外検知の根拠: 2 プロジェクトの実測——体数の既定は写像 plan への 1 体 4 観点兼務が機能した 1 件と観点分離が効いた非写像 2 件、閾値は消耗事例を除く通算巡数の観測最大からの導出（Issue-0098 / Issue-0103 / Issue-0107 / Issue-0114。ADR-0120）。観測世代はレビュアー claude-opus-5・claude-sonnet 系、作成側 claude-fable-5 / claude-opus-5 系。閾値を画した巡数分布の出所は単一リポジトリのガイドライン拡張サイクルに限られ、通常開発の巡数分布は実質未観測。値は「正しい予算」ではなく分布外の合図であり、計数可能な確定点が積み上がっても発火機会が生じない状態は引き下げ・退役側の、観測最大を更新する新実測は引き上げ側の材料として読む。再較正・退役はユーザー指示の機会に、その時点の記録から AI が再導出して提案し、判断はユーザーが行う
```

- [ ] **Step 1-9: Task 1 の検証**

Run:

```bash
grep -c "規範改定型" skills/pre-finalization-review/SKILL.md
```

Expected: `5`（`grep -c` は一致**行数**を返す。内訳: 2-2 小節の定義段落・記録 bullet・迷い既定 bullet の 3 行＋手順 1 の 1 行＋発火条項の 1 行。凍結 bullet・停止判定は含まない。数が合わない場合は出現箇所を `grep -n` で列挙して本計画の逐語と突合する）

Run:

```bash
grep -c "通常型 4 巡・規範改定型 8 巡" skills/pre-finalization-review/SKILL.md && grep -c "成果物の型" skills/pre-finalization-review/SKILL.md && grep -c "提示規則 2-2" skills/pre-finalization-review/SKILL.md
```

Expected: 1 行目 `1`（発火条項のみ）・2 行目 `3`（2-2 記録 bullet・手順 1・**既存**の適用例「観点構成の型別調整」行〈変更前から存在〉）・3 行目 `2`（手順 1 の 1 行・発火条項の 1 行〈同一行内 2 出現〉）。数が合わない場合は `grep -n` で出現箇所を列挙して本計画の逐語と突合する

Run:

```bash
grep -c "分布外検知" skills/pre-finalization-review/SKILL.md
```

Expected: `6`（2-2 小節の定義段落・凍結 bullet・迷い既定 bullet の 3 行＋発火条項の 1 行＋停止判定の 1 行＋根拠と世代の 1 行）

---

### Task 2: `skills/session-handoff/SKILL.md` — 反復継続記載へ「成果物の型」を追加

**Files:**
- Modify: `skills/session-handoff/SKILL.md`（update 操作・手順 2）

- [ ] **Step 2-1: update 手順 2 の反復継続記載の列挙へ独立項目を挿入**

現行（手順 2 の該当部分。行全体のうち反復継続記載の列挙冒頭）:

```
確定前レビューの反復が継続中の場合は、対象確定点の型・実施済みの方式と巡数・
```

置換後:

```
確定前レビューの反復が継続中の場合は、対象確定点の型・成果物の型（値は「規範改定型／通常型／規範改定型（迷い判定・閾値は通常型の値）」の 3 値。定義は `pre-finalization-review` 提示規則の 2 型分類。「対象確定点の型」とは別項目）・実施済みの方式と巡数・
```

（同行の後続の列挙〈レビュアーのモデル以降〉は変更しない）

- [ ] **Step 2-2: Task 2 の検証**

Run:

```bash
grep -c "成果物の型" skills/session-handoff/SKILL.md
```

Expected: `1`

---

### Task 3: `docs/current/specs/2026-08-05-dispatch-and-pre-review-skills-design.md` — 写しの同期

**Files:**
- Modify: `docs/current/specs/2026-08-05-dispatch-and-pre-review-skills-design.md`

- [ ] **Step 3-1: 根拠 ADR 行へ ADR-0120 を追加**

現行（4〜5 行目付近）:

```
- **根拠 ADR**: ADR-0066（A 群 / B 群の分割）、ADR-0067（新規スキル＋配線）、ADR-0069（拡張先は自 repo）、ADR-0070（規範文で実装）、ADR-0071（B 群の発火判定）、ADR-0072（確定前レビューの発動条件）、ADR-0073（根拠・世代の記録と退役経路）、ADR-0093（破壊的検証の委譲制約と受け取り時確認）、ADR-0117（前提実在観点・前提検査・受け取り時義務の追加）
```

置換後（末尾へ追加）:

```
- **根拠 ADR**: ADR-0066（A 群 / B 群の分割）、ADR-0067（新規スキル＋配線）、ADR-0069（拡張先は自 repo）、ADR-0070（規範文で実装）、ADR-0071（B 群の発火判定）、ADR-0072（確定前レビューの発動条件）、ADR-0073（根拠・世代の記録と退役経路）、ADR-0093（破壊的検証の委譲制約と受け取り時確認）、ADR-0117（前提実在観点・前提検査・受け取り時義務の追加）、ADR-0120（初回体数の型別既定・通算巡数の分布外検知）
```

- [ ] **Step 3-2: 冒頭の委譲文の同期（4 観点委譲の固定前提を外す）**

現行:

```
2 スキルの関係は一方向である。`pre-finalization-review` は 4 観点を独立サブエージェントへ委譲するため、その委譲プロンプトを `subagent-dispatch` で組む。逆向きの依存はない。
```

置換後:

```
2 スキルの関係は一方向である。`pre-finalization-review` は各観点を独立サブエージェントへ委譲するため（既定。初回フル巡の体数は成果物の型で層別され、1 体 4 観点兼務の場合がある——ADR-0120）、その委譲プロンプトを `subagent-dispatch` で組む。逆向きの依存はない。
```

- [ ] **Step 3-3: 構成図ラベルの同期**

現行（構成図内）:

```
        │ 4 観点の委譲に使う
```

置換後:

```
        │ 観点の委譲に使う
```

- [ ] **Step 3-4: 手順の骨子 1 の写しの同期（初回体数の正本記録 1 文の写しを含む）**

現行:

```
1. 対象成果物とレビュー観点 4 つを立てる（敵対的 / 実装整合性 / 仕様適合 / 前提実在——機構が守るもの・得るものの実在を方針の正本と実体で検査。ADR-0117）（観点は 4 つを維持。反復のフル巡に限り体数のみ 1〜4 体へ縮小可——「反復の実施」参照）
```

置換後:

```
1. 対象成果物とレビュー観点 4 つを立てる（敵対的 / 実装整合性 / 仕様適合 / 前提実在——機構が守るもの・得るものの実在を方針の正本と実体で検査。ADR-0117）（観点は 4 つを維持。体数は初回・反復とも 1〜4 体へ縮小可。初回フル巡の体数の既定は成果物の型で層別する——規範改定型は観点分離〈4 体〉、レビュー済み上流からの写像通常型に限り 1 体 4 観点兼務、迷えば観点分離側。1 体兼務を適用した確定点では初回体数を当該サイクルの正本〈plan または課題ログ〉へ 1 行残す。ADR-0120）
```

- [ ] **Step 3-5: 手順の骨子 2 の写しの同期**

現行:

```
2. 各観点を独立サブエージェントへ委譲する。**モデルは前工程のレビューと変える**（既定。反復での体数縮小・レビュアーを立てない方式は「反復の実施」が特則）。前提実在観点には方針の正本と実体の所在を委譲プロンプトで渡す
```

置換後:

```
2. 各観点を独立サブエージェントへ委譲する。**モデルは前工程のレビューと変える**（既定。初回フル巡の体数の型別既定は手順の骨子 1、反復での体数縮小・レビュアーを立てない方式は「反復の実施」が特則）。前提実在観点には方針の正本と実体の所在を委譲プロンプトで渡す
```

- [ ] **Step 3-6: 「反復の実施」節の写しの同期**

現行（冒頭 1 文）:

```
指摘反映後の再レビューの実施方式はフル巡（4 観点・体数 1〜4 体・新規レビュアー）/ 差分確認巡（前巡指摘の対応表方式 1 体。継続レビュアーは差分確認巡に限り可——能力ゲート・新規レビュアー必須の優先・骨格安定例外の各条件は pre-finalization-review「反復の実施」節が正本）/ 機械検証（レビュアーなし・巡を立てない）の 3 種。
```

置換後:

```
指摘反映後の再レビューの実施方式はフル巡（4 観点・体数 1〜4 体・新規レビュアー。初回フル巡の体数の型別既定は手順の骨子 1 側——ADR-0120）/ 差分確認巡（前巡指摘の対応表方式 1 体。継続レビュアーは差分確認巡に限り可——能力ゲート・新規レビュアー必須の優先・骨格安定例外の各条件は pre-finalization-review「反復の実施」節が正本）/ 機械検証（レビュアーなし・巡を立てない）の 3 種。
```

（同段落の後続文は変更しない）

- [ ] **Step 3-7: Task 3 の検証**

Run:

```bash
grep -c "ADR-0120" docs/current/specs/2026-08-05-dispatch-and-pre-review-skills-design.md
```

Expected: `4`（根拠 ADR 行・冒頭委譲文・骨子 1・反復の実施）

---

### Task 4: ADR-0107 — 部分修正 2 系統＋注記

**Files:**
- Modify: `docs/records/decisions/0107-iterative-review-recommendation-by-revision-nature.md`（Consequences 末尾）

- [ ] **Step 4-1: Consequences 末尾（現行最終の `- **注記（ADR-0117）**: …` 行の直後）へ以下の 3 行を追記**

```markdown
- **部分修正（ADR-0120）**: 決定 1 の「成果物の型による追加条件は置かない」は**発火閾値に限り**部分的に反転された——反復提示には、当該確定点の通算巡数が型別閾値（通常型 4 巡・規範改定型 8 巡。型は成果物の 2 型分類）に達した状態で方式を問わず毎回発火する分布外検知の発火条項が追加された（発火は停止判定ではなく提示義務であり、推奨の向き・選択肢の集合は変えない）。決定 3 の兼務枠「反復のフル巡に限り」は、写像通常型の初回フル巡へ拡張された。決定 4 の反復継続記載の項目列挙へ独立項目「成果物の型」が追加された。反復・停止判定の骨格は現役のため、Status は Accepted のまま維持する
- **部分修正（ADR-0120）**: 評価可能性の退役側トリガー（フル巡推奨の長期全見送り）・改良側トリガー（軽量側推奨後の重大欠陥）の計数からは、分布外検知の発火機会が生じた確定点を代理指標として除いて読む（発火の遵守は観測できないため代理指標で除外する。両トリガーに同一の代理指標を適用する。計数母集団を変えるため部分修正とする）
- **注記（ADR-0120）**: 本 ADR の評価可能性の計数経路（git 履歴上の `review=` 行）は、ADR-0120 の分布外検知の発火機会の計数も共用する。決定 2 の停止判定「件数・巡数でなく指摘の分類で判定する」には、分布外検知の発火が通算巡数を停止判定・骨格安定の判断へ流入させない旨の但し書きが実装先（pre-finalization-review）で添えられた（決定を変えない明確化）
```

- [ ] **Step 4-2: Task 4 の検証**

Run:

```bash
grep -c "ADR-0120" docs/records/decisions/0107-iterative-review-recommendation-by-revision-nature.md
```

Expected: `3`

---

### Task 5: ADR-0080 — 部分修正

**Files:**
- Modify: `docs/records/decisions/0080-review-presentation-scaled-by-unreviewed-normative-content.md`（Consequences 末尾）

- [ ] **Step 5-1: Consequences 末尾（現行最終の `- **部分修正（ADR-0117）**: …` 行の直後）へ以下の 1 行を追記**

```markdown
- **部分修正（ADR-0120）**: 提示規則に成果物の 2 型分類（規範改定型／通常型。判定は Decision 2 の推奨判定と同一材料で確定点到達時に行い、当該確定点の反復中は凍結する）が追加され、初回の確定点提示には判定した型・初回体数の既定・概算コストの明示が加わった（Decision 3-2〈反対材料併記〉の適用範囲の拡大ではなく、確定点提示の判断材料の追記）。推奨判定・反対材料欄の骨格は現役のため、Status は Accepted のまま維持する
```

- [ ] **Step 5-2: Task 5 の検証**

Run:

```bash
grep -c "部分修正（ADR-0120）" docs/records/decisions/0080-review-presentation-scaled-by-unreviewed-normative-content.md
```

Expected: `1`

---

### Task 6: ADR-0117 — 部分修正

**Files:**
- Modify: `docs/records/decisions/0117-premise-existence-viewpoint-and-premise-checks.md`（Consequences 末尾）

- [ ] **Step 6-1: Consequences 末尾（現行最終の `- 配布対象ソースの変更のため、…` 行の直後）へ以下の 1 行を追記**

```markdown
- **部分修正（ADR-0120）**: Decision 1「観点数の写像」の時点注記「4 体構成・1 体 4 観点兼務の実測は無い」は、両主張とも実測の反映により更新された（4 体側 = 規範改定型 spec 確定点の反復実測〈Issue-0107 事例 10〉、1 体兼務側 = 写像 plan の初回フル巡実測〈Issue-0103 検討状況 2026-08-30〉）。Consequences「費用が増える」の時点注記「4 体構成の実測は無い」も同様に更新して読む（規範改定型 spec 確定点で 4 体 ≈58〜69 万トークンの実測が得られた——見積り値の記載は据え置き）。あわせて初回フル巡の体数に成果物の型別既定（規範改定型 = 観点分離 4 体、レビュー済み上流からの写像通常型 = 1 体 4 観点兼務）が追加された。前提実在観点の常設・検査手順の骨格は現役のため、Status は Accepted のまま維持する
```

- [ ] **Step 6-2: Task 6 の検証**

Run:

```bash
grep -c "部分修正（ADR-0120）" docs/records/decisions/0117-premise-existence-viewpoint-and-premise-checks.md
```

Expected: `1`

---

### Task 7: ADR-0067 — 注記

**Files:**
- Modify: `docs/records/decisions/0067-pre-finalization-review-as-new-skill.md`（Consequences 末尾）

- [ ] **Step 7-1: Consequences 末尾（現行最終の `- **注記（ADR-0117）**: …` 行の直後）へ以下の 1 行を追記**

```markdown
- **注記（ADR-0120）**: 注記（ADR-0107）の兼務縮小枠「反復のフル巡に限り」は、ADR-0120 により写像通常型（レビュー済み上流からの写像）の初回フル巡へも拡張された。「観点は最低 3 つ」は 1 体 4 観点兼務でも観点数として維持されるため、決定の変更にあたらない
```

- [ ] **Step 7-2: Task 7 の検証**

Run:

```bash
grep -c "注記（ADR-0120）" docs/records/decisions/0067-pre-finalization-review-as-new-skill.md
```

Expected: `1`

---

### Task 8: `docs/working/handoff/master.md` — 稼働規範の要約行の追随

**Files:**
- Modify: `docs/working/handoff/master.md`（稼働規範の要約行。push 記録の未コミット更新はコミット `e5796d9` で解消済み＝作業ツリーは clean。本タスクの追随分のみを Task 9 のコミットに同梱する。feature handoff「既知のブロッカー・懸念」の当該記載は次回 update で解消済みへ更新する）

- [ ] **Step 8-1: 該当行の末尾へ追随を追記**

現行（該当行の末尾部分）:

```
**確定前レビューは ADR-0117 により 4 観点（敵対的・実装整合性・仕様適合・前提実在）・体数 1〜4 体。集約手順 5 は指摘 1 件ずつの前提検査、3-2 (b) の「なし」記載は探索先併記が必須**
```

置換後:

```
**確定前レビューは ADR-0117 により 4 観点（敵対的・実装整合性・仕様適合・前提実在）・体数 1〜4 体。集約手順 5 は指摘 1 件ずつの前提検査、3-2 (b) の「なし」記載は探索先併記が必須**。**ADR-0120 により確定点は 2 型分類（規範改定型／通常型。到達時に凍結・提示へ型と体数既定を明示）——初回フル巡の体数は規範改定型 = 観点分離 4 体・写像通常型 = 1 体 4 観点兼務（迷えば観点分離）、反復提示には通算巡数の分布外検知（通常型 4 巡・規範改定型 8 巡で発火時注記 2 点。発火は提示であり上限ではない）が常設**
```

- [ ] **Step 8-2: Task 8 の検証**

Run:

```bash
grep -c "ADR-0120" docs/working/handoff/master.md
```

Expected: `1`

---

### Task 9: version bump・執行点 4 手順・コミット

**Files:**
- Modify: `.claude-plugin/plugin.json`（version `0.1.15` → `0.1.16`）
- Modify: `.claude-plugin/marketplace.json`（version `0.1.15` → `0.1.16`）
- Regenerate: `dist/`・`.agents/plugins/marketplace.json`（生成器が導出）

- [ ] **Step 9-1: 2 つの version を更新**

`.claude-plugin/plugin.json` の `"version": "0.1.15"` → `"version": "0.1.16"`、`.claude-plugin/marketplace.json` の `"version": "0.1.15"` → `"version": "0.1.16"`（両者の一致は生成器が検査する）。

- [ ] **Step 9-2: 執行点手順 1 — 生成器の実行**

Run: `scripts/build-dist.ps1`（`skills/`・`.claude-plugin/` を変更したため。`template.manifest` 記載ファイルは変更していないため `sync-template.ps1` の生成実行は不要）
Expected: 正常終了（非ゼロ終了なら規約違反・version 不一致を修正して再実行）

- [ ] **Step 9-3: 執行点手順 2 — 両生成器の -Check 実行**

Run: `scripts/build-dist.ps1 -Check` および `scripts/sync-template.ps1 -Check`
Expected: 両方とも正常終了（差分なし）

- [ ] **Step 9-4: 執行点手順 4 — 配布物の目視**

`dist/` 配下の `pre-finalization-review` / `session-handoff` の生成物を読み、CONTRIBUTING.md の 5 パターン（括弧内の識別子以外の語の同居・半角括弧・書式例の実在固有名・自己参照・docstring/表示メッセージ）を目視確認する。特に今回追加した文中の識別子除去後の読みやすさ（「（…。Issue-0114 / Issue-0103。ADR-0120）」等の除去後残骸）を確認し、問題があればソース側の括弧構成を修正して Step 9-2 からやり直す。

- [ ] **Step 9-5: ステージングと変更内容の実体確認（コミット直前の読み直し）**

まず pathspec 指定で add する（未追跡の本計画ファイルは `git diff` に現れないため、先に add してから `--cached` で読む）:

```bash
git add skills/pre-finalization-review/SKILL.md skills/session-handoff/SKILL.md docs/current/specs/2026-08-05-dispatch-and-pre-review-skills-design.md docs/records/decisions/0120-stratified-initial-review-bodies-and-round-outlier-detection.md docs/records/decisions/0107-iterative-review-recommendation-by-revision-nature.md docs/records/decisions/0080-review-presentation-scaled-by-unreviewed-normative-content.md docs/records/decisions/0117-premise-existence-viewpoint-and-premise-checks.md docs/records/decisions/0067-pre-finalization-review-as-new-skill.md docs/working/handoff/master.md docs/working/plans/2026-08-31-adr-0120-stratified-review-implementation.md .claude-plugin/plugin.json .claude-plugin/marketplace.json .agents/plugins/marketplace.json dist/
```

次に `git status --short` で、ステージ外に `docs/inbox/` 3 件・`docs/conversation_log.md` が未追跡のまま残っていること（巻き込み禁止）を確認し、`git diff --cached` で全ステージ内容を読み直して本計画の逐語と一致していることを確認する（AGENTS.md 検証規範: 節目の確定直前はツールの戻り値ではなく実体の読み直しで確認する）。ADR-0120 のステージ差分は、Consequences 実装対象への「根拠と世代」1 項目の追記（本 plan の確定前レビューでの採用時に適用済み。趣旨は Step 1-8 冒頭の括弧書きのとおり）**のみ**であり、他の差分が無いことを確認する。

- [ ] **Step 9-6: コミット（執行点手順 3 — 生成物を同コミットに含める）**

```bash
git commit -m "feat: ADR-0120 実装 - 初回体数の型別既定と通算巡数の分布外検知（plugin 0.1.16）"
```

Expected: コミット成功。

---

## 実装後の残作業（本計画のスコープ外・handoff 既載）

- ADR-0120 の Accepted 昇格（実装完了チェックポイント。`decision-log`「承認の昇格」の手順・サイクル全体整合検査を含む）
- 昇格時: Issue-0103 close・Issue-0114 検討状況への対策範囲追記（ADR-0120 Consequences の規定どおり）

## 期待値とタスク編集内容の突合（AGENTS.md 検証規範）

- Task 1 の grep 期待値（`規範改定型` = 5 行・`通常型 4 巡・規範改定型 8 巡` = 1 行・`成果物の型` = 3 行〈既存の適用例「観点構成の型別調整」行 1 行を含む〉・`提示規則 2-2` = 2 行・`分布外検知` = 6 行〈Step 1-8 の根拠と世代 1 行を含む〉）は Step 1-1〜1-8 の置換後テキストの一致行数＋既存出現の実測から導出した（第 1 巡の独立レビュアーが置換後ファイルへの機械適用で検算し、`分布外検知` = 6 を含む全期待値は差分確認巡の機械適用で実測一致を確認済み。R2 規約判定も violations = 0 を実測済み）。実装時に差が出た場合は `grep -n` で出現箇所を列挙して照合し、逐語一致なら実測値を正として計画のこの行に実測値を追記する
- Task 3 の `ADR-0120` = 4 行は Step 3-1/3-2/3-4/3-6 の 4 編集に対応（Step 3-3 は識別子を含まない。Step 3-5 も含まない）
- Task 4 の `ADR-0120` = 3 行は追記 3 行に対応（3 行目は同一行内に 2 出現するが行数は 1）。Task 5〜8 は各 1 行
- Task 9 で ADR-0120 をステージする差分は Consequences 実装対象への 1 項目追記のみ（本 plan の確定前レビューでの採用時に適用済み。Step 9-5 が他差分なしを確認する）
