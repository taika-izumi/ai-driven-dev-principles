# レビュー推奨と追加物の費用評価の実装計画

> **実行担当者へ:** `superpowers:executing-plans` を用いてタスク順に実行する。サブエージェント方式を選ぶ場合は `superpowers:subagent-driven-development` を用いる。チェックボックスは実施後に更新する。

**目的:** レビュー方式・人数の推奨を状況判断へ移し、追加する仕組みの費用を既存の採否手順で比較できるようにする。

**構成:** `pre-finalization-review` の既存4文書とCONTRIBUTING.mdを改定する。初回の強い推奨、実施のユーザー判断、停止・記録・独立性の既存条件を維持する。別の検査スクリプトや台帳は作らない。

**使用技術:** Markdown、JSON、既存のPowerShell配布生成器。製品コードの新設・変更はない。

**設計:** `docs/current/specs/2026-08-05-dispatch-and-pre-review-skills-design.md` の「スキル2」以降。ADR-0124・0125。設計レビューはフル実施1回＋差分再確認1回で実質的な収束。

**状態:** 確定（2026-09-05）。計画レビュー第1回の指摘2件を反映し、ユーザー判断で追加レビューをせず確定した。タスク1〜4のローカル実装・検証済み。判断例の限界と実装レビューIR-1の修正は実施記録を参照。

## 全体の制約

- 初回の未レビュー規範内容には4観点の独立レビューを強く推奨するが、実施・見送りはユーザー判断。誤記等の除外、レビュー済み上流の扱い、記録不明時の扱いを維持する。
- フル実施4観点・担当1〜4体、差分再確認1体、機械検証はレビュアーなし。方式・人数を状況から判断する。
- レビュー通算回数の通常型4回・規範改定型8回の注意喚起と復元規定を維持する。回数上限にはしない。
- 停止2経路、骨格安定の停止・継続利用上の条件、退避・独立性・記録形式を維持する。方式の推奨を固定する用途からは外す。
- 原則・AGENTS.md、委譲制約の全般、実装時の逸脱判断・レビュー深度を変更しない。既存仕様書のスキル1の責務も変更しない。
- 配布対象ソースの記法と語彙はCONTRIBUTING.mdと `docs/overview/wording-replacements.md` に従う。出所の参照番号は全角括弧内または出所リストへ置く。
- 既存の未追跡 `.claude/`、`docs/conversation_log.md`、inbox3件を編集・ステージしない。生成物は生成器から作り、手修正しない。

## 逸脱判断の既定

`skills/start-work/references/plan-deviation-defaults.md`（plugin 0.1.19）を適用する。実行中に配布版が変わっても、この計画の判断基準は自動変更しない。

本計画は文書改定であり、実コード全文を計画から書き写す方式ではない。タスク別独立レビューを置かず主担当が逐次確認する方式で実行する場合も、設計変更級の逸脱では当該タスクを独立レビューへ切り替える安全弁を維持する。各タスクで計画との差分と逸脱記録を自己照合する。

設計レビューからの判断記録: `docs/working/issues/flow/0107-iterative-review-recommendation-divergence/0107-2026-09-05-spec-review-r1.md` のCR-1と、同 `0107-2026-09-05-spec-review-r2.md`。評価専用の必須保存行を追加する案は採用せず、復元可能な既存記録へ評価を限定した。実装時にこの案が再提案された場合も、既存の判断を照合して扱う。実装時レビューへ未解決のまま委ねた指摘はない。

逸脱が発生したタスクでは、そのタスク末尾へ正本の書式 `逸脱記録: 型 / 帰結 / 参照番号・根拠` の行を残す。書き手は主担当。確定前レビュー中の改訂にはこの行を付けない。

## 準備と確定の順序

現在のブランチは `codex/issue-0107-0124-review-judgment`。既存の仕様・ADR草稿・レビュー記録・引き継ぎの未コミット変更を継続して扱う。ここから別の作業場所へ移す場合は、未追跡のADRとレビュー記録も含む現在の作業状態を引き継ぐ。

以下の読取専用コマンドを作業ルートで実行する。

```powershell
git -c safe.directory=D:/Dev/002_AiDev/MakeAiInstructions branch --show-current
git -c safe.directory=D:/Dev/002_AiDev/MakeAiInstructions status --short
pwsh -NoProfile -File scripts/build-dist.ps1 -Check
pwsh -NoProfile -File scripts/sync-template.ps1 -Check
```

計画作成時の実測: 両Checkは終了0、`Convention violations: 0` と `Up to date.`。ソース36件、テンプレート7ファイル＋空索引3件は診断情報であり、今後の固定完了条件にはしない。

タスク1〜3でソースと生成物の差が生じるため、その途中でCheck成功やコミットを要求しない。タスク4で生成・検証し、関連ソースと生成物を同じ実装コミットに含める。設計・計画の草稿のみを先にコミットする場合にも、索引に対応するテンプレートのCheckを通す。

## タスク1: 推奨の判断と反復手順を改定する

**変更ファイル:**

- `skills/pre-finalization-review/SKILL.md`
- `skills/pre-finalization-review/references/iteration-norms.md`

**入力:** 設計書の「初回の強い推奨と方式・人数の判断」「提示する内容」「反復の実施と歯止め」「停止・採否・記録」。

**出力:** 初回と反復で共用する判断の正本はSKILL.md、反復固有の条件と実施はiteration-norms.md。次タスクはこの責務分担を前提に集約手順を更新する。

- [x] `writing-skills` のRED/GREEN/REFACTORと判断例検証の手順を適用し、下の改定前確認を実施する。結果を見てからソースを編集する。同スキルのDeploymentは、本リポジトリの配布物同時コミット規約に従ってタスク4へ統合する。タスク1・2は同じスキルの一連の改定であり、途中でcommit/pushしない。
- [x] 編集前に2文書を読み、初回の除外条件、2型の凍結・復元、反復提示の全経路、停止2経路、継続利用条件を特定する。
- [x] SKILL.mdの「確定点での提示（提示規則）」の推奨判定は未レビューの規範内容に限定したまま維持する。同一サイクル内の重複推奨防止を含む但し書きを落とさない。
- [x] 「観点と初回フル実施の体数」の節名を維持し、中に「方式・人数の判断」を設ける。型ごとの人数固定を次の判断文へ置き換える。

> 初回・反復とも、レビュー済みと未確認の範囲、改訂の影響、直近指摘が元の設計か修正から出たものか、残る懸念を確認できる手段、後続の実在する検証、確認の便益と費用から方式・人数を判断する。4観点は維持し、担当は1〜4体とする。初回の未レビュー規範への強い推奨を解除するものではない。判断に迷う場合は確認範囲を広くする側を推奨し、方式が未決ならフル実施、人数が未決なら観点分離とする。

- [x] 提示規則2-2の「型を使うのは2箇所」を回数注意喚起の用途へ直す。「対応する初回体数の既定」は「判断した人数」に直す。型の凍結・記録・復元・迷い時の閾値は維持し、迷い時の人数は共通判断へ参照する。初回の型・人数・概算費用の明示と1体兼務の記録義務は残す。
- [x] 提示規則3-2を「推奨理由と最も強い反対材料を1〜2文で示す。反対材料が無ければその旨と確認した出所を示す」に整理する。適用範囲を現在の確定点・反復提示から広げず、迷い由来の推奨であることも示す。短文化は理由部分に限り、未レビュー範囲・採否・必須の発火時情報を省かない。
- [x] iteration-norms.mdの「推奨の向きは次の順で決める」から2段型・締めモードによる固定推奨を外し、SKILL.md「方式・人数の判断」を参照する。変更が削除中心の場合も、新規規範文の有無だけでフル実施へ戻さない。
- [x] 増設2回連続の節を必要性・縮小余地点検へ置き換える。方式を問わない計数と、履歴喪失時の退避・git差分からの復元を残す。必要検査・既存要求の充足・追加物自身の問題を区別し、合理的な縮小案だけを提示する。増設が続けば直近2回で点検し、見送りによる計数リセットを点検免除にしない。
- [x] 停止判定・骨格安定・精度型指摘の採否と修正の既定を残す。ただし骨格安定から「締めモードを推奨する」と機械的につながる文だけは共通判断の参照に直す。骨格安定を差分再確認の推奨の必須条件にしない。
- [x] フル実施の反復4体固定と初回型別既定の参照を、共通の人数判断へ直す。差分再確認は次の対象規定へ拡張する。新規規範を含む場合の新規レビュアー優先と骨格安定の例外、改訂前退避は維持する。

> 改訂前退避またはgit差分から改訂箇所を全数列挙し、前回指摘の対応表とともに渡す。改訂が影響する上流の条件・限定列挙・完了基準も確認箇所を示して含め、上流側に差分がないことだけで除外しない。影響範囲を確定できない場合は、その不確実性をフル実施側の判断材料にする。

- [x] 新しい共通判断への参照と、維持した既存の節名が実在することを確認する。既存2文書の参照表・「正本として持つ内容」も最終本文に合わせる。

**確認:** 設計書の事例表8行のうち、初回の強い推奨・除外、修正への重点化、必要検査の維持、上流条件、回数、停止の7行を新しい本文へ当てはめる。旧本文なら形式で固定されていた箇所が、意図した判断へ変わることを文章上の条件と参照で確認する。全体を実行して品質を保証する自動テストではない。

特に維持する条件: 通算4/8回と不明時復元、指摘ゼロ時の確定、全不採用時の提示、機械検証で修正した場合の再提示、停止の2経路、継続レビュアーの能力条件・新規優先・例外、無条件の改訂前退避。`git diff --`で各差分を読み、維持対象まで消していないことを確認する。

逸脱記録: 事実誤り・期待値の陳腐化の訂正 / 採用 / IR-1、共通方式判断への既存参照と読取表を一致させた、独立レビュー切替: 不発

## タスク2: 指摘採否と新設ADRに費用比較を組み込む

**変更ファイル:**

- `skills/pre-finalization-review/references/review-procedure.md`
- `skills/pre-finalization-review/references/examples-and-evidence.md`
- `CONTRIBUTING.md`

**入力:** タスク1の共通判断と、設計書「追加する仕組みの採否」、ADR-0125。

**出力:** 既存の前提検査に続く費用比較と、その記録先・適用範囲。別の審査工程を作らない。

- [x] review-procedure.md手順1・2にある型別人数固定の参照を、SKILL.mdの共通判断へ変更する。4観点、実証、隔離、作成側と異なるモデル、既存の修正前退避は維持する。
- [x] 手順5の「各指摘の前提と実体を確認してから採否を決める」を残し、仕組みの新設・増設を採用する案に次の比較を加える。

> 追加案について、防ぐ失敗の発生実測または再現可能な到達経路、既存手順の文言修正・簡素化・リスク受容で足りるか、導入・運用・保守・副作用を比較する。今回限りか継続して維持するものかを示し、後者は将来の更新・実行・整合維持の負担も考慮する。満たすべき要求を確認し、その要求を満たす案の費用を比較する。単に想像できるだけの失敗や架空の費用値は根拠にしない。比較は既存の指摘対応表・報告へ添え、採否は既存のユーザー判断に残す。

- [x] 同手順に、評価は既存の計画・課題ログ・対応表・ADRから復元できる事例に限り、評価専用の必須保存行は設けないことを短く記す。復元範囲を開示し、不明を適用なし・便益なしへ換算せず、廃止判断に必要な情報が不足する場合は保留する。
- [x] CONTRIBUTING.md「全シナリオ共通: 新設の評価可能性」の既存の代替案比較の箇条へ、次を加える。適用対象は新設を含むADRのまま、既存の期待検出量・廃止条件とコミット前確認を維持する。

> 比較には、防ぐ失敗の実測または再現可能な到達経路、導入・運用・保守・副作用、今回限りか継続して維持するものかを含める。継続して維持するものは将来の負担も考慮する。分かる範囲で定性的に示し、別の審査工程・点数表・台帳は設けない。

- [x] examples-and-evidence.mdの根拠と世代を更新する。旧型別人数の固定を現行規範として説明しない。既存の人数別の実測は非拘束の判断材料として残し、回数閾値の根拠と廃止の条件を維持する。ADR-0124・0125の出所・モデルの限界・評価可能性を短く記載する。
- [x] 新しい評価の記録不足の扱いを既存全スキルへ一般化しない。decision-logはCONTRIBUTINGの既存参照を使う。実装時の逸脱判断の採用基準は変更しない。
- [x] 下の判断例を改定後のスキル一式とともに新規コンテキストへ渡し、基準と照合する。規範が狙う判断を妨げていないことを確認してから次へ進む。

**確認:** 設計書の「追加物の修繕が続く」事例を手順5へ当てはめ、増設2回を待たず最初の追加案から費用を比較できることを確認する。実証済みの初回失敗を「過去に未発生」だけで排除しないこと、記録不足をゼロ評価しないことを確認する。構文的に同じ単語が存在するだけでは合格にしない。

### 今回限りの判断例検証

恒久的なテスト・台帳は新設せず、結果は本計画の実施記録に残す。使用モデルと推論強度は各比較で同じにする。対象は判断を誘導する文言の効果であり、性能の統計的保証ではない。

**同一の入力事例:**

> 規範文書の初回4観点レビューは完了している。今回の改訂は、不要な中間工程の削除、指摘どおりの参照先訂正、例外を2種から4種へ広げる修正である。変更されていない上流文書には「例外は2種に限る」とある。別の指摘は、異常入力で静かに壊れることを実行で再現した数行の検査の追加を求めている。さらにレビュー指摘の全件保存のための恒久台帳が提案されているが、目的は既存記録から確認できる事例だけを評価することである。ここまでに複数回レビューして時間も費用もかかっている。次の確認方式と人数、これらの指摘への対応を短く示すこと。ファイル編集や外部操作はしないこと。

**実施:** 各試行を新規コンテキストで行い、同じ入力事例・モデル・推論強度・共通指示を用いる。対象スキル以外の環境由来の指示は比較で共通とし、差は下表の本文供給だけにする。委譲時は `subagent-dispatch` の常時適用と条件判定を付ける。本文供給は、主担当が固定した入力ファイル1件の全文を1回だけ読み込む方法に揃え、それ以外のツール使用・追加ファイル読込みを禁止する（対照は本文供給なしの旨だけを同じ方法で読む）。過去の試行結果や正解の判定表は渡さない。

| 試行 | 回数・時点 | 対象4文書の供給と解釈 |
|---|---|---|
| 対象スキルなしの対照 | 編集前5回 | pre-finalization-reviewの本文を供給せず、現行版・改定版・キャッシュの追加読込みも許可しない |
| 現行版による診断 | 編集前1回 | 編集前に退避した0.1.19の4文書を全文供給する。反復比較の群ではなく、既存規範を適用した判断の診断例とする |
| 改定版の確認 | タスク2終了後5回 | 更新した同じ4文書を全文供給する。部分的な新しい文言だけを抜き出して渡さない |

4文書は `skills/pre-finalization-review/SKILL.md` と `references/iteration-norms.md`・`review-procedure.md`・`examples-and-evidence.md`。主担当が供給本文を固定し、各出力とともに版（または内容ハッシュ）・供給有無・下記(a)〜(e)の判定を本計画の実施記録へ残す。各試行は結論と根拠だけを返し、全文書の再レビューにはしない。環境による対象スキルの自動供給を排除できない場合は、その試行を「ガイダンスなし」の比較に使わず、比較不能の理由を記録する。比較を成立させるための新しい基盤は作らない。

**判定:** 各出力を主担当が読み、(a) 規範文・削除という形式だけで再度フル実施を固定しない、(b) 上流の2種という制限を確認対象に入れ、黙って4種を承認しない、(c) 実証された初回の失敗への必要な検査を一律縮小しない、(d) 評価目的に不要な恒久台帳を自動採用しない、(e) 推奨と反対材料を短く示し、実施を勝手に開始しない、を確認する。最安の人数・方式との一致は合格条件にしない。

効果の比較は対照5回と改定版5回の範囲に限り、「対象スキル本文を与えた場合との違い」として読む。改定文言だけの効果や、旧版より見落とし率が低いという主張には使わない。現行版1回と既存課題の実測は、旧規範にどの問題があったかを確認する限界付きの診断材料とする。

対照でも誤りが出ない場合は、対象スキルを与える便益を今回の試行では示せないと記録し、同じ試行を漫然と増やさない。既存課題の実測と改定前の診断を照合して必要性を判断する。設計を変える必要が生じたら逸脱規範に従う。改定後に基準を外した場合も反例を特定してから修正し、理由なく禁止文や新しい分類を追加しない。

逸脱記録: 実体に合わせる調整 / 採用 / 実施記録の本文供給経路調整、固定入力1回読取りへ全群を統一、独立レビュー切替: 不発

## タスク3: 決定記録と仕様を実装へ同期する

**変更ファイル:**

- `docs/records/decisions/0080-review-presentation-scaled-by-unreviewed-normative-content.md`
- `docs/records/decisions/0107-iterative-review-recommendation-by-revision-nature.md`
- `docs/records/decisions/0120-stratified-initial-review-bodies-and-round-outlier-detection.md`
- `docs/records/decisions/0102-codify-evaluability-mandate-in-contributing.md`
- `docs/records/decisions/0117-premise-existence-viewpoint-and-premise-checks.md`
- 必要な同期: 現行設計書、ADR-0124・0125、決定索引、課題0107・0124と課題索引、この計画、引き継ぎ

**入力:** タスク1・2の実体。**出力:** 古い決定から新しい決定へ到達でき、仕様と実装の責務・条件が一致した状態。

- [x] 次の部分修正注記を各旧ADRのConsequencesへ追加する。旧Decision本文とAccepted状態は維持し、Supersededにはしない。

| 旧ADR | 追加する注記 |
|---|---|
| 0080 | `- **部分修正（ADR-0124）**: 初回の強い推奨と実施判断を維持し、方式・人数の判断と推奨理由・反対材料の提示を状況に即した形へ変更する。StatusはAcceptedを維持する。` |
| 0107 | `- **部分修正（ADR-0124）**: 方式の固定推奨を状況判断へ変更し、増設時は必要性・縮小余地を点検する。差分再確認に影響する上流条件を含める。停止・独立性・退避の既存条件は維持し、StatusはAcceptedを維持する。` |
| 0120 | `- **部分修正（ADR-0124）**: 初回人数の型別固定を状況判断へ変更する。成果物2型と通算回数の注意喚起は維持し、StatusはAcceptedを維持する。` |
| 0102 | `- **部分修正（ADR-0125）**: 新設を含むADRの既存比較に失敗の根拠と導入・維持費用を含める。期待検出量・廃止条件と既存の執行点は維持し、StatusはAcceptedを維持する。` |
| 0117 | `- **部分修正（ADR-0125）**: 確定前レビューの前提検査を維持し、新設・増設を採用する案に費用と代替案の比較を加える。その他の委譲全般へ適用を広げず、StatusはAcceptedを維持する。` |

- [x] 設計書とタスク1・2の全変更文書を読み合わせる。設計書の接続表の対象がすべて存在し、維持対象の節名への参照が解決することを確認する。
- [x] 過剰適合の再点検を行う。引用事例は2プロジェクト10事例、モデルの記載がない事例は不明扱い。文言・適用条件を設計から変更した場合は対応するADR・仕様へ記録する。
- [x] ADR-0124・0125のAccepted昇格と課題closeは、タスク4の実装検証・サイクル全体整合検査後に行う。この段階で成功を先取りしない。

**確認:** 旧ADR5件の注記の参照先、Accepted状態、設計書の対象表を読み直す。規範の正本を旧ADRから引用する箇所で新旧の矛盾が残っていないか確認する。過去のレビュー記録・課題ログの本文は歴史として書き換えない。

## タスク4: 配布物の生成・検証・確定を行う

**変更ファイル:** `.claude-plugin/plugin.json`、`.claude-plugin/marketplace.json`、生成物 `dist/`・`.agents/plugins/marketplace.json`・必要な `template/`、タスク3の確定記録。

**入力:** タスク1〜3。**出力:** ローカルで配布可能な0.1.20と、仕様・決定・実装・生成物が整合したコミット。push・マージはこの計画のローカル実装完了後に既存の完了処理として扱う。

- [x] 2つのソースJSONのversionを0.1.19から0.1.20へ同時に更新する。既存の版が変わっていた場合は採番を再確認し、この計画へ逸脱を記録する。生成物の版は手編集しない。
- [x] 生成器の通常実行はdist/templateを再作成するため、実行ルートと対象2ディレクトリが作業リポジトリ配下であること、リンク先が外部ではないこと、未追跡・ignoredの手作業ファイルが混ざっていないことを確認する。混在時は削除を進めず必要なファイルを保全する。`pre-action-review`で既存のリスク分類に従う。
- [x] 既存の生成器を次の順で実行する。1つが失敗したらその原因を修正してから次へ進む。

```powershell
pwsh -NoProfile -File scripts/build-dist.ps1
pwsh -NoProfile -File scripts/sync-template.ps1
pwsh -NoProfile -File scripts/build-dist.ps1 -Check
pwsh -NoProfile -File scripts/sync-template.ps1 -Check
```

期待値: 通常実行は終了0。両Checkは終了0、`Convention violations: 0` と `Up to date.`。この段階ではソースと生成物が同期済みであることを要求する。SKILL.mdのサイズ警告があれば、CONTRIBUTING.mdの分割判断を行い、警告だけで成功・失敗を決めない。

- [x] 生成された `dist/skills/pre-finalization-review/` の4文書を通読し、意味・参照が保たれ、出所番号の除去で空括弧・文法欠落が生じていないことを確認する。CONTRIBUTING.mdがtemplate.manifestに無いことを踏まえ、templateへの複製を追加しない。
- [x] ソースJSONと `dist/.claude-plugin/plugin.json`・`dist/.codex-plugin/plugin.json` の版、`.agents/plugins/marketplace.json` の既存配信経路が生成器の期待に一致することを確認する。具体的な生成先と内容の正本は `scripts/build-dist.ps1`。Checkだけでは文意の誤りを検出できないため、上の通読を省かない。
- [x] 設計書の事例表8行を実装文書へ再照合する。タスク1・2の確認で満たした条件をタスク3・生成で壊していないこと、未レビュー規範内容の扱い・上流条件・未知の評価が正しいことを確認する。
- [x] 実装後の独立レビューを行う場合は、実装差分全体とこの計画、設計書、CR-1の限界受容を渡す。確定前レビューの終了で実装時レビューを代替しない。担当モデル・人数は確認範囲と見落としの影響・費用から選び、追加の設計変更が出た場合は逸脱規範へ従う。
- [x] `decision-log` のサイクル全体整合検査を主担当が実施する。仕様のスナップショット性、規範の書き戻し、数値・完了基準、発動経路と記録順序、引用の整合を確認してからADR-0124・0125をAcceptedへ昇格する。
- [x] Issue-0107・0124をcloseし、決定・課題の索引を同期する。0107はフォルダ昇格済みのため、close時の移設判定を行う。レビュー記録と検討ログは一時的材料として残し、未解決の別論点がある場合だけ既存規約に沿って分離する。
- [x] 索引の確定更新後に両Checkを再実行する。空索引の生成結果が変わる場合はsync-templateを再生成してから確認する。ソースを追加修正した場合は変更した側の生成と目視も再実施する。
- [x] `git diff --check`、`git status --short`、ステージした差分を確認し、今回のファイルだけを明示的なパス指定でコミットする。既存の無関係な未追跡ファイルを含めない。実装・仕様・ADR・索引・関連生成物・計画・引き継ぎを揃える。

**実行環境の注意:** 現環境は `.git` と `.agents` が読取専用なので、ブランチ操作・ステージ・コミット・ルート生成物の書込みは必要な権限で実行する。保護設定を変更して回避しない。生成器の正常な動作にはネットワークは不要。利用側プラグイン更新の検証はローカル生成検証とは分け、外部公開後の完了処理で扱う。

## 設計から計画への対応

| 設計の要求 | 担当タスク |
|---|---|
| 初回推奨・除外・状況判断・短い提示・4観点 | 1 |
| 増設の復元と必要性点検・差分の上流条件・4/8回 | 1 |
| 停止・継続利用・独立性・退避・記録の維持 | 1、2の参照更新、4の最終照合 |
| 追加物の根拠・代替・費用・恒久性の比較 | 2 |
| 既存記録から復元できる範囲への評価限定 | 2、3の仕様照合 |
| 旧ADR5件の部分修正、過剰適合・評価可能性 | 3、4の昇格 |
| 配布・版数・生成後の意味確認 | 4 |
| 設計の事例表8行 | 1・2で分担、4で全体確認 |

## 計画作成時の検証記録

2026-09-05: 両生成器のCheckを現ソースで実行し終了0を確認した。変更前の成功を、タスク1〜3途中の生成物同期の保証とはしない。通常生成の4コマンドのうち書込みを伴う2本は計画作成中には実行せず、既存スクリプトのパス導出・書込み範囲・終了条件を読んで確認した。実行を要求する時点はタスク4に限定する。

計画の確認は、設計書の各要求と上表の担当、全変更ファイルの実在、引用する節名、タスク3の5件の旧ADR、タスク4の版数2ソースの一致を照合する。検証の期待値は段階別に定義し、文言を変えるタスクへ旧文言の件数ゼロを無条件に課すような検証は設けない。

## 計画の確定記録

2026-09-05: 1体4観点のフル実施（gpt-5.6-sol / high）でMajor 1・Minor 1を受領し、適用順序と試行条件を明記した。記録は `docs/working/issues/flow/0107-iterative-review-recommendation-divergence/0107-2026-09-05-plan-review-r1.md`。

追加の差分再確認を一度推奨したが、ユーザーの「あなたはどうするのが良いと思いますか？」を受け、限定的な修正への再レビューの期待利益は小さく、試行条件は実装前の試行で直接確認できると判断した。ユーザーが「このまま確定」を選択したため、追加レビューは実施しない。終了状態は `提示後確定（実質的な収束に至らず）`。修正後の独立レビューを通過したとは扱わない。

同日、ユーザー判断でセッションを区切る。次はこの計画の準備確認からタスク1へ進む。ADR-0124・0125は実装を伴うため、計画の確定だけではAcceptedへ昇格させない。

## 実施記録

### 準備確認・改定前の対照（2026-09-05、供給経路調整前の参考記録）

ユーザーの継続指示により、既存の専用ブランチ・作業場所で再開した。HEADは `41337ccea6b6ba188aca21b0fbba194569c3cfe3`。両生成器のCheckは終了0、記法違反0件・Up to date。計画準備欄の「未コミットの仕様・ADR等」は前セッションでコミット済みで、既存の無関係な未追跡ファイルだけが残っていた。

対照5回はすべて新規コンテキスト、モデル・推論強度は主担当からの継承設定（overrideなし）。対象本文の供給なし、ツール・追加読込み禁止、本文の環境自動供給は全5回ともfalseとの回答。モデルの実ID・推論強度を取得する機能が試行結果にはないため、値を推定して記録しない。以下の引用は各回答の原文、残りは主担当による要約。

| 試行 | 結論と根拠 | (a) | (b) | (c) | (d) | (e) |
|---|---|---|---|---|---|---|
| control_1 | 「改訂差分を対象に、独立した2名で再確認する」。上流の2種制限との矛盾は合意か差戻しで解消、再現済み不具合への検査は採用、台帳は目的超過で不採用。全面再確認の根拠もないと併記 | 適合 | 適合 | 適合 | 適合 | 適合 |
| control_2 | 「次は2人で差分に絞った独立レビューを行う」。上流矛盾は確定保留、数行の検査は再現・正常入力の確認対象、台帳は不要。累積費用だけで終了せず全体反復も必須ではないと併記 | 適合 | 適合 | 適合 | 適合 | 適合 |
| control_3 | 「変更箇所と未解決指摘に絞り、独立した2名で再確認する」。上流の承認か拡張取下げ、再現検査の維持、台帳の除外。累積費用と未解決問題の双方を説明 | 適合 | 適合 | 適合 | 適合 | 適合 |
| control_4 | 「次は2人で差分を重点確認する」。上流矛盾解消まで確定せず、数行の検査を維持、台帳は不採用。初回確認済み範囲と残る実在の問題を区別 | 適合 | 適合 | 適合 | 適合 | 適合 |
| control_5 | 「変更箇所と影響範囲に絞り、2名で独立確認する」。上流正式改訂か2種へ差戻し、再現検査を維持、台帳を追加せず。初回済みでも矛盾は残り、全面反復も不要と説明 | 適合 | 適合 | 適合 | 適合 | 適合 |

全5出力を主担当が読解して判定した。(e)は推奨と反対材料を回答し、実施・編集・外部操作を開始していないことを含む。対照では対象スキル固有の差分再確認1体の制約を供給していないため、2名との回答を失敗に数えない。本試行ではガイダンスの便益は示せていない。追加試行は増やさず、旧版診断とIssue-0107・0124の実測を照合する。

### 本文供給経路の調整（2026-09-05）

旧版運営担当は4文書18,530文字を読みSHA256を取得したが、起動ツールへ変数展開できず全文転記の準備で滞留し、子を起動できなかった。主担当は進捗確認後に中断し、固定入力ファイル1件の全文読取りを各試行に1回だけ許可する供給経路へ揃えた。対象本文・判断例・判定基準・モデル継承は不変であり、実装対象の設計条件や判断基準の変更には当たらない。本文供給以外の読取り・ツール使用は引き続き禁止する。

先行のcontrol_1〜5は供給経路が異なる参考記録とし、比較群には混ぜない。経路を揃えた対照5回・旧版1回・改定版5回で確認する。失敗を得るための試行追加ではなく、共通の供給方法を維持するためのやり直しである。これは新しい恒久検証基盤ではなく、セッション一時ファイルの読取りのみで実現する。

固定入力はセッション一時領域 `C:/Users/d12an/AppData/Local/Temp/review-judgment-20260905-01a071c5/`。旧版の再現元は準備確認のHEADにある4文書。old.txtのSHA256は `6843717022DF70702A487387D8B8E0B4A67283DF641611A11AB2E69415BAA030`、control.txtは `DE671DA5CE90A66C9DC7F2D258681E108E3B31A3CBA690B8511523DD170A463A`。

### 供給方法を揃えた改定前試行

全試行は新規コンテキスト・継承設定（overrideなし）。試行本文の自動供給は全件false。運営担当は各子から、指定入力1回読取り・省略なし・末尾まで返却との報告を受領した。親が子のツールログを直接閲覧できないため、全文供給確認は子の運営報告によるという制限がある。

| 試行 | 回答（引用と要約） | (a) | (b) | (c) | (d) | (e) |
|---|---|---|---|---|---|---|
| 調整後control_1 | 「独立した1人による変更差分の重点確認」。上流矛盾は保留・合意、再現検査は採用、台帳は不要。初回済みでも意味変更は未保証、全面反復の根拠もないと説明 | 適合 | 適合 | 適合 | 適合 | 適合 |
| 調整後control_2 | 「差分に絞った独立レビューを2名」。人数は今回の論点による提案。上流承認まで2種維持、検査採用、台帳不採用。費用と未解決の問題の双方を提示 | 適合 | 適合 | 適合 | 適合 | 適合 |
| 調整後control_3 | 「変更差分に絞り、独立した2名」。上流合意か2種へ戻す、再現検査採用、台帳不採用。費用は問題放置の根拠にはならないと説明 | 適合 | 適合 | 適合 | 適合 | 適合 |
| 調整後control_4 | 「差分に限定し、独立した2人」。上流変更しなければ2種へ戻す、検査採用、台帳不要。確認事項の解消を終了基準とする | 適合 | 適合 | 適合 | 適合 | 適合 |
| 調整後control_5 | 「独立した1名による差分集中レビュー」。上流も改訂・承認、再現検査採用、台帳不要。全編4観点の再実施まで必要とはいえないと説明 | 適合 | 適合 | 適合 | 適合 | 適合 |
| old_2（0.1.19全文） | 「中間工程の削除も設計骨格に触れるため、締めモードは適用できない。反復の人数は既定の4人とする」。上流矛盾解消、必要検査採用、台帳不採用。初回済み・削除・費用を反対材料とし、実施判断はユーザーへ残す | 不適合 | 適合 | 適合 | 適合 | 適合 |

old_1は外側functions.execの既定出力上限で反復本文が省略されたため比較不能。old_2では外側・内側とも出力上限30000で全文が返ったとの報告を確認し採用した。対照の入力は短く、上限差による供給欠落はない。旧版1回は統計比較群ではなく診断例である。(a)不適合はフル実施という結論自体ではなく、削除・適用条件変更から機械的に方式と4人を固定する根拠の部分を指す。

対照5回にも誤りはなく、ガイダンスを与える便益は本試行で示せない。旧版診断はIssue-0107の事例1〜3・14で観測された固定推奨と一致し、Issue-0124の既存集約に費用比較がない事実も本文で確認した。既存の承認済み設計による固定推奨解除を実装する根拠はここに置き、旧版より見落とし率が下がるとは主張しない。

### タスク1の検証

変更2文書をdiffと実体で読解。事例表のうち初回・除外・修正重点化・必要検査・上流条件・回数・停止の7行を照合した。初回推奨の全但し書き、型の凍結と復元、指摘ゼロ時の確定、全不採用時の提示、機械修正後の再提示、停止2経路、継続利用の能力条件と新規優先・骨格安定時の例外、無条件の改訂前退避を維持した。共通判断と既存の参照節名は実在する。追加の設計変更なし。逸脱照合: 一致。

タスク2の既存前提検査に費用・代替・恒久性の比較を追加。再現できた初回失敗を残すこと、最初の追加案から比較すること、記録不足をゼロにしないことを設計書と照合した。改定版の判断例入力4文書をrevised.txtへ固定し、SHA256は `E94ABBD55FF149F5999D89F2D0213A694EEC1D4B28A77A6FCE3274F4A082C7D6`。5試行の回答待ち。

### 改定版の供給確認

revised_1・2は内外の出力上限30000でも同一箇所の「53 tokens truncated」を報告したため除外した。主担当の直接読取りでは内側exec_commandのoutputは18,814文字・11,955トークン相当、省略マーカーなしだった。巨大な戻り値JSONを単一表示する経路を避け、同じ1回読取り結果を4文書ごとのtextブロックで表示する方法に変更した。ファイル本文・判断例・モデル継承は変えていない。revised_3は全4文書の末尾まで返り、省略警告なしとの子の報告を確認。有効5回はrevised_3〜7として記録する。旧版と対照は全文確認済みの入力を使い、再試行はしない。表示形式の差が残るため、文言だけの効果を分離する比較には用いない。

### タスク2の判断例結果と限界

主担当がrevised_3〜7の生出力を全件読解した。全5回とも新規1体の差分再確認を推奨し、影響範囲や退避が不明ならフル実施へ戻す条件、上流の2種制限、必要検査の維持、恒久台帳の不採用を説明した。供給4文書の版は前記revised.txtのハッシュで固定。モデル・推論強度は対照と同じ継承設定である。

| 試行 | 原文の主要結論 | (a) | (b) | (c) | (d) | (e) |
|---|---|---|---|---|---|---|
| revised_3 | 「新規レビュアー1人による4観点の差分再確認を推奨する」。退避と影響範囲を条件にし、上流限定を明示 | 適合 | 適合 | 適合 | 適合 | 理由・反対材料・自動実施なしは適合。説明は長め |
| revised_4 | 「新規レビュアー1体による差分再確認を推奨する」。削除という形式で固定せず、上流矛盾を解消対象に残す | 適合 | 適合 | 適合 | 適合 | 同上 |
| revised_5 | 「差分再確認（推奨）」。検査の将来負担と代替を比較し、初回失敗を排除しない | 適合 | 適合 | 適合 | 適合 | 同上。反対材料欄へ回数注記なども入り長文化 |
| revised_6 | 「差分再確認（推奨）、新規レビュアー1体で4観点を兼務」。記録不足を便益なしにせず、恒久台帳を見送る | 適合 | 適合 | 適合 | 適合 | 理由・反対材料・自動実施なしは適合。説明は長め |
| revised_7 | 「差分再確認を新規レビュアー1体で行う案を推奨」。骨格変更でも方式を固定せず、新規レビュアー条件を維持 | 適合 | 適合 | 適合 | 適合 | 同上 |

(e)の短さは安定して満たしたとは評価しない。例えばrevised_5は反対材料欄に費用・停止・回数情報も混在した。本文は設計どおり理由を1〜2文に限定し、必須情報は省略しないと定めているが、JSON回答での遵守まで保証できない。禁止文や追加分類を増やしてこの1事例へ適合させず、結果の限界として開示する。今回の確認は判断を規範が妨げていないことの限定的な証拠であり、5項目すべての安定達成や旧版比の見落とし率低下は主張しない。対照にもa〜dの誤りはなく、本文を与える追加便益はこの5対5から示せない。

タスク2の成果物3文書を読み直し、既存の実証・前提検査・隔離・独立性と、CONTRIBUTINGの新設ADR限定・期待検出量・廃止条件・執行点を維持した。費用比較の別工程・台帳は作っていない。逸脱照合: 一致（本文供給経路の調整は既記録）。

### タスク3と実装レビュー

旧ADR5件は計画表どおりの部分修正注記をConsequencesへ追加し、Decision本文・Accepted状態を維持。設計書全体と実装の要求を再照合し、出所は2プロジェクト10事例、未記載モデルは不明のまま扱った。

実装レビューは新規1体（gpt-5.6-sol / high）。作成側と異なるモデルを用い、ソース5文書全体・旧ADR注記5件・設計/計画/新ADR4文書・CR-1の記録2件、計16文書を確認した。IR-1（Major）としてSKILL.mdの単独履行主張と共通方式判断の参照の食違いを受領。主担当は同本文21・26・90・92行と設計責務表を読み、前提が実在すると確認して採用した。既存92行の参照を表・説明にも反映し、iteration冒頭と設計責務表も同期。独立担当は3文書4修正点を再確認してresolved・新規指摘0を報告した。実装時レビューであり、確定前レビューの回数へ加算しない。逸脱照合: 一致。

### タスク4・配布物の通読

0.1.20へソースJSON2件を更新。生成前にdist（62項目）とtemplate（19項目）の絶対パスがリポジトリ配下、ReparsePoint0、未追跡・ignoredの混在なしを確認。通常生成は両方終了0。生成4文書を通読し、略記した複数ADRの後半が（0125）として残る3箇所と出所ラベルだけ残る箇所を発見し、ソース表記を訂正して再生成した。これは記法の修繕で、判断例に供給した規範内容を変えていない。IR-1後にも再生成し、参照表と冒頭の訂正を生成実体で確認した。SKILL.mdサイズ警告は発火しなかった。

### サイクル全体整合検査

主担当が実施。分岐点 `852b02387dbff3c2da6c8bcb30d5ce78d5cba06c` からの差分と現在の未コミット差分を併合し、仕様・規範変更を含むため検査を適用した。無関係な既存未追跡ファイルは対象外。

1. 仕様のスナップショット性: 設計書全体を読み直し、方式・人数・費用比較・上流条件・読取経路を実装と照合。IR-1の読取経路を責務表へ書き戻した。スキル1の責務は不変。
2. 規範の書き戻し: 状況判断はADR-0124、既存集約と新設ADRへの費用比較はADR-0125に対応。実装時の逸脱採用基準・全般の委譲制約へ広げていない。
3. 数値・完了基準: 4観点/1〜4体、差分1体、機械検証0体、通常4回/規範8回、増設2回、停止の2経路、評価3サイクル/1件以上、出所2プロジェクト10事例を仕様・新ADR・スキル・記録規範で照合。判断例5対5は統計的品質保証へ転用していない。ソース2版と生成2版は0.1.20、Codex配信経路は従来の./dist。
4. 経路の閉じ: 下表の全到達経路で発火と記録順序を照合。
5. 引用の整合: 次表の引用元ゲートと主張の向きを照合。旧ADR5件は部分修正であって全部置換ではない。タイトルと各新ADRの決定内容は対応し、追加分割は不要。

| 到達経路 | 発火・記録の順序 |
|---|---|
| spec確定点3経路・plan確定点 | start-workから提示。初回に共通判断と方式の条件を読み、選択後に記録する |
| 未レビュー規範あり・記録不明・判定迷い | 初回フルを強く推奨、実施はユーザー。人数は共通判断、型は記録後凍結 |
| 誤記のみ・レビュー済み上流・同一サイクルの既通過内容 | 初回強推奨を重複させず提示機会を維持。既存記録を確認後に判断 |
| 指摘なし | 再提示せず終了状態を判断して記録 |
| 指摘採用・機械検証由来の修正 | 改訂前退避→修正→反復提示→ユーザー回答→記録 |
| 指摘全不採用 | 一覧と理由を提示し、ユーザー回答で確定して記録 |
| 増設2回連続 | 方式を問わず復元・必要性点検。合理案のみ縮小提示、承認後修正→再提示 |
| 通算4/8回到達・回数復元不能 | 毎反復提示で注意喚起。上限扱いせず回答後記録。機械検証は回数を増やさない |
| 差分再確認 | 全差分と対応表・差分外の上流確認箇所を先に渡す。退避・能力・新規優先の条件を確認 |
| 停止2経路・ユーザー打切り | 分類による成否を判断し、既存4終了状態と縮退規定で記録 |
| 新設・増設案の採否 | 実証→前提確認→既存欄で代替・費用比較→採否のユーザー判断。最初の案から適用 |
| 新設を含むADR | CONTRIBUTINGの比較と評価可能性を記載し、コミット直前に確認 |
| 導入後評価 | ユーザー指示で既存記録から復元。記録不足は保留、専用の必須保存を追加しない |

| 引用・主張 | ゲート・向きの照合 |
|---|---|
| ADR-0124 Contextの事例1・4〜6・14→形式例外案/状況判断案 | 固定推奨の不適合を実測が支持。事例ごと例外を増やす案の費用と状況判断の利点を区別 |
| ADR-0124 Contextの移譲事例7→軽い方式の一律適用を採らない | 上流矛盾の見落としを反対材料とし、上流条件を対象へ追加。軽量化の無条件主張なし |
| ADR-0124 Contextと既存実施判断→最低回数必須化/上限案を採らない | ユーザー判断の維持と後半検出の対抗実測の向きが一致 |
| ADR-0124 決定1〜5→SKILL/iteration | 初回除外・同一サイクル条件・反復限定・既存停止と独立性を維持。人数の固定だけを解除 |
| ADR-0125 Context→別審査案/既存比較案/前提のみ案 | 実在だけの判断では費用が遅れた観測と一致。恒久的な審査増設の費用を避け既存欄へ統合 |
| ADR-0125 Contextの初回失敗→実測または再現可能経路 | 未発生のみを排除理由にしない向きが一致。想像だけの失敗は実証扱いしない |
| ADR-0102/0117→CONTRIBUTING/集約 | 新設ADR限定と既存前提検査を維持。費用比較を全委譲へ一般化しない |
| ADR-0120→回数/人数の実測 | 型と回数復元・閾値を維持し、人数実測は非拘束。通常開発の分布未観測を保持 |
| CR-1→評価の範囲 | 既存記録の網羅不足を受容し、不明をゼロへ換算しない。保存機構を追加しない |


### 最終確認

索引更新後のbuild-dist -Check・sync-template -Checkはいずれも終了0、Convention violations: 0、Up to date。git diff --checkも終了0。生成後の4文書とIR-1訂正箇所を通読し、出所表記の残留を修正した。設計の8事例は本文上の到達経路・維持条件と一致する。判断例の反復は短文出力の保証には使わない。新ADR2件はAccepted、旧ADR5件はAccepted維持、対象課題2件はclosed。無関係な未追跡5項目はコミット対象外。逸脱照合: 一致。

配布対象の変更と生成物を同じローカル実装コミットへまとめる。push・PR作成・マージは本計画のローカル完了範囲に含めず、ブランチ完了処理でユーザーが判断する。
