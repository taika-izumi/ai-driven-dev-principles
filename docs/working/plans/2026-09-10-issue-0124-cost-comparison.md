# Issue-0124 採否時の費用比較を具体化する実装記録

## 確定した範囲

ADR-0165を実装する。2026-09-10の本会話でユーザーが、独立レビュー3件の不採用案と設計確定・実装への移行を承認した。設計レビューは `docs/records/reviews/2026-09-10-issue-0124-spec-r1.md`。検証対象は同設計で定めた既存記録の判断例と配布物。自動検査器の新設、反復点検の発動条件変更、外部公開は対象外。

## 逸脱判断の既定

プラグイン0.1.25の `skills/start-work/references/plan-deviation-defaults.md` に従う。包括委任なし。設計レビューF1〜F3は不採用で確定。反復条件への変更混入を差分で確認する。新しい設計判断が必要な場合は相談する。

## タスクと検証

1. `skills/pre-finalization-review/references/review-procedure.md`手順5を前提→対応案→増設案の比較→採否の4段階へ整理する。増設の明示、文言による義務追加、比較漏れの補完を含める。既存の実証・承認・評価の条件を維持する。
2. 現用仕様 `docs/current/specs/2026-08-05-dispatch-and-pre-review-skills-design.md` の採否節を同じ順序へ更新する。`examples-and-evidence.md`に今回の根拠と評価を接続し、ADR-0125へ部分修正注記を追加する。
3. 小さな訂正・必要な増設・不要な増設・追加物の簡素化の4例を既存記録から照合する。期待値は順に「不要な費用欄なし」「比較後に必要な対策を選べる」「受容等を選べる」「追加物を減らせる」。手順5と仕様の4段階が一致し、`iteration-norms.md`の差分が0であることを確認する。
4. `scripts/build-dist.ps1`で生成し、同スクリプトと`sync-template.ps1`の両`-Check`が終了コード0となること、配布本文の参照除去後の意味が保たれることを確認する。独立レビュー・検証後に公開済み0.1.25から予定版0.1.26へ更新し再生成する。

タスクの編集内容と期待値を照合済み。4段階は判断順序、4例は確認事例であり、比較の費目数と混同しない。恒久的な検査器は追加しない。

## 実施状況

- 変更前: 両`-Check`成功。Issue-0124に費用比較の不適用が記録されている。今回エージェントの失敗を再現したものではない。
- 設計確定: フル実施1体4観点・claude-sonnet-5・1回。指摘3件を不採用とする案をユーザーが承認。ADR本文の改訂なし。

- タスク1・2: 手順5を4段階の表へ整理。現用仕様の既存の採否節を更新し、評価の根拠を既存ファイルへ接続。ADR-0125の部分修正注記を追加。新しい工程・自動検査器は作成していない。逸脱照合: 一致。
- タスク3: 下表の4例を原記録と改定本文で照合した。各段階がソース・配布・仕様に各1件存在し、ソースと配布の判断順序表は完全一致。反復規範のソース・配布の差分はともに0。エージェントの振る舞いを再現する実験は実施していない。
- タスク4: 旧版0.1.25のまま配布生成済み。両`-Check`終了0、規約違反0、同期済み。生成本文を読み、出所番号の除去による文法欠落、実在のパス入り書式例、自己参照の混入がないことを確認。`git diff --check`成功。実装レビュー後の版更新は未実施。

## 判断例の照合

| 例と原記録 | 4段階での判断・確認結果 |
|---|---|
| 小さな訂正: `0107-2026-09-05-spec-review-r1.md`のCR-3（出所事例数の訂正） | 前提は実際に参照した事例数との一致。列挙に基づく数値訂正で新義務なし。費用欄を一律に要求せず、訂正を採用できる |
| 必要な増設: `2026-09-05-review-judgment-and-cost-evaluation.md`の判断例（再現した異常入力に数行の検査を追加） | 前提は静かに壊れる生成物の防止。増設あり。実行で再現した経路と、文言修正・受容では生成物を守れないこと、検査の更新・実行負担を比較して採用できる。初回の失敗という理由で排除しない |
| 不要な増設: 同判断例および上記レビューCR-1（全指摘の恒久台帳・保存義務） | 要求は復元できる事例の評価。全件保存は要求を超える。増設案と既存記録の利用を比較し、記載・維持負担から台帳を不採用にできる。問題の指摘を認めることと提案された増設の採用を分けられる |
| 追加物の簡素化: Issue-0124「課題内容」のIssue-0119自動検査の除外 | 前提は語の置き換えで、追加した検査等が反復の対象になった。新設を続ける案には費用比較を要求でき、目的を満たす文言修正と追加検査の除外を選べる。過去に未発生という一点で排除する論理にはしない |

参照するレビューは `docs/working/issues/flow/0107-iterative-review-recommendation-divergence/` 配下、計画は `docs/working/plans/` 配下。判断例は本文の論理照合であり、新たなエージェント試行の成功や費用削減の実測として数えない。

## 実装完了時の確認

- 実装レビュー: claude-sonnet-5・Read限定・指摘0。承認済みの10文書を確認し、主担当が原本・コピーのハッシュ保全を照合。新たな設計変更なし。逸脱照合: 一致。
- 版更新: 公開元master 84f269fの0.1.25を確認し、予定版0.1.26へ更新。再生成後の両Check成功、レビュー済み本文のハッシュ一致、反復規範の差分0。
- 全体整合検査・粒度点検: 指摘なし。ADR-0165をAccepted、Issue-0124をclosedとした。詳細は `docs/records/reviews/2026-09-10-issue-0124-implementation.md`。
- タスク1〜4完了。ローカルmasterへの取り込み・振り返り・公開は別の工程として残る。
